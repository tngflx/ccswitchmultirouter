# Engineering Journal (newest first)

## 2026-09-22 - Preserve maintained reasoning fallback during MultiRouter catalog projection

- **What happened:** The live MultiRouter wizard stopped at Step 4 with `Codex subagent V2 configuration is incomplete (unknown_reasoning_capability_requires_declaration)` while two enabled profiles were `deepseek-v4-flash` and `deepseek-v4-pro`.
- **Root cause:** Existing provider catalog rows had no persisted `reasoning` metadata. Runtime reasoning code knew exact DeepSeek V4 capabilities through the maintained builtin fallback, but `codex_catalog_model_specs` only consulted the shared resolver and provider-config declarations. The projected specs therefore marked those routable models as unknown and the save validator rejected the wizard payload.
- **What we did:** Added the exact-model maintained builtin reasoning fallback (including `upstreamModel`) to catalog projection, after explicit/shared/provider-config metadata so declared capabilities remain authoritative. Added a regression assertion for a metadata-less `deepseek-v4-flash` catalog row.
- **Evidence:** `cargo test --manifest-path src-tauri/Cargo.toml --lib codex_catalog_text_only_capabilities_override_hardcoded_name` passed **1/1**; `cargo check --manifest-path src-tauri/Cargo.toml --lib` passed; `pnpm typecheck` passed. Live logs showed the save rejection at 23:09:41 on September 22, 2026. Existing request logs showed upstream 200 responses, so this wizard failure was validation/configuration rather than proxy transport.
- **What NOT to do again:** Do not make the user manually declare capabilities for exact models already covered by the maintained builtin resolver, and do not weaken the save validator; fix the catalog projection that feeds it.

## 2026-09-21 - Complete port-ownership retry fix and live capture verification

- **What happened:** The user reopened CCSwitch and requested completion of the remaining fix plus live verification.
- **Root cause:** The retry fix was present in source but the active development binary still predated it. The earlier capture failure was caused by the Tauri window being minimized, not by an absent window handle.
- **What we did:** Restored the Tauri window non-activatingly with `ShowWindowAsync(SW_SHOWNOACTIVATE)`, captured the live 1870x997 surface, and kept the startup change that stops retrying `PORT_OWNERSHIP_GUARD` errors while retaining retries for transient startup failures.
- **Evidence:** Capture helper passed with `PrintWindow=True`, `NonBlankRatio=1`, and 738 sampled colors at `C:\Users\Felix\.cc-switch\inspection\capture-20260921-123554.png`. `cargo check --lib`, `pnpm typecheck`, targeted Rust regressions **2/2**, and focused proxy UI/hook tests **7/7** passed. The running binary remains older than `src-tauri/src/lib.rs` (binary 10:32:10; source 12:01:10), so live runtime proof of the new retry branch requires the user to restart `pnpm dev`.
- **What NOT to do again:** Do not stop at a minimized-window error when a non-activating restore is allowed, and do not claim a source fix is live until binary and source timestamps are compared.

## 2026-09-21 - Stop retry storms when proxy port ownership is unverifiable

- **What happened:** Historical startup logs showed twelve identical takeover retries within roughly six seconds when port 15721 was occupied by a process CCSwitch could not safely identify.
- **Root cause:** Startup retried every takeover error uniformly. `PORT_OWNERSHIP_GUARD` is a safety decision requiring user action or a port change, not a transient bind race; retrying cannot make the foreign process verifiable.
- **What we did:** Startup takeover now stops retrying immediately for `PORT_OWNERSHIP_GUARD` errors and emits one actionable error telling the user to release the port or choose another port. Transient non-ownership errors retain the existing bounded retry policy.
- **Evidence:** Regression `tests::startup_takeover_does_not_retry_unverifiable_port_ownership` passed **1/1**; `pnpm typecheck` passed; scoped Rust formatting and `git diff --check` passed. The live window remained hidden to tray, so the requested capture helper still could not find a visible CCSwitch window; no foreground activation or process termination was performed.
- **What NOT to do again:** Do not retry an unverifiable foreign listener as if it were a transient startup race, and do not kill an owner without the existing identity, executable, start-time, scope, and protocol proof.

## 2026-09-21 - Live restart verification confirms Codex takeover recovery path

- **What happened:** After the user restarted the development app, we rechecked the actual running processes, executable timestamp, startup log, proxy traffic, and targeted tests.
- **Root cause:** The previous stale-binary blocker is cleared. The observed startup warnings were recovery evidence from the prior unclean exit, not a current takeover failure.
- **What we did:** Verified CCSwitch PID 22716 started at 10:32:11, the supervisor at 10:32:21, and the Codex/ChatGPT shell as a child of CCSwitch at 10:32:44 with `--remote-debugging-port=9229`. Verified startup restored `codex` takeover, started `127.0.0.1:15721`, completed MultiRouter projection, injected 114 models, and forwarded subsequent Codex requests to `https://api.sublyx.org/v1/responses`. No source changes were needed from this verification pass.
- **Evidence:** Current startup segment contained **0 errors**, **2 expected recovery warnings**, **1 takeover restoration**, **1 model-menu injection**, and **245 proxy request records** through the inspected runtime. `pnpm typecheck` passed; `cargo check --manifest-path src-tauri/Cargo.toml --lib` passed; Codex Desktop tests passed **49/49**; proxy-service tests passed **94/94**; focused proxy UI/hook tests passed **7/7**. The capture helper could not capture the Tauri window because it was hidden to tray; no foreground or disruptive UI action was used.
- **What NOT to do again:** Do not treat the `UncleanExit` recovery warning or the historical September 19 port-owner refusal as evidence that the September 21 startup failed; distinguish historical failures from the current process generation and verify parent/child ownership before making runtime claims.

## 2026-09-20 - Accept the current Codex summary turn envelope without weakening action rejection

- **What happened:** A live disposable-task acceptance on Codex app-server `0.155.1` reached and completed the manual summary turn, but CCSwitch rejected it as `Manual summary turn used a forbidden action item: userMessage` before creating the fresh root.
- **Root cause:** Current `thread/read(includeTurns=true)` returns the summary turn's own persisted `userMessage` together with `reasoning` and `agentMessage`. The validator allowed only assistant/reasoning items and mistook the expected prompt envelope for a tool/file action.
- **What we did:** Allow a `userMessage` only when its serialized content contains the exact internal `manual-summary-v1` marker; unrelated user messages and every unknown/tool/action item remain fail-closed. Added a live-shaped regression fixture and an explicit unrelated-user-message rejection test.
- **Evidence:** The live turn contained exactly `userMessage`, `reasoning`, and `agentMessage`; the agent returned all eight required headings and used no tools. Updated summary tests passed **18/18**; Request Health tests passed **6/6**; the generated Rust compatibility-script contract passed **1/1**; `pnpm typecheck` passed. End-to-end fresh-root acceptance still requires the rebuilt renderer template to be loaded.
- **What NOT to do again:** Do not equate the summary turn's own authenticated user prompt envelope with a tool action, and do not broadly allow arbitrary user messages; require the internal marker exactly.

## 2026-09-20 - Make Request Health notification dismissal a bounded snooze

- **What happened:** The native “Don't remind me (continue)” action permanently set `windowsNotificationsEnabled=false`, which disabled the only interactive Request Health surface indefinitely and made “Summarize + new session” unavailable until manual settings repair.
- **Root cause:** A temporary per-notification dismissal was wired to a global permanent feature toggle; the setting had no expiry or preset duration.
- **What we did:** Kept notifications enabled, added a persisted snooze expiry with a configurable 1/4/24-hour preset, made review activation honor expiry, and exposed the preset plus an explicit summarize-unavailable warning in the Request Health UI. Restored the user's on-disk preference to notifications enabled.
- **Evidence:** `tests/lib/codexSummaryHandoff.test.ts` passed **17/17**; Request Health component tests passed **6/6**; `pnpm typecheck` passed; `cargo check --lib` passed; `git diff --check` passed. Live acceptance remains pending because the running process predates this source change and must be rebuilt/restarted.
- **What NOT to do again:** Do not persist “Don't remind me” as an indefinite global disable when the user only dismissed one review; snooze the notification and preserve the review/summarize path.

## 2026-09-20 - Re-audit Codex 0.155.1 and restore the summarize workflow boundary

- **What happened:** Live Codex CLI/app-server `0.155.1` inspection showed oversized requests were being auto-approved, and the manual “Summarize + new session” path would fail against the current app-server contract if selected.
- **Root cause:** The persisted setting had `requestHealth.windowsNotificationsEnabled=false`, which intentionally returns `ContinueOnce` before creating a review token; the only interactive surface for choosing summarize is the Windows notification. Separately, the handoff script still used removed/unsupported app-server fields and methods: `project/list`, `thread/metadata/update.projectId`, `thread/start.projectId`, `thread/start.historyMode`, and `thread/start.environments`. The installed schema only accepts legacy/paginated thread history as a returned contract, and current paginated creation is unsupported.
- **What we did:** Restored the on-disk Windows reminder preference to `true`; added a Request Health UI warning when summarize is enabled but reminders are disabled; removed unsupported project discovery/metadata mutation and history/environment copying from fresh-session creation; retained strict root-ID, workspace, model/provider/reasoning, title, no-tool acknowledgement, and no-compaction invariants.
- **Evidence:** Current live logs show request-health diagnostics at 0.5-2.1 MB with exact client/transformed/upstream accounting and upstream HTTP 200; `tests/lib/codexSummaryHandoff.test.ts` passed **17/17**; Request Health component tests passed **6/6**; generated Rust compatibility-script test passed **1/1**; `cargo check --lib` passed; `pnpm typecheck` passed. `codex app-server generate-ts` from installed `0.155.1` confirmed the schema; official OpenAI documentation confirms `thread/start`, `thread/read`, `turn/start`, and `thread/name/set`, and documents paginated creation as unsupported.
- **What NOT to do again:** Do not diagnose an absent summarize action from request size alone; inspect notification settings and review-token events first. Do not validate app-server integrations with synthetic `historyMode="full"` or project mutation fixtures that the installed protocol does not accept.

## 2026-09-20 - Localize session traffic UI and complete OAuth test boundary

- **What happened:** The Codex session-traffic panel was mounted and functionally tested, but its primary headings/status/sync/error strings were hardcoded in Chinese, and OAuth component tests emitted an unhandled MSW warning for the account-pool policy query.
- **Root cause:** The panel port preserved presentation text inline instead of using the locale contract; the shared Tauri MSW fixture predated the account-pool query.
- **What we did:** Added the session-traffic translation namespace to all four locales for the panel's top-level and collector-status strings, routed those strings through `react-i18next`, and added a deterministic `get_codex_account_pool_policy` MSW response.
- **Evidence:** Current-checkout focused Vitest passed **12/12** (`CodexSessionTrafficPanel`, `CodexOAuthSection`) without the previous MSW warning; `pnpm typecheck` passed; `git diff --check` passed. The panel still has legacy hardcoded detail/table copy outside this first namespace slice and should not be described as fully localized.
- **What NOT to do again:** Do not call a UI port complete when the component is mounted but its user-facing copy bypasses i18n or its tests depend on unhandled IPC requests.

## 2026-09-20 - Port OAuth pool soft-avoidance recovery after reference audit

- **What happened:** Re-audited `BigStrongSun/ccswitchmulti` and `farion1231/cc-switch` after the latest local merge. Most recent fork and upstream proxy fixes were already present semantically; the fork's OAuth account pool still returned no candidates when every account was temporarily soft-avoided or cooling down.
- **Root cause:** Soft avoidance was treated as a hard empty-pool condition. The forwarder therefore produced an immediate `503 NoAvailableProvider` without probing any upstream, so no success could clear the transient state and the outage could persist for the full avoidance window.
- **What we did:** Ported `BigStrongSun/ccswitchmulti` commit `4861a2046` (the relevant source portion of release v3.20.2-16): preserve eligible pool entries, fall back to the earliest recovering account, retain fail-closed behavior for invalid credentials/quota reserves, and add actionable pool diagnostics.
- **Evidence:** `cargo test --manifest-path src-tauri/Cargo.toml --lib codex_oauth -- --test-threads=1` passed **122/122**; `cargo check --manifest-path src-tauri/Cargo.toml --lib` passed; `pnpm typecheck` passed; staged diff check passed. The pre-existing dirty `pnpm-lock.yaml` was not changed. No original-repository commit was ported because the inspected candidates were already implemented or incompatible with the fork's custom contracts.
- **What NOT to do again:** Do not treat temporary pool avoidance as credential failure or import whole release snapshots. Preserve the fork's pool/auth ownership and port only the bounded recovery invariant with a regression test.

## 2026-09-20 — Merge origin maintenance changes without restoring obsolete provider-owned ordering

- **What happened:** `origin/main` had one divergent commit (`dc1d92d32`) touching eleven files. Its useful changes overlapped newer local work, while its provider-level model-order mutation and older desktop-capture playbook had been superseded by router-owned global ordering and the reusable non-activating capture workflow.
- **Root cause:** Both branches independently fixed the same quota-health and model-order incidents at different ownership layers. A textual merge would have reintroduced source-provider `sortIndex` writes, stale-alias validation during global ordering, duplicate capture instructions, and weaker quota status matching.
- **What we did:** Audited every incoming file and every conflict hunk. Kept global order exclusively in schema-v2 `codexRouting.modelOrder`; retained the stricter 402/403/429 terminal-quota boundary and existing credential/unknown-402 regressions; incorporated provider-update failure logging, richer model-order failure context, the Open Log Directory action, the stronger direct/pool quota assertion, and four missing historical journal entries. Omitted the redundant root `live-capture.png`, obsolete provider-order IPC/service/API, and superseded capture playbook.
- **Evidence:** Pre-commit `cargo check --manifest-path src-tauri/Cargo.toml` and `pnpm typecheck` passed. `CodexRouterWorkspacePage.test.ts` passed **103/103**; the terminal-quota regression passed **1/1**; `git diff --cached --check` passed; all required fork identifiers remained present. The full backend suite did **not** run: `cargo test` failed replacing the live `src-tauri/target/debug/cc-switch.exe` with Windows OS error 5. The full frontend suite did **not** pass: observed timeouts were `CodexFormFields` model-specific API-key-group editing (**1**), `StreamRetryToggle` (**2**), and `LanguageSwitcher` (**1**); the Vitest process then remained open and the user explicitly requested stopping the wait and skipping further test work, so no final aggregate summary or isolated reruns were obtained.
- **What NOT to do again:** Do not revive provider-catalog ordering after ownership moved to the router, broaden health-neutral errors beyond recognized quota statuses, duplicate an older desktop automation policy over the current helper-based workflow, or report a final suite as green when it was locked, timed out, or stopped.

## 2026-09-19 — Surface compatibility retries immediately in the probe dialog

- **What happened:** The new backend compatibility-retry event was initially accepted by the frontend contract but ignored by the progress dialog.
- **Root cause:** The dialog only learned about fallback adaptations from the completed probe record, so users received no live explanation during the retry.
- **What we did:** Mapped `tool_schema_moonshot_mfjs` retry events to the existing localized safe-fallback indicator and added a regression test for the live event path.
- **Evidence:** `pnpm typecheck` passed; `CodexProtocolProbeProgressDialog.test.tsx` passed **8/8**.
- **What NOT to do again:** Do not add a progress event without wiring it into the live progress reducer and testing the user-visible state.

## 2026-09-19 — Emit explicit compatibility-retry progress for schema fallback

- **What happened:** Added a structured `compatibility_retry` progress event when the deep probe retries a forced-tool request after an explicit schema rejection.
- **Root cause:** The result contained fallback evidence only after the branch completed; live progress could not explain why the probe was retrying.
- **What we did:** Added compatibility rule/trigger/change enums, emitted the event only for the existing bounded schema fallback, and extended the TypeScript event union without changing the request payload or retry policy.
- **Evidence:** `cargo check --manifest-path src-tauri/Cargo.toml` passed; protocol compatibility tests passed **81/81**; `pnpm typecheck` passed; `git diff --check` passed.
- **What NOT to do again:** Do not emit adaptation events for outages, generic HTTP failures, or ambiguous responses; keep them tied to explicit evidence.

## 2026-09-19 — Preserve structured protocol metadata without replacing the fork runner

- **What happened:** Continued the protocol-evidence port by adding explicit tool-schema dialect/evidence and history-replay metadata to probe branch results.
- **Root cause:** The fork recorded only a generic safe-fallback tag, so consumers could not distinguish the default OpenAI schema from a schema accepted after explicit rejection, nor see the replay policy associated with the selected transport.
- **What we did:** Added backward-compatible serde enums and optional TypeScript fields, populated current behavior as `open_ai`/`unspecified` by default, marked successful conservative retries as `moonshot_mfjs`/`explicit_rejection`, and mapped Chat versus Responses replay policy without replacing the existing request builder.
- **Evidence:** `cargo check --manifest-path src-tauri/Cargo.toml` passed; protocol compatibility tests passed **81/81**; `pnpm typecheck` passed; `git diff --check` passed.
- **What NOT to do again:** Do not import upstream request-builder internals wholesale just to expose metadata; preserve the existing probe contract and add compatible evidence fields first.

## 2026-09-19 — Mount session traffic observability without removing legacy router traffic

- **What happened:** The ported `CodexSessionTrafficPanel` existed and matched upstream, but the router workspace still rendered only the older inline session summary.
- **Root cause:** The component and collector query had been added as orphaned code; `CodexRouterWorkspacePage` did not import the panel or subscribe to `useSessionCollectionStatus`.
- **What we did:** Mounted the panel in the traffic view, added collection-status querying only while that view is active, and extended the router test mock. Kept the existing provider and legacy session traffic sections in place to avoid dropping fork-specific behavior during this incremental port.
- **Evidence:** `pnpm typecheck` passed; `CodexRouterWorkspacePage.test.ts` passed **103/103**; `CodexSessionTrafficPanel.test.tsx` passed **10/10**.
- **What NOT to do again:** Do not treat a copied component and passing isolated component tests as a complete feature port until the owning workspace mounts it and its integration mocks cover the new hook.

## 2026-09-19 — Surface bounded protocol adaptation evidence in probe results

- **What happened:** Continued the deep-probe adaptation slice by carrying safe tool-schema fallback evidence from the backend into the frontend result and progress dialog.
- **Root cause:** A successful fallback was previously invisible: the probe could recover, but users could not tell that the provider rejected the original schema and passed only after a conservative retry.
- **What we did:** Added `tool_schema_safe_fallback` adaptation evidence to transport branch results, populated it only when the explicit schema-rejection fallback succeeds, added TypeScript typing, and displayed localized evidence in the probe dialog.
- **Evidence:** `cargo check --manifest-path src-tauri/Cargo.toml` passed; `pnpm typecheck` passed; locale tests passed **7/7**; `git diff --check` passed.
- **What NOT to do again:** Do not silently adapt a successful probe without recording the adaptation that made it pass.

## 2026-09-19 — Add bounded tool-schema fallback to deep protocol probes

- **What happened:** Continued the upstream adaptation port by adding one narrowly-scoped deep-probe fallback for explicit tool-schema validation failures.
- **Root cause:** Some providers reject the richer function schema shape while accepting a conservative nonce-enum schema. The old runner stopped immediately on any forced-tool failure and could not distinguish that case from an outage.
- **What we did:** On `ToolSchemaRejected` only, the forced-tool stage retries once with a minimal nonce-bound schema (`additionalProperties: false`, no competing tools). No fallback occurs for 429/503, reasoning replay failures, network errors, or generic HTTP failures. Added localized UI labels and regression coverage.
- **Evidence:** `cargo test --manifest-path src-tauri/Cargo.toml protocol_compatibility::cases --lib` passed **6/6**; capture tests passed **6/6**; `cargo check` passed; `pnpm typecheck` passed; locale test passed **7/7**.
- **What NOT to do again:** Do not retry protocol probes on availability errors or broadly mutate schemas after ambiguous failures. Keep adaptation single-purpose, bounded, and tied to explicit evidence.

## 2026-09-19 — Introduce an immutable prepared-request boundary for protocol probes

- **What happened:** Continued the safe upstream port by separating logical probe construction from transport-specific wire transformation.
- **Root cause:** The runner previously rebuilt the logical request and transformed it inline inside `send_case`, leaving no explicit boundary for proving that the sent body was finalized before dispatch.
- **What we did:** Added `PreparedProbeRequest`, which retains logical and wire bodies plus transport and policy fingerprints. The runner now finalizes one prepared payload before sending and logs only the non-secret fingerprint/size metadata. Existing light/deep behavior and IPC contracts remain unchanged.
- **Evidence:** `cargo test --manifest-path src-tauri/Cargo.toml protocol_compatibility::cases --lib` passed **5/5**; `cargo check --manifest-path src-tauri/Cargo.toml` passed; `pnpm typecheck` passed; `git diff --check` passed.
- **What NOT to do again:** Do not merge the upstream request-builder graph by replacing the fork’s runner contract. Establish the immutable payload boundary first, then add provider-specific adaptation cases behind it.

## 2026-09-19 — Add safe protocol-probe rejection evidence without replacing the request builder

- **What happened:** Followed the upstream comparison by porting the low-risk part first: explicit tool-schema/reasoning-replay rejection classification and data-only SSE event-type normalization.
- **Root cause:** The fork treated every non-2xx probe response as a generic HTTP failure and ignored event types embedded in SSE JSON payloads. That prevented the probe from distinguishing adaptation-worthy validation failures from transient availability failures.
- **What we did:** Added redacted `ToolSchemaRejected` and `ReasoningReplayRejected` capture errors for 400/422 responses only; 429/503 remain ordinary HTTP failures. Mapped those errors through the existing redacted failure contract and exposed the new failure kinds to TypeScript. Added fixture coverage for schema rejection, reasoning replay rejection, rate-limit safety, and data-only SSE events.
- **Evidence:** `cargo test --manifest-path src-tauri/Cargo.toml protocol_compatibility::capture_tests --lib` passed **6/6**; `cargo check --manifest-path src-tauri/Cargo.toml` passed; `pnpm typecheck` passed; targeted frontend tests passed **33/33**.
- **What NOT to do again:** Do not import the newer upstream request-builder/adaptation graph wholesale. Keep the existing probe contract stable and add evidence boundaries incrementally.

## 2026-09-19 — Port session observability and paginated-history recovery follow-up

- **What happened:** Ported the upstream session-traffic observability panel, local session-collector status plumbing, and the paginated-history recovery follow-up into the current fork without replacing unrelated dirty-tree work. Added the collection-status IPC/query contract and retained the existing light/deep protocol-probe contract after auditing the incompatible newer adaptation branch.
- **Root cause:** The fork had the initial config-consistency surface and subagent usage data, but no collector status contract or evidence-aware traffic panel, while the newer upstream probe UI depended on types and request-builder contracts absent from this checkout.
- **What we did:** Added the evidence-aware traffic panel and fixtures/tests, collector service and status command, paginated-history repair modules, and follow-up consistency dialog/hook/API versions. Preserved fork-side protocol probe files rather than applying an incompatible latest snapshot. Added optional unlock-result history fields needed by recovery UI.
- **Evidence:** `pnpm typecheck` passed; targeted Vitest passed **36/36** (session traffic, config consistency, recovery hook, usage formatting); `cargo check --manifest-path src-tauri/Cargo.toml` passed; `cargo test --manifest-path src-tauri/Cargo.toml codex_history --lib` passed **51/51**.
- **What NOT to do again:** Do not copy a whole upstream subsystem snapshot into a dirty fork when its type/request contracts have moved independently; port the owning backend/frontend boundary together and compile after each boundary.

## 2026-09-19 — Resolve leftover protocol-probe conflict markers that made the checkout unloadable

- **What happened:** The working tree still contained unresolved `<<<<<<<` markers in the protocol compatibility backend, probe progress dialog, and TypeScript API. The Rust parser consequently failed with an unclosed-delimiter error, which could surface as an application startup/runtime crash rather than a normal feature failure.
- **Root cause:** An earlier attempted convergence of the fork's light/deep protocol probe API with a newer upstream adaptation-probe implementation was left half-merged. The fork's commands and tests already depended on the light/deep `run_protocol_compatibility_probe_in_mode` contract, while the conflict-side adaptation code required a different `send_case` signature and additional request types.
- **What we did:** Audited both conflict sides and kept the fork-side implementation for all five conflicted files so the existing command/API/test contract remains coherent. Removed every conflict marker, staged the resolved files, and recompiled before doing further UI verification. The upstream adaptation side was not silently combined because its request builder was not present in the fork-side runner and textual concatenation would have produced invalid or semantically mixed probe requests.
- **Evidence:** `cargo check --manifest-path src-tauri/Cargo.toml --lib` passed; protocol runner tests passed **14/14**; `pnpm typecheck` passed; targeted frontend tests passed **75/75**; repository `git diff --check` passed; repository-wide marker search returned no conflict markers. Full Rust tests were attempted but blocked at final executable replacement by Windows `Access is denied` because the normal development binary is in use. Full frontend tests were attempted and exposed two pre-existing timeout files (`StreamRetryToggle` and `LanguageSwitcher`) amid otherwise passing tests; they were not falsely reported as green.
- **What NOT to do again:** Do not leave a half-merged probe implementation in the checkout, and do not combine incompatible `send_case`/request-shape contracts by taking both conflict sides textually. Resolve the owning protocol API first, then compile and run the probe tests before claiming the app is runnable.

## 2026-09-19 — Make Codex takeover toggles and startup recovery own one verified runtime transition

- **What happened:** Confirming the main-window Codex restart prompt changed backend takeover state but left the switch displaying its previous value. Separately, startup restored persisted Codex takeover while an already-running Codex/ChatGPT unified shell kept its stale runtime snapshot, so requests could continue outside the restored route.
- **Root cause:** The confirmed-restart UI called the raw Tauri API instead of the shared takeover mutation, bypassing pending state, success feedback, and the only takeover-status cache invalidation. At startup, the routing writer and the Desktop lifecycle were separate: the historical launch gate deliberately treated an existing Desktop as idempotent even when persisted takeover required a cold runtime reload. The restart command also forgot to snapshot the running executable before termination and could leave Codex closed when the takeover mutation failed.
- **What we did:** Routed confirmed restarts through a dedicated takeover mutation that remains pending until proxy and takeover queries refetch. The backend now remembers the verified Desktop executable, always attempts relaunch after a confirmed stop even if the takeover mutation fails, and reports both failures when necessary. Persisted Codex takeover startup now stops only the verified Codex shell (including the `ChatGPT.exe` shell from the `OpenAI.Codex` package, never lowercase Codex CLI/app-server or standalone ChatGPT), restores takeover, and relaunches the shell that was running; this does not depend on the separate launch-Codex setting. Process-inspection failure is fail-closed. Current `BigStrongSun/ccswitchmulti` and `farion1231/cc-switch` history plus open/closed PR and issue searches contain no equivalent complete fix; upstream's existing launch gate explicitly skips an already-running Desktop.
- **Evidence:** Focused proxy toggle/hook tests passed **7/7**; scoped Prettier, scoped Rust formatting with child-module traversal disabled, four-locale takeover-key validation, and scoped `git diff --check` passed. `pnpm typecheck`, `cargo check --lib`, repository-wide Rust formatting, and repository-wide `git diff --check` are **blocked by pre-existing unresolved merge conflicts** in `protocol_compatibility` files, `CodexProtocolProbeProgressDialog.tsx`, and `src/lib/api/protocol-compatibility.ts`; those conflicts were not modified. Live acceptance was not run because the development app must be rebuilt from a conflict-free tree first.
- **What NOT to do again:** Do not bypass query-owned mutations from a confirmation branch, do not call a takeover restored while the verified client shell still holds the pre-takeover runtime snapshot, do not terminate before remembering the executable needed for relaunch, and do not let a failed config mutation silently strand the client closed.

## 2026-09-17 — Add a non-interactive Codex warning without duplicating approval logic

- **What happened:** The user wanted a Codex in-app warning alongside Windows Request Health notifications, but explicitly rejected a second CCSwitch approval card. A second decision implementation would risk conflicting approvals and stale UI.
- **Root cause:** The existing Windows toast was the only visible pending-review surface, while the Codex renderer compatibility layer already had a CDP lifecycle suitable for an advisory-only display. The missing distinction was between an informational renderer warning and the authoritative backend approval token.
- **What we did:** Added one renderer advisory banner synchronized from the backend pending-review token. It is non-interactive, expiry-bound, persisted only as short-lived display metadata for renderer reload recovery, and removed through the existing single `resolve_pending_review` path. Windows remains the only surface that can Continue, Block, or Summarize + new session. Added shared CDP synchronization helpers and regressions for deduplicated rendering, central removal, expiry cleanup, and the absence of approval/compaction actions in the injected bridge. Reference repositories contain no equivalent fork-specific Request Health flow to port.
- **Evidence:** `tests/lib/codexRequestHealthWarning.test.ts` passed **2/2**; Codex Desktop warning bridge tests passed **2/2**; Request Health backend tests passed **17/17**; handoff tests passed **17/17**; `cargo check --lib`, scoped Prettier, Rust formatting, JavaScript syntax, and `git diff --check` passed.
- **What NOT to do again:** Do not add a second approval state in the renderer, let an injected banner resolve requests directly, or make the Codex private DOM the authoritative safety boundary.

## 2026-09-17 — Make Windows Request Health reminder opt-out reversible and failure-visible

- **What happened:** The Windows “Don't remind me (continue)” action did disable future Request Health approval reminders and the Request Health panel already exposed a checkbox to re-enable them, but the native toast callback discarded settings-persistence errors. A failed atomic settings write therefore continued the request while falsely appearing to have muted reminders, and no regression covered the mute-to-re-enable path.
- **Root cause:** The toast callback performed a full settings read/modify/write inline with `let _ = update_settings(...)`, splitting preference ownership from the settings layer and suppressing the only evidence that persistence failed.
- **What we did:** Added an atomic settings-layer mutation for `windows_notifications_enabled`; the toast callback now logs and records persistence failure and displays a localized follow-up Windows notification explaining that reminders remain enabled. Added backend coverage proving disabled reminders continue without creating a pending review and frontend coverage proving the Request Health checkbox can restore reminders through the canonical settings save path. Current `BigStrongSun/ccswitchmulti` and `farion1231/cc-switch` contain no equivalent fork-specific Request Health notification preference flow to port.
- **Evidence:** Request Health backend tests passed **17/17**; the focused disabled-notification regression passed **1/1**; `RequestHealthPanel` passed **6/6**, including re-enable persistence; workspace Request Health tests passed **2/2**; `cargo check --lib`, Rust formatting, scoped Prettier, and `git diff --check` passed.
- **What NOT to do again:** Do not suppress persistence failures for a user-facing “don't remind” action, and do not mutate the full settings object from a notification callback when one settings-owned atomic field mutation is sufficient.

## 2026-09-17 — Correct and deduplicate manual-summary fresh-session continuity

- **What happened:** Re-auditing the claimed “Summarize + new session” fixes found that the implementation still unconditionally overwrote a readable fresh-task title, verified only project/workspace/name despite claiming to verify all copied settings, duplicated client/thread validation logic between summary and fresh-session runners, and allowed repeated bridge calls to enqueue multiple fresh roots for one source task.
- **Root cause:** Continuity checks had accumulated incrementally in separate call paths instead of one owning boundary. The fallback title was treated as authoritative rather than conditional, copied settings were passed without persisted-state verification, and duplicate-root protection covered fallback clients but not repeated job dispatch.
- **What we did:** Added shared request-client discovery, exact thread reads, stable metadata comparison, and one fresh-task continuity verifier. The fresh runner now re-reads canonical persisted metadata and fails closed on project, workspace, model, provider, reasoning effort, history mode, or environment-root drift. It preserves an existing readable title and assigns `Handoff — …` only when needed. Pending fresh-session jobs now deduplicate by source task. Current `BigStrongSun/ccswitchmulti` and `farion1231/cc-switch` mains and nearby history contain no equivalent manual-summary fresh-root implementation to port.
- **Evidence:** `tests/lib/codexSummaryHandoff.test.ts` passed **17/17**; manual-summary Request Health tests passed **3/3**; the generated compatibility-script contract passed **1/1**; `cargo check --lib`, `pnpm typecheck`, scoped Prettier, JavaScript syntax, and `git diff --check` passed. The full Rust library suite passed **3,761 passed / 6 ignored**. Full `cargo test` did **not** run because the live development process locked `src-tauri\target\debug\cc-switch.exe` (`Access is denied`). The full frontend run did **not** pass: four unrelated UI tests timed out at 5 seconds (`CodexFormFields`, two `StreamRetryToggle`, and `LanguageSwitcher`) and remained timed out when rerun serially (**62 passed / 4 failed** in those three files).
- **What NOT to do again:** Do not claim a copied setting is verified merely because it was sent to `thread/start`; do not overwrite a readable destination title with a stale source-derived title; and do not protect only client fallback while leaving repeated job dispatch able to create duplicate roots.

## 2026-09-17 — Correction: live tab clipping was a DPI-unaware capture artifact

- **What happened:** The September 17 entry “Prevent MultiRouter tabs from clipping at laptop-sized widths” attributed missing right-side tabs/cards to responsive UI overflow. Repeated captures shifted which logical column was missing as grid density changed, revealing that the screenshot itself was cropped.
- **Root cause:** `scripts/capture-tauri-window.ps1` ran in a DPI-unaware PowerShell host. `GetWindowRect` returned the 1496×798 DPI-virtualized rectangle, while WebView2 `PrintWindow` painted the 125%-scaled physical renderer (approximately 1870×998), so the helper allocated a bitmap about 20% too narrow and short.
- **What we did:** Set per-monitor-v2 DPI awareness before any HWND rectangle query, with a legacy system-aware fallback. Reverted the responsive tab/status changes and regression that were based on cropped evidence; the product layout remains unchanged until a correctly sized capture proves a real defect.
- **Evidence:** Pre-fix captures consistently reported 1496×798 and cropped one grid column. The renderer process advertises `--device-scale-factor=1.25`, exactly matching the missing 20%. Corrected capture `C:\Users\Felix\.cc-switch\inspection\capture-20260918-010043.png` reports **1870×997** and shows all seven workspace tabs, all six Status subpages, and all five health cards fully inside the live Tauri window. The workspace suite passed **103/103**, `pnpm typecheck`, PowerShell syntax parsing, scoped formatting, and `git diff --check` passed; a source-order regression now requires DPI awareness before window enumeration.
- **What NOT to do again:** Do not interpret `PrintWindow` pixels as layout evidence until the capturing process and target compositor use the same DPI coordinate space; fix capture instrumentation before changing product CSS.

## 2026-09-17 — Rebuilt development binary contains the audited handoff fixes

- **What happened:** After the user restarted `pnpm dev`, the previous stale-binary blocker was rechecked against the actual process, binary, logs, and embedded strings.
- **Root cause:** The earlier live process was built before the final audit edits, so passing source tests could not establish that the normal development app had loaded them.
- **What we did:** Compared the running executable against every backend input, inspected the executable for the new summary/process/quota invariants, reviewed startup and router logs, restored the hidden Tauri window with `SW_SHOWNOACTIVATE`, and captured it with the required helper. Foreground focus remained unchanged. The startup acknowledgement modal did not expose semantic UI Automation controls and did not accept two bounded background window-message attempts, so no foreground click was used.
- **Evidence:** New process PID **45560** started at **2026-09-18 00:43:50 +08:00** from a binary written at **00:43:49**, newer than the newest backend input at **2026-09-17 23:50:34**. The binary contains `manual-summary-v1`, required-heading validation, fail-closed action-item validation, projectless-root validation, the inconclusive-process-probe guard, and terminal-quota markers. Logs show application startup, guarded unclean-exit recovery, proxy listening on `127.0.0.1:15721`, Codex live-config takeover, **114-model** Desktop injection, and a post-restart request prepared and forwarded at **00:44:54**. Capture `C:\Users\Felix\.cc-switch\inspection\capture-20260918-004701.png` verified the rebuilt live Tauri renderer was compositing; the informational routing acknowledgement covered the underlying page.
- **What NOT to do again:** Do not treat source timestamps or test binaries as proof that the running app was rebuilt, and do not escalate a hidden/modal verification into foreground input when background semantic controls are unavailable.

## 2026-09-17 — Extended dirty-tree and page-by-page audit closed six more regressions

- **What happened:** A broader audit of all current dirty changes plus the MultiRouter and About pages found six additional defects beyond the summary handoff fixes: accidental pnpm 12/package-lock drift contradicted the repository's `<12` engine; a model-list HTTP 200 body with neither `data` nor an error detail still became a silent empty catalog; repeated collision resolution multiplied rows when one upstream model had multiple persisted aliases; terminal quota HTTP 403 was classified as an account-pool credential failure; an inconclusive Windows process probe allowed automatic Codex CLI update to proceed; and raw/oversized backend errors could bypass frontend redaction or flood the model-order page. The live Overview capture also exposed the seven-tab workspace strip clipping at the right edge.
- **Root cause:** Several boundaries were only partially fail-closed: package-manager self-management rewrote owned metadata; model-fetch and process probes treated missing evidence as success; alias projection was not idempotent; pool classification checked generic 403 before terminal-quota semantics; exact-error rendering had no shared redaction/length boundary; and the seven-column tab layout activated too early for scaled WebView windows.
- **What we did:** Restored `package.json` and `pnpm-lock.yaml` byte-for-byte to `HEAD`; reject model-list envelopes missing `data`; deduplicate collision owners and emitted visible/upstream identities; classify recognized 402/403/429 terminal quota as quota before credential handling; make process-probe failure or ambiguous output skip updates; redact both localized/raw HTTP errors and bound exact UI details to 2,000 characters; fail closed on every non-message/reasoning summary action item; and wrap workspace tabs until `xl`. Current BigStrongSun/farion1231 source, recent history, and open/closed issue/PR searches were checked for each material area; upstream has a broader model-fetch error-envelope implementation, but no equivalent manual-summary, alias-idempotency, auto-update transition, or workspace-tab fix to port.
- **Evidence:** Hidden-renderer automation visited all seven workspace tabs and all six Status subpages at a 1198×638 effective viewport; every panel rendered and `documentElement.scrollWidth === innerWidth` on every page. The About page exposed the updated auto-update control and also had no horizontal overflow. Live Tauri capture `capture-20260917-235057.png` proved the original tab clipping. Focused frontend passed **169/169**, followed by the warning-free workspace rerun **104/104**; wizard tests passed **44/44**; summary tests passed **14/14**. Backend passed model-fetch **44/44**, session-usage **49 passed / 1 ignored**, quota-related **42/42**, settings commands **12/12**, plus the focused process-probe and generated-summary-script regressions. `pnpm typecheck`, `cargo check --lib`, Rust formatting, scoped Prettier, Node launcher/cache tests **50/50**, JavaScript syntax, and `git diff --check` passed. The final live Tauri acceptance remains pending because the running binary predates the newest backend fixes.
- **What NOT to do again:** Do not let a local package manager rewrite repository toolchain ownership, interpret missing response/probe evidence as success, make catalog transformations non-idempotent, classify status codes without inspecting terminal semantics, display unredacted backend text, assume physical window pixels equal CSS breakpoint width, or describe localhost renderer navigation as live Tauri navigation.

## 2026-09-17 — Prevent MultiRouter tabs from clipping at laptop-sized widths

- **What happened:** The first live Tauri capture after rebuilding showed the MultiRouter tab strip clipping the Status label and pushing Test publish beyond the right edge at a 1496×798 physical-pixel window.
- **Root cause:** The workspace forced seven grid columns at the `lg` breakpoint, while the WebView's device scale factor made the effective CSS viewport narrower than the wide layout assumed. Seven `TabsTrigger` cells were therefore compressed or overflowed before the `xl` layout was appropriate.
- **What we did:** Keep the workspace tabs at three columns, the Status subnavigation at three columns, and link-health cards at two columns until `2xl`; only genuinely wide windows use the dense 7/6/5-column layouts. The first correction changed only the seven-column breakpoint, but rebuilt captures `capture-20260918-005310.png`, `capture-20260918-005403.png`, and `capture-20260918-005700.png` proved the live scaled window still clipped one tab, one Status subpage, and the fifth health card. Added min-width constraints and a regression asserting the final responsive contract and all seven tabs.
- **Evidence:** Live capture `C:\Users\Felix\.cc-switch\inspection\capture-20260917-235057.png` showed the original clipped strip. Rebuilt live captures through `capture-20260918-005700.png` narrowed the root cause to overly dense `xl` layouts rather than stale code. Focused workspace and formatting checks were rerun after each correction; final live capture remains the acceptance boundary.
- **What NOT to do again:** Do not treat physical window width as the CSS breakpoint width on a scaled WebView, and do not enable dense workspace/status grids at `xl` without checking the actual Tauri window.

## 2026-09-17 — Close remaining manual-summary handoff validation gaps

- **What happened:** A new audit of automatic approval dispatch plus manual coding-agent “Summarize + new session” found that the source path was not yet totally fixed: the public marker text could bypass Request Health without proving an active internal handoff; any non-empty assistant text was accepted even when required handoff sections were absent; completed summary turns were not rejected after tool/file actions; Rust stopped polling 20 seconds before the renderer's summary deadline; and fresh-session metadata did not strictly prove source identity or preserve a projectless assignment.
- **Root cause:** The implementation trusted prompt wording and non-empty output instead of validating the handoff contract at its owning boundaries. Renderer and Rust timeouts were configured independently, and project continuity validation covered named projects but not the empty/projectless state.
- **What we did:** Scope the internal marker bypass to the exact session's active handoff guard; require all eight handoff headings; reject known command, file, MCP, function/tool, and web-search action items; extend Rust summary polling beyond the renderer deadline; require `thread/read` identities to match; support string and object project roots; repair unexpected project assignment to `null`; and verify task ID, project, workspace, and discoverable name after acknowledgement. Current `BigStrongSun/ccswitchmulti` and `farion1231/cc-switch` main branches, recent history, and open/closed issue/PR searches contain remote-compaction and cross-tool handoff work but no equivalent manual coding-agent fresh-root workflow to port.
- **Evidence:** `tests/lib/codexSummaryHandoff.test.ts` passed **13/13**; `proxy::request_health::tests` passed **16/16**; the generated compatibility-script regression passed **1/1**; `cargo check --manifest-path src-tauri/Cargo.toml --lib`, `pnpm typecheck`, JavaScript syntax checking, scoped Prettier, and `git diff --check` passed. The running `cc-switch.exe` was started at **22:45:20** from a binary written at **22:44:27**, while the audited sources were written at **23:08–23:13**, so live acceptance of these final fixes is still pending a user restart/rebuild.
- **What NOT to do again:** Do not treat an internal-looking prompt as authorization, accept arbitrary non-empty text as a handoff summary, trust “no tools” wording without inspecting the completed turn, let an outer coordinator time out before its inner job, or validate only non-empty project IDs.

## 2026-09-17 - Trigger Codex CLI update after enabling its saved option

- **What happened:** Enabling automatic Codex CLI updates did not check for an update until CCSwitchMulti restarted.
- **Root cause:** The flag was consumed only by startup initialization; the settings save path never dispatched the existing updater.
- **What we did:** Dispatch the existing updater in the background only after a successful OFF-to-ON save and all rollback-capable steps. Prevent overlapping automatic checks and recheck opt-in/process state before installation. Updated the existing hint in all four locales. Current BigStrongSun and farion1231 implementation/history and open/closed PR searches exposed no equivalent opt-in transition to port; reuse the fork's existing updater. This does not change development rebuild timing or add per-tool automatic updates.
- **Evidence:** Settings unit tests passed 12/12, CLI-management unit tests 83/83, targeted frontend settings/locale tests 24/24, TypeScript checking and scoped Prettier passed. No actual package installation or live UI acceptance was performed; loading the backend change requires the user's rebuild. Full suites were not run for this incremental change.
- **What NOT to do again:** Do not equate saving an enabled flag with starting its action, or restart the user's app to hide a missing save-transition dispatch.

## 2026-09-17 — Restore the requested two-hour rebuild delay

- **What happened:** The user explicitly requested the delayed backend rebuild remain at two hours; the prior working-tree change to 50 seconds was not wanted.
- **What we did:** Restored `DEFAULT_DELAY_MS` to **7,200,000 ms (2 hours)** and updated its regression/help text. No other rebuild-delay behavior was changed.
- **Evidence:** `node --test scripts/dev-tauri-delayed.node-test.mjs scripts/prune-cargo-target.node-test.mjs` passed **50/50**; `pnpm dev -- --help` reports `7200000 / 2 hours`; scoped Prettier and `git diff --check` passed.
- **What NOT to do again:** Do not change the user's requested rebuild delay while fixing target cleanup.

## 2026-09-17 — Accept pnpm argument separators in the dev launcher

- **What happened:** Verification of the rebuild fix exposed that this workstation's pnpm 12.4.2 invokes `pnpm dev -- --delay 0` as `node ... "--" "--delay" "0"`, and the launcher rejected the separator as `Unknown argument "--"`.
- **Root cause:** The launcher parsed `process.argv.slice(2)` directly instead of removing pnpm's leading argument separator. The plain `pnpm dev` path was unaffected, but documented `--delay` and `--no-rebuild` overrides were broken under this pnpm behavior.
- **What we did:** Normalize one leading `--` before help and option parsing, with a regression covering separator and separator-free invocations. The current BigStrongSun and farion1231 reference repositories contain no equivalent delayed-launcher/cache implementation or relevant open/closed pull request to port; this remains fork-local development tooling.
- **Evidence:** Before the fix, `pnpm dev -- --delay 0` failed with `Unknown argument "--"`; after the fix it reached the expected live-session guard (`Port 3000 is already in use`). Launcher/cache Node tests passed **50/50**; scoped Prettier and `git diff --check` passed.
- **What NOT to do again:** Do not assume every pnpm version strips the script argument separator; normalize the boundary before validating launcher options.

## 2026-09-17 — Repair fresh-task isolation in manual summary handoff

- **What happened:** A later re-audit of `runFreshSessionFromSummary` found two isolation defects that the prior “project continuity” fix did not cover: the runner accepted a `thread/start` response whose ID equaled the source ID and could then hand off into the original task; and after creation succeeded, an error from naming/polling could cause a fallback app-server client to create a duplicate root. An interim claim that this had been “checked in the original project context” was false; no live fresh task had been created.
- **Root cause:** Both runner functions looped over all discovered clients and caught every error, even after a mutating request had been issued. There was also no explicit source/fresh ID boundary before sending the handoff turn.
- **What we did:** Fail closed when Codex returns the source ID as the fresh root, and stop a workflow from being replayed through another client after the summarizer starts/interrupts or the fresh runner calls `thread/start`. Removed fixed `effort: medium` overrides so the source effort is inherited rather than altered. Added focused regressions for source-ID reuse and post-creation fallback duplication, and updated the project-handoff fixture to cover canonical-root repair for a legacy task without `projectId`. Reference-repo check found no equivalent upstream implementation to port.
- **Evidence:** `pnpm exec vitest run tests/lib/codexSummaryHandoff.test.ts --reporter=verbose --no-file-parallelism` passed **9/9** after recovery; `pnpm typecheck` passed; scoped Prettier and `git diff --check` passed; `cargo check --manifest-path src-tauri/Cargo.toml --lib` passed after the source fixes. The running `cc-switch.exe` remained older than the source (started 18:59:51, binary written 14:07:13), so this is **not** live acceptance.
- **What NOT to do again:** Do not claim project-context runtime verification without an actual fresh task ID and project assignment, and do not retry a partially completed mutating workflow through another request client.

## 2026-09-17 — Fix delayed dev rebuild default and diagnose Cargo target growth

- **What happened:** `pnpm dev` left the running Tauri binary stale after Rust edits, while `src-tauri/target/debug` grew to **7.06 GiB**.
- **Root cause:** The delayed launcher default had been changed from the historical 50-second quiet period to **3,600,000 ms (one hour)**, so backend edits did not rebuild promptly. The target-size guard was **12 GiB**, so 7 GiB of accumulated debug/test artifacts was intentionally retained. The directory contains **5.56 GiB `debug/deps`** and **1.36 GiB `debug/build`**, with 13,197 files older than seven days; this is Cargo debug output, not a 7 GiB application bundle. A dirty `package.json`/lockfile had also upgraded pnpm to 12.4.2 while declaring `pnpm <12`; those files were restored to the current `main` baseline.
- **What we did:** Restored the package metadata/lockfile to `HEAD`, changed the normal backend quiet period back to **50 seconds** (use `pnpm dev:immediate` / `--delay 0` for immediate rebuild), and lowered guarded Cargo-target cleanup from **12 GiB** to **4 GiB**. Cleanup still refuses to run while the current `cc-switch.exe`/Cargo tree owns the normal target; no process was killed and no alternate target was created.
- **Evidence:** The live process is PID 49208, started at 18:59:51 on September 17, 2026, with executable timestamp 14:07:13 while source edits are newer, proving the running binary is stale. `node --test scripts/dev-tauri-delayed.node-test.mjs scripts/prune-cargo-target.node-test.mjs` passed **49/49**; script syntax and Prettier checks passed; `git diff --check` passed. `cargo check --manifest-path src-tauri/Cargo.toml --lib` passed earlier after the quota fix. A dry-run cleanup correctly reported `Cleanup deferred ... target is owned by cc-switch.exe PID 49208`.
- **What NOT to do again:** Do not call `pnpm dev` a fresh Rust rebuild when its quiet period is one hour, do not mistake `target/debug` accumulation for a production bundle, and do not run cleanup or kill the live process behind the user’s back.

## 2026-09-17 — Re-audit manual summary project continuity and truthful handoff content

- **What happened:** The earlier live acceptance created a projectless disposable task and used a summary prompt that could omit failures or call the handoff “compact,” so it did not prove the user's real `ccswitchmulti-fork` workflow.
- **Root cause:** Fresh-session creation copied `cwd` and a nullable `thread.read.projectId`, but Codex's migrated project assignment can be absent from the thread row while the canonical project is still discoverable from `project/list` roots. The new thread also received no explicit readable name, and the summary prompt lacked mandatory sections and evidence-preservation rules.
- **What we did:** Resolve the source project by longest matching canonical project root, pass project/workspace/history/model/environment metadata into `thread/start`, repair and re-read a dropped `projectId` through `thread/metadata/update`, set a bounded `Handoff — ...` name through `thread/name/set`, and verify project, cwd, and name both before and after the acknowledgement turn. Added an internal `manual-summary-v1` marker so Request Health bypasses only this intentional summary request. Strengthened the summary prompt with exact headings and “never claim unrecorded tests / never omit failures” rules.
- **Evidence:** `tests/lib/codexSummaryHandoff.test.ts` passed **10/10**; `proxy::request_health::tests::manual_summary_turn_bypasses_active_handoff_guard` passed **1/1**; `codex_desktop::tests::compatibility_script_summarizes_before_starting_a_fresh_session` passed **1/1**; `cargo check --manifest-path src-tauri/Cargo.toml --lib` passed; `pnpm typecheck` passed; generated JavaScript syntax, Prettier, and `git diff --check` passed. A real live acceptance on the rebuilt app is still pending because the running process must be restarted and the user-facing action triggered.
- **What NOT to do again:** Do not call a projectless synthetic task proof of project-preserving handoff, do not infer project continuity from `cwd` alone, and do not claim live completion before the rebuilt renderer emits and completes the actual handoff.

## 2026-09-17 — Keep terminal Codex quota exhaustion out of provider health

- **What happened:** Sublyx daily-quota exhaustion was initially normalized to HTTP 402, but four repeated Codex attempts then opened the route circuit and later reconnects received generic HTTP 503 `NoAvailableProvider`.
- **Root cause:** The response-boundary quota normalizer worked, but the outer failover layer classified every 402 `UpstreamError` as retryable and recorded it as a provider-health failure. Retry/failover eligibility and provider-health ownership were conflated, so an account-capacity result poisoned the provider circuit.
- **What we did:** Recognized terminal quota errors at the health-accounting boundary for 402/403/429 and release their circuit permit neutrally. They remain eligible for failover because another configured candidate may have quota, but they no longer increment persistent health or open the circuit. Added regressions for terminal and unclassified 402 behavior. Reference-repo audit: current `BigStrongSun/ccswitchmulti` has terminal-429 recognition but retains the same health-accounting gap; current `farion1231/cc-switch` has no equivalent terminal quota response handling, and recent open/closed PR and issue searches contained no fix for this boundary.
- **Evidence:** Live logs at 19:56:49–19:56:55 showed four Sublyx `DAILY LIMIT EXCEEDED` HTTP 402 failures followed by `[CB-004]` circuit open; a later HalfOpen quota response reopened it. `cargo test --manifest-path src-tauri/Cargo.toml --lib quota -- --nocapture` passed **41/41**, including the new direct-provider circuit regression and the existing Sublyx 402/403 normalization tests. `cargo check --manifest-path src-tauri/Cargo.toml --lib` and `git diff --check` passed.
- **What NOT to do again:** Do not count terminal subscription, billing, or daily-quota exhaustion as provider health merely because it is still eligible for failover, and do not treat a later `NoAvailableProvider` as the original upstream cause without inspecting the preceding circuit events.

## 2026-09-17 — Live manual-summary fresh-session acceptance

- **What happened:** The user required an immediate live proof that “Summarize + new session” works and does not use native compaction.
- **Root cause:** Source tests alone could not prove that the current Codex Desktop renderer, app-server bridge, summary turn, fresh root creation, and acknowledgement completion all interoperate on this machine.
- **What we did:** Created disposable idle source task `01a0af1a-ce67-7033-8969-904b295feb4a`, invoked the live renderer’s manual-summary job, required its non-empty plain-text result, then invoked the fresh-session bridge. It created root task `01a0af1c-0789-74c1-8469-f6e77c63153e` and completed acknowledgement turn `01a0af1c-08c8-7821-9351-407457d44697`. Read back both tasks and both rollout files. Corrected the remaining fresh-task prompt phrase from “compact summary” to “manual handoff summary.”
- **Evidence:** The source summary turn completed in 9.494 seconds with no tool calls or file mutation; the fresh acknowledgement completed with `Handoff received. I’m ready for your next instruction.` Both rollout files had **0** `compacted`, `contextCompaction`, `context_compaction`, `thread/compact/start`, or `responses/compact` records. The fresh task contained only the supplied handoff summary as prior conversational context and did not replay source history. After the wording correction, `tests/lib/codexSummaryHandoff.test.ts` passed **7/7**, the Rust generated-script no-compaction test passed **1/1**, `pnpm typecheck` passed, and scoped syntax/Prettier plus `git diff --check` passed.
- **What NOT to do again:** Do not call this workflow verified from mocks alone, do not test it by interrupting the user’s active working task when a disposable task suffices, and do not use “compact summary” wording for a manual coding-agent handoff.

## 2026-09-17 — Use paginated history bases for Codex usage replay

- **What happened:** Every startup and sync pass deferred the same 6.5 MB August 22 Codex rollout because its `forked_from_id` referenced a deleted ancestor, even though its `history_base.thread_id` pointed to an existing immediately preceding rollout page. The current Request Health run also prompted a re-audit of “Summarize + new session.”
- **Root cause:** Usage replay deduplication treated lineage (`forked_from_id`) as the physical replay parent and ignored the authoritative paginated `history_base`. Separately, native result text still called the manual coding-agent handoff a “compact summary,” despite the runtime no longer using compaction. The summarize option itself is approval-driven, not automatic: when Windows approval reminders are disabled, preflight deliberately continues without starting a handoff.
- **What we did:** Prefer `history_base.thread_id` as the replay parent when present, retaining existing explicit fork/subagent-parent behavior otherwise. Added a regression where the lineage parent is missing but the paginated base exists. Corrected native handoff failure/result wording to “manual summary.” Reference audit: original commit `e4b03a38c` was already adapted locally as `04c50fdeb`; original `45b9a952a` addresses incomplete file tails, not this missing-parent case; neither reference repository currently implements usage replay from `history_base`.
- **Evidence:** `services::session_usage_codex::tests` passed **49/49** with 1 ignored corpus test; the focused history-base regression passed **1/1**; `tests/lib/codexSummaryHandoff.test.ts` passed **7/7**; the Rust generated-script no-compaction test passed **1/1**; the manual-summary Request Health bypass test passed **1/1**; `cargo check --manifest-path src-tauri/Cargo.toml --lib` passed; scoped rustfmt and `git diff --check` passed. Repository-wide `cargo fmt --check` remains red only on pre-existing formatting drift in `proxy/forwarder.rs`. The running `cc-switch.exe` predates these Rust edits and still reports one deferred file, so live acceptance requires restarting `pnpm dev`.
- **What NOT to do again:** Do not use a historical lineage ancestor as the replay source when Codex provides an immediate paginated history base, do not describe manual agent summarization as compaction, and do not call the approval-button workflow automatic.

## 2026-09-17 — Verify persisted model order through the live Codex picker

- **What happened:** After rebuilding with `pnpm dev`, the user asked whether the saved ordering survived in SQLite and actually reached the running Codex Desktop model picker.
- **Root cause:** Persistence and presentation cross four independently inspectable layers: router `modelOrder` in SQLite, the compiled JSON/cache/inline catalogs, the CDP compatibility payload, and the live Statsig picker whitelist. Comparing only the first few rows could conceal later alias drift.
- **What we did:** Read the active `codex-multirouter` row directly from `~/.cc-switch/cc-switch.db`, parsed all 114 stored ranks, compared the generated catalog, `models_cache.json`, and inline `config.toml` models, then queried both live Codex renderers on CDP port 9229 without foreground input. The 15 non-identical stored names are stale derived aliases that the compiler recovers into current visible names at the same ranks; 99 names match literally. Both renderer payloads are byte-order equivalent to the published catalog, and the first 114 Statsig `available_models` entries are exactly that payload; seven Codex-native fallback models are appended afterward.
- **Evidence:** SQLite has 114 ranks. Published catalog, models cache, and inline config each have 114 models. Both live renderer payloads and the catalog share SHA-256 `544f03a10ad514f19f5e91993e7f8b19d5102affe63c661f1a96ca732b45ea7d`. Both live picker whitelists have 121 entries with the routed 114-entry prefix exactly equal to the payload, `default_model=gpt-5.6-sol-sublyx`, and `use_hidden_models=false`. The application log records successful 114-model injection into both current targets at 19:00:46 and 19:01:28.
- **What NOT to do again:** Do not require byte-identical stale visible names between persisted preferences and a regenerated catalog; verify same-rank identity recovery and then compare the actual published and renderer sequences end to end.

## 2026-09-17 — Surface and log exact model-order save failures

- **What happened:** The Model ordering page displayed a generic “Operation failed” message and directed users to logs even when the failure occurred entirely in frontend validation, so no backend log entry existed.
- **Root cause:** `workspaceErrorMessage` intentionally collapsed unknown ASCII messages, and the ordering catch neither preserved the raw detail nor reported the caught error through the existing redacted frontend logger.
- **What we did:** The ordering catch now renders the original error detail after credential redaction and reports the same exception as `[frontend] codex_model_order_save` through Tauri's log plugin, including the router provider ID. Other workspace actions retain their existing translated error mapping.
- **Evidence:** Focused ordering/error coverage passed **3/3**, including exact UI detail and stale-alias saves; the complete workspace test file passed **103/103** before the formatting-only pass; `pnpm typecheck`, scoped Prettier, and `git diff --check` passed.
- **What NOT to do again:** Do not tell users to inspect backend logs for a frontend-only failure unless that catch explicitly writes a diagnostic entry, and do not replace the only actionable error text with a generic sentence.

## 2026-09-17 — Make model-order saves router-only

- **What happened:** Reordering models could fail before IPC with a generic UI error even though model order is a router-owned preference. Replaying the removed preflight against the live database identified the first blocker as `~deepseek/deepseek-flash-latest`, which no enabled source catalog could resolve; consequently no backend save log existed for the click.
- **Root cause:** `saveOrder` persisted `codexRouting.modelOrder` but also re-resolved every visible model through every route and rewrote `sortIndex` into source-provider catalogs. That duplicated global-order ownership and made any stale/derived alias anywhere abort an unrelated ordering save.
- **What we did:** Removed source-provider resolution and writes from the ordering action. Save/reset now update only the schema-v2 router fields (`modelOrder`, display style, default effort), which the compiler already applies to the published catalog. Updated the success copy in all four locales and added a regression proving a stale route alias does not block or trigger a source-provider write. Current `BigStrongSun/ccswitchmulti` PR #26 preserves aggregate order across provider deletion but does not establish this fork's router-owned `modelOrder`; the original repository has no equivalent MultiRouter ordering owner, so neither reference implementation was ported.
- **Evidence:** `pnpm exec vitest run src/components/codex/CodexRouterWorkspacePage.test.ts --maxWorkers=1 --minWorkers=1` passed **101/101**; the focused stale-alias regression passed **1/1**; `pnpm typecheck` passed; scoped Prettier and `git diff --check` passed.
- **What NOT to do again:** Do not make a router preference save validate or mutate independently owned source catalogs, and do not require every route alias to be healthy before persisting an ordering-only change.

## 2026-09-17 — Permit systematic background-only desktop UI audits

- **What happened:** The desktop-audit rules were interpreted as allowing only one capture attempt and requiring the user to open every target page manually, which prevented systematic page-by-page bug discovery even when automation could stay entirely in the background.
- **Root cause:** Rules 22-A through 22-D conflated non-disruption with read-only single-screen capture and imposed a strict attempt budget on all automation rather than only forbidding disruptive foreground interaction.
- **What we did:** Reframed the invariant around preserving the user's focus, cursor, and workspace. Semantic background navigation, repeated state-checked captures, and non-activating restore/show are now explicitly allowed; blind coordinate guessing and foreground input remain prohibited.
- **Evidence:** `scripts/validate-agents-md.ps1` validates the updated guide.
- **What NOT to do again:** Do not equate “non-disruptive” with “no navigation,” and do not ask the user to drive the app when UIA/CDP/background window APIs can audit it without stealing focus.

## 2026-09-17 — Make persisted route aliases the frontend model-name authority

- **What happened:** The compiler honored explicit route aliases first, while the ordering/wizard path independently recomputed `<model>-<provider-suffix>` names. Provider renames and multi-alias routes could therefore make the frontend reconstruct identities that differed from the compiled catalog.
- **Root cause:** `resolveWizardModelNameCollisions` had no access to persisted routes, and `buildWizardRoutesFromSources` removed all but one existing alias for a canonical target.
- **What we did:** Passed existing schema-v2 routes into collision resolution, materialized every valid explicit alias as the visible model identity, preserved all persisted aliases for each canonical target, and used that resolution when rebuilding saved model order and plans. Upstream still recomputes names; the original repository has no MultiRouter wizard, so neither reference implementation owns this fork-specific invariant.
- **Evidence:** `pnpm vitest run tests/lib/codexMultiRouterWizard.test.ts src/components/codex/CodexRouterWorkspacePage.test.ts` passed 144/144; `pnpm typecheck` passed; compiler tests passed 28/28. The first full Rust library run had one suite-order/shared-state failure that passed immediately in isolation; the clean rerun passed **3,753/3,753** with 6 ignored.
- **What NOT to do again:** Do not derive a replacement visible name when a persisted explicit alias already defines the route contract, and do not collapse multiple aliases that intentionally point at the same upstream model.

## 2026-09-17 — Preserve provider model-fetch and workspace error details

- **What happened:** Providers returning HTTP 200 error envelopes were shown as successful empty model lists, and ASCII backend details such as `HTTP 402: DAILY LIMIT EXCEEDED` were collapsed into a generic workspace error.
- **Root cause:** The model response parser only read `data`; the UI error mapper treated any uncoded ASCII message as non-actionable.
- **What we did:** Added response-envelope fields (`success`, `msg`, `message`, `error`) and return their detail on unsuccessful/missing-data responses. Workspace errors now preserve HTTP status messages while retaining generic fallback for unrelated low-level errors. Added focused Rust and Vitest regression tests.
- **Evidence:** `cargo test --manifest-path src-tauri/Cargo.toml services::model_fetch::tests --lib` passed 43/43; `pnpm vitest run src/components/codex/CodexRouterWorkspacePage.test.ts` passed 101/101; `pnpm typecheck` passed.
- **What NOT to do again:** Do not interpret HTTP 200 as semantic success without checking the provider envelope, and do not discard actionable upstream status/detail text merely because it lacks a machine-readable snake_case code.

## 2026-09-17 - Recover orphaned provider-suffixed model order entries

- **What happened:** Comparing the live `codexRouting.modelOrder` against the compiled projection showed **15 of 115** saved order entries matching no published model, so those models silently lost their saved rank.
- **Root cause:** `modelOrder` persists _derived_ visible names. Two independent producers of those names disagree and drift: the Rust compiler uses a route's explicit alias first then applies `unique_visible_model` collision renaming (`deepseek-v4-flash-opencode-zen-opencode-go`), while the frontend ordering page names colliding models with `aliasModelName` = `<model>-<provider-suffix>` (`deepseek-v4-flash-opencode-go`). When the collision set also changes, names such as `claude-opus-4-8-opencode-zen` revert to `claude-opus-4-8`. Both cases orphan the persisted entry.
- **What we did:** Added `recover_orphaned_visible_model` to `apply_router_model_order`: when a saved name matches no current visible model and no unique canonical/upstream identity, strip the owning provider's normalized suffix and match that provider's canonical/upstream model, applying the rank only when exactly one entry matches. Shared `label_suffix` with `provider_name_suffix` so both sides derive identical suffixes. Added `router_model_order_recovers_orphaned_provider_suffixed_names`.
- **Evidence:** `cargo test --lib router_model_order` 4/4 passed. Replaying the exact rule over the live projection and live `modelOrder` resolved all 15 orphans, 0 unresolved and 0 ambiguous, giving 115/115 visible models a saved rank; e.g. `deepseek-v4-flash-opencode-go` -> `deepseek-v4-flash-opencode-zen-opencode-go` and `claude-opus-4-8-opencode-zen` -> `claude-opus-4-8`.
- **What NOT to do again:** Do not persist derived collision-suffixed visible names as the stable identity for user preferences, and do not treat the frontend ordering catalog and the Rust compiled catalog as interchangeable — they still disagree on explicit-alias collisions. The remaining structural fix is to make the ordering surface consume the backend-compiled visible names (or key the order by provider + canonical model) instead of reimplementing naming in TypeScript.

## 2026-09-17 - Reconcile the active MultiRouter projection on startup

- **What happened:** After the user restarted the rebuilt app, the compiler tests passed but the persisted projection status still showed `generatedAt 2026-09-16T03:34:05.343Z` and the old warning set. A restart did not rebuild the cached projection artifact.
- **Root cause:** `restore_proxy_state_on_startup` reconciled the raw Codex model-catalog file and restored takeover, but never called the router projection publisher (`ensure_codex_multirouter_projection`). The projection was only rebuilt by provider mutations or the Status-page forced retry, so a compiler/catalog change could remain stale indefinitely across restarts.
- **What we did:** Added `ensure_active_codex_multirouter_projection`, which resolves the active schema-v2 Router, ignores direct providers, and calls the existing non-forced `ensure_projection_with_publisher` (a no-op when the dependency fingerprint matches). Wired it into startup after takeover restoration and before Codex Desktop launch gating; a non-ready projection now keeps `codex_ready=false`. Added a regression covering active publish, no-op on matching fingerprint, and direct-provider skip.
- **Evidence:** `cargo test --manifest-path src-tauri/Cargo.toml --lib codex_multirouter` passed 100/100, including `active_startup_reconciliation_publishes_current_router_projection`; `cargo check --manifest-path src-tauri/Cargo.toml --lib` passed; scoped rustfmt checks and `git diff --check` passed. Full `cargo test` was not run; this change was verified with the library test target because the live app owns the debug binary.
- **What NOT to do again:** Do not treat the persisted projection status as authoritative after a compiler or provider-catalog change, and do not make users manually retry a projection that startup can reconcile safely.

## 2026-09-17 - Live Tauri UI audit of MultiRouter model ordering and alias surfaces

- **What happened:** The user asked for the running cc-switch UI to be re-audited after the model-order/alias work. The app was hidden to tray, so a quiet `PrintWindow` capture was blank; the window had to be restored with explicit user instruction before the WebView2 compositor painted.
- **Root cause:** The app minimizes to tray on close (`prevent_close` + `window.hide()` in `src-tauri/src/lib.rs`), and `Process.MainWindowHandle` in this build points at an 18x18 placeholder rather than the real Tauri frame, so restore/capture helpers must enumerate top-level windows and target the `Tauri Window` class.
- **What we did:** Restored the real frame, dismissed the "Codex local routing is enabled" dialog via UIA Invoke, captured seven non-blank pages with `scripts/capture-tauri-window.ps1` (overview, model ordering, model sources, routing rules, Sub-Agents, status), and verified text via the WebView2 UIA tree. Captures live in `docs/audits/2026-09-17-live-tauri-ui-audit/`.
- **Evidence:** Two findings: (1) persisted `codexRouting.modelOrder` still contains stale OpenCode Zen names (`deepseek-v4-flash-opencode-zen`, `deepseek-v4-pro-opencode-zen`, and four other legacy `*-opencode-zen` entries), which the ordering UI renders through renamed current models and the compiler flags as policy references needing attention; (2) the Routing rules candidate refresh showed one unnamed `Failed` row matching the Sublyx source, whose upstream was returning `402 / DAILY LIMIT EXCEEDED`, and no database provider has an empty name.
- **What NOT to do again:** Do not treat `Process.MainWindowHandle` as the real Tauri frame, do not treat a hidden-window PrintWindow blank as evidence, and do not promise a UI audit while the app is tray-hidden.

## 2026-09-17 - Fix compiler rank loss for renamed model order entries

- **What happened:** The live audit found `codexRouting.modelOrder` still referencing pre-collision OpenCode Zen names, and the compiled catalog no longer applied those old ranks.
- **Root cause:** `apply_router_model_order` keyed ranks only by exact visible-model name. When an alias collision renamed a visible model (or the provider catalog absorbed the renamed identifier), the old order entry no longer matched anything and its rank was silently discarded.
- **What we did:** Resolve stale order entries through canonical/upstream identity only when that identity matches exactly one catalog entry; ambiguous shared-identity cases (OpenCode Zen/Go both exposing `deepseek-v4-flash`) keep their existing warning behavior instead of smearing a rank over both models. Added two compiler regressions.
- **Evidence:** `codex_multirouter::compiler` 27/27 tests pass, including both new tests; `cargo check --manifest-path src-tauri/Cargo.toml --lib` passes; scoped rustfmt check passes. Full `cargo test` was not run because the running `cc-switch.exe` locks `src-tauri/target/debug/cc-switch.exe`; `cargo test --lib` avoids the locked binary.
- **What NOT to do again:** Do not auto-rewrite ambiguous order references into a shared provider identity, and do not run commands that link the binary while the user's app is running; ask for a user rebuild or use `--lib` checks.

## 2026-09-17 - Encode the non-disruptive Tauri capture path so audits stop getting stuck

- **What happened:** A live Tauri UI audit stalled when the running `cc-switch.exe` was hidden to tray; `PrintWindow` returned a uniform surface and the WebView2 renderer child was not exposed through child-window enumeration. The process was healthy and actively proxying, so the app itself was not broken.
- **Root cause:** Tauri closes to tray via `prevent_close` + `window.hide()` (`src-tauri/src/lib.rs`), and a hidden WebView2 compositor does not paint into `PrintWindow` captures. The working Win32 recipe existed only in transcripts, not as a runnable repo artifact.
- **What we did:** Added `scripts/capture-tauri-window.ps1`: locates the `cc-switch` process, prefers a WebView2/Chrome renderer window, captures via `PrintWindow` with `PW_RENDERFULLCONTENT`, verifies the image is non-blank (sampled unique colors + non-white ratio), and can never activate, restore, move, resize, or send input. Added `AGENTS.md` rule 22-D referencing the helper and the hidden-window failure mode.
- **Evidence:** `pwsh -File scripts\capture-tauri-window.ps1` correctly exits non-zero against the running tray-hidden app with `No visible candidate window was found ... The app may be hidden to tray`. Git diff is limited to `AGENTS.md`, `scripts/capture-tauri-window.ps1`, and this journal.
- **What NOT to do again:** Do not rediscover Win32 capture each audit; do not treat a blank `PrintWindow` capture as page evidence; do not restore a hidden window without the user's explicit permission.

## 2026-09-16 - Verification follow-up for stale credential protection

- **Correction to the entry below:** Final verification was attempted after the focused checks. The full frontend run did not complete: `StreamRetryToggle` timed out twice and `LanguageSwitcher` timed out once, those same three tests also timed out when rerun in isolation, and the remaining Vitest process stopped producing output until it was interrupted. The full `cargo test` command failed with OS error 5 while replacing the live `target/debug/cc-switch.exe`. The complete Rust library suite ran 3,755 tests: 3,747 passed, 2 failed, and 6 were ignored; the failures were unrelated shared-environment tests (`LOCALAPPDATA` missing and a temporary pricing path disappearing), and both passed 1/1 when rerun individually. The credential-preservation regression passed in both focused and full-library runs.
- **What NOT to do again:** Do not report a final suite as passed when it timed out, hung, or was blocked by the live executable; preserve the exact failing test names and isolated rerun evidence.

## 2026-09-16 - Prevent model-order saves from restoring stale provider credentials

- **What happened:** OpenRouter DeepSeek requests began returning `401 User not found` even though the route and model alias were valid. The stored OpenRouter key was later corrected, after which both direct OpenRouter and live CCSwitch requests succeeded.
- **Root cause:** The Model ordering workspace constructed complete source-provider objects from cached page state and sent them through the generic `update_provider` command merely to change model `sortIndex` values. If a credential was edited after that workspace snapshot, saving model order could restore the stale cached `auth` object and overwrite the newer API key.
- **What we did:** Added a narrow `update_codex_provider_model_order` command and service mutation. It reloads the latest provider row and changes only matching `modelCatalog.models[*].sortIndex` fields; the UI no longer sends complete source providers for ordering. The generic router update remains separate because it owns the router's routing document. Neither current reference repository contains an equivalent model-order workspace mutation to port.
- **Evidence:** Database snapshots showed the failing key hash persisted through the 16:19 backup and a different live key after correction. OpenRouter `GET /api/v1/key` returned 200; a direct minimal completion for `~deepseek/deepseek-v4-flash-latest` returned 200 and resolved to `deepseek/deepseek-v4-flash-0731`; the live CCSwitch `/v1/responses` path also returned 200 with `OK`. The backend credential-preservation regression passed 1/1; focused model-order UI tests passed 3/3; Tauri command registration passed 1/1; `pnpm typecheck`, `cargo check --manifest-path src-tauri/Cargo.toml`, and `git diff --check` passed. `cargo fmt --check` still reports the pre-existing `transformed_request_bytes` wrapping drift in `proxy/forwarder.rs`. Full suites were not run.
- **What NOT to do again:** Never use a cached full-provider snapshot for a narrow derived-field mutation. Credential, endpoint, and protocol ownership must remain with the latest persisted provider row.

## 2026-09-16 - Keep terminal Sublyx quota failures out of provider health

- **What happened:** Codex still displayed `无可用 Provider` after Sublyx returned a real daily quota response, even though the proxy had already normalized the response to HTTP 402.
- **Root cause:** The inner Codex rejection policy correctly recognized Sublyx's `DAILY_LIMIT_EXCEEDED` envelope and stopped replaying it, but the outer failover classifier treated every HTTP 402 as a retryable provider failure. Concurrent quota failures therefore incremented the Sublyx circuit breaker until `CB-004` opened it; the next request had no permitted provider and emitted `NoAvailableProvider`.
- **What we did:** Keep recognized terminal quota failures eligible for failover to another provider while making them health-neutral. This preserves the real 402 `insufficient_quota` response and prevents a credential allowance from poisoning provider availability.
- **Evidence:** The live log recorded four normalized Sublyx quota failures at 16:52:57-16:53:00 followed by `CB-004 熔断器触发: 连续失败 4 次 → Open`. The new circuit-neutral regression passed 1/1; terminal quota tests passed 2/2; 403 normalization, 402 normalization, and transient-429 recovery each passed 1/1; `cargo check --manifest-path src-tauri/Cargo.toml` passed. A first test build hit the existing live `cc-switch.exe` replacement lock (OS error 5), then the already-built library tests completed successfully. Full suites and live rebuilt-app acceptance were not run.
- **What NOT to do again:** Do not classify a recognized terminal quota response as upstream health degradation, and do not infer that a quota error means the provider list is empty or unavailable.

## 2026-09-16 - Persist model-order save diagnostics and expose logs at the failure

- **What happened:** A Model ordering save failure was visible only as an in-page error, while `~/.cc-switch/logs/cc-switch.log` contained no corresponding save event. The current live database and generated Codex catalog no longer reproduced a route-resolution failure: all 64 published models resolved to enabled source-provider catalog entries.
- **Root cause:** `ModelOrderTab.saveOrder` caught both local preflight errors and `update_provider` IPC rejections without calling the existing credential-redacting frontend logger. The backend `update_provider` command also converted service errors to strings without recording the rejected app/provider boundary, leaving no durable evidence after the UI message disappeared.
- **What we did:** Report model-order failures through `reportFrontendError` with the failed stage, router id, source-provider ids, model count, and reset mode; add an Open Log Directory action beside the error; and log backend provider-update rejections without serializing provider configuration. BigStrongSun PR #26 is older model-order preservation work already represented locally; farion issue #5518 requests generic mapping sorting but has no implementation to port, and neither repository's recent commits/PRs contained a newer fix for this diagnostic gap.
- **Evidence:** The focused workspace and frontend logger tests passed 119/119; filtered Rust provider command tests passed 15/15; `pnpm typecheck`, `cargo check --manifest-path src-tauri/Cargo.toml`, scoped Prettier, and `git diff --check` passed. A repository-wide `cargo fmt --all -- --check` failed on the wrapping of `transformed_request_bytes` in the concurrently modified `src-tauri/src/proxy/forwarder.rs`; the separate quota/circuit-breaker changes in that file were not modified or included in this work.
- **What NOT to do again:** Do not treat a caught save exception as user-visible-only state. Persist redacted failure context at the catch boundary, keep backend mutation errors attributable to an app/provider id, and never dump full provider payloads or credentials into logs.

## 2026-09-16 - Reaudit MultiRouter schema v2, model ordering, Sub-Agents, and Codex versions

- **What happened:** Repeated model-order persistence failures exposed that routing, ordering, and Sub-Agent saves did not share a complete schema-v2 serialization contract. The installed npm Codex CLI was also one stable patch behind, raising concern that version drift caused the failures.
- **Root cause:** The routing bugs were owned locally: global order lacked an authoritative router field, dirty state used a derived catalog, and `serializeCodexRoutingV2` dropped extensions during unrelated saves. Stale aliases could also produce partial provider updates, and the implicit default model was selected before applying user sort order. The npm CLI version drift was separate: npm Codex was 0.153.4 while stable was 0.154.0; Desktop bundled app-server was independently versioned at 0.154.0-alpha.6.2.
- **What we did:** Added router-owned `modelOrder`, preserved all known schema-v2 fields during read/serialize, made model-order resolution atomic, sorted before choosing the implicit default, retained Sub-Agent/provider reconciliation, and added an opt-in startup Codex CLI stable updater that skips while any Codex process is running. Ported/adapted upstream model-order commit `0e6a690a0` and original npm probe commit `556bb2cab`; the schema-v2 protection (`cc52141a8`) and provider-driven Sub-Agent sync (`0dbdfd019`) were already present locally.
- **Reference verdict:** Current `BigStrongSun/ccswitchmulti` has newer traffic/history/official-auth work but no router-owned cross-provider `modelOrder`; its serializer still drops several extensions. Open PRs #95/#97 are unrelated response/image routing and were not ported. Current `farion1231/cc-switch` contributed the npm dist-tags fix. Its merged stale-account takeover fix `15884b203` addresses deleted/recreated managed OAuth accounts, not model routing or CLI version drift; it touches a separate large auth transaction and was not ported without evidence of that incident. DeepSeek/catalog capability fixes already have equivalent fork coverage.
- **Evidence:** `cargo check` and `pnpm typecheck` passed. MultiRouter Rust tests passed 79/79; Sub-Agent Rust tests 142/142; Sub-Agent V2 frontend tests 130/130; workspace/wizard/takeover frontend tests 157/157; automatic-update settings test 1/1; implicit-default and npm endpoint tests 1/1 each. Scoped Prettier and `git diff --check` passed. Live rebuilt Tauri acceptance was not performed.
- **What NOT to do again:** Do not blame npm CLI patch drift for application-owned persistence bugs, do not serialize schema-v2 documents by copying only the currently edited fields, and do not apply cross-provider ordering updates until every model resolves successfully.

## 2026-09-16 - Fix second-save detection and routing serializer order loss

- **What happened:** After saving a model order once, changing the order again could leave Save disabled or allow an unrelated routing save to erase the saved order.
- **Root cause:** Dirty-state comparison used the sorted catalog projection rather than the router-owned `modelOrder`, and `serializeCodexRoutingV2` rebuilt routing from only routes/subagent fields, dropping `modelOrder`, `modelDisplayStyle`, and `defaultReasoningEffort`.
- **What we did:** Compare the draft against the router-owned persisted order, use that order for reset and save-change detection, and preserve all schema-v2 routing extension fields when serializing route/subagent edits. Added second-save and serializer regressions.
- **Evidence:** Focused workspace tests passed 99/99 and takeover tests passed 2/2; `pnpm typecheck`, `cargo check`, Prettier, and `git diff --check` passed.
- **What NOT to do again:** Do not derive dirty state from a derived projection when an authoritative persisted field exists, and do not rebuild a schema-v2 routing document by copying only the fields currently being edited.

## 2026-09-16 - Restart Codex Desktop before applying takeover toggles

- **What happened:** Toggling Codex proxy takeover often failed while Codex Desktop or its app-server was still running because the live config change could not replace the active runtime.
- **Root cause:** The toggle wrote live configuration without owning the desktop process lifecycle, so an already-running client retained stale configuration and made the switch appear broken.
- **What we did:** Added read-only Codex Desktop process detection using the existing Desktop-only identity rules, plus a user-confirmed restart command that terminates only verified Desktop shell processes, applies the requested takeover state, and relaunches through the existing CDP/model-picker path. The toggle now offers this recovery before changing takeover; declining confirmation leaves Codex untouched. Lowercase `codex.exe` CLI/app-server processes are never matched.
- **Evidence:** Focused `ProxyToggle` tests passed 2/2 for confirmed restart and declined confirmation; `pnpm typecheck`, `cargo check`, scoped Prettier, and `git diff --check` passed. The restart path has not been exercised against a live Codex Desktop process in this session.
- **What NOT to do again:** Do not silently kill a user's running desktop application, and do not broaden Desktop process matching to the lowercase CLI/app-server process.

## 2026-09-16 - Persist MultiRouter model order on the router

- **What happened:** A saved cross-provider model order disappeared after leaving and reopening the Model ordering tab, even though source-provider rows had received `sortIndex` values.
- **Root cause:** Schema-v2 routers remove their derived `modelCatalog`, and `buildModelCatalogForRoutes` intentionally ignores source-provider custom order when constructing the aggregate catalog. The global order therefore had no router-owned persisted field. The schema-v2 frontend normalizer also omitted newly added routing extensions.
- **What we did:** Added `codexRouting.modelOrder` as the authoritative global picker order. Save order now persists it on the router, the frontend aggregate restores it on remount, `readCodexRouting` preserves it, and the Rust compiler applies it to the published model catalog with dense `sortIndex` values.
- **Evidence:** `pnpm typecheck` and `cargo check --manifest-path src-tauri/Cargo.toml` passed. Frontend remount/ranking regressions passed 2/2. Rust `router_model_order_controls_the_compiled_catalog` passed 1/1. Scoped Prettier and `git diff --check` passed.
- **What NOT to do again:** Do not store a cross-provider aggregate preference only inside independently owned source-provider rows; persist it at the router that owns the aggregate projection.

## 2026-09-16 - Preserve model order and prioritize the first provider

- **What happened:** The model-order tab kept replacing the current catalog with a recommendation-derived order whenever its catalog props refreshed, and the recommendation ranking ignored the provider ordering the user sees elsewhere.
- **Root cause:** Recommendation generation was incorrectly used as initialization logic instead of an explicit user action; provider identity was treated as a scoring detail rather than a workflow priority.
- **What we did:** The tab now initializes and refreshes from the persisted `sortIndex` order without recomputing recommendations. Recommendations run only from the explicit button and reserve the first two ranked slots for models from the first enabled provider in the route/provider ordering, then fill remaining slots from other providers.
- **Evidence:** `pnpm typecheck` passed; focused recommendation/search regressions passed 3/3; `git diff --check` passed. Full workspace suite is being run after this final adjustment.
- **What NOT to do again:** Never silently reorder a user’s draft because a catalog prop changed, and never call an inferred popularity score more authoritative than the provider ordering the user explicitly chose.

## 2026-09-15 - Keep filtered model search draggable and rank eight models

- **What happened:** Searching the new model-order workspace disabled every drag handle, so a filtered result could not be moved into the ranked column; the ranked column also remained limited to five.
- **Root cause:** The redesign retained the old list's `dragDisabled={Boolean(modelSearch)}` guard. Its reorder function was also based on the filtered array, so simply enabling drag would have dropped or misordered models hidden by the search.
- **What we did:** Kept search results draggable, changed drag completion to move entries inside the complete ordered catalog, built the sortable context from the always-visible ranked entries plus filtered available entries, and expanded the ranked/recommended limit from five to eight. Updated all four locale descriptions and the search regression.
- **Evidence:** `pnpm typecheck` passed; focused recommendation and searched-ranking tests passed 2/2 before the final formatting-only change. The complete workspace test file and formatting checks were rerun afterward.
- **What NOT to do again:** Do not disable the primary interaction merely because a collection is filtered, and never apply a filtered reorder by replacing the full backing collection.

## 2026-09-15 - Unify MultiRouter model ranking into one drag workspace

- **What happened:** The model-order page rendered a separate read-only Top Models strip above another full sortable model list, forcing users to reconcile two representations of the same order.
- **Root cause:** The top-five summary and the editing surface evolved independently even though both represented one persisted `sortIndex` sequence.
- **What we did:** Replaced the duplicate strip/list layout with one cross-column drag workspace: ranked top five on the left and remaining available models on the right. Added a deterministic recommendation action and default draft ordering based on provider catalog order, coding fit, declared capabilities, context, recency, and provider diversity. The UI explicitly avoids claiming popularity when provider APIs expose availability but not usage.
- **Reference verdict:** Current `BigStrongSun/ccswitchmulti` and `farion1231/cc-switch` contain no equivalent two-column ranking workspace or provider-popularity implementation. OpenRouter's public models API exposes model metadata and creation order, while its rankings page is a separate product surface; standard OpenAI-compatible `/models` responses do not provide a portable popularity field. The local recommendation layer therefore consumes only owned catalog metadata and labels its evidence honestly.
- **Evidence:** `pnpm typecheck` passed. `CodexRouterWorkspacePage.test.ts` passed 95/95, including recommendation ordering and exact-rank persistence. Scoped Prettier and `git diff --check` passed.
- **What NOT to do again:** Do not render a second read-only summary for an order that is already directly editable, and do not market model availability or API response order as real usage popularity.

## 2026-09-15 - Defer Codex MultiRouter publication and compact third-party catalog payloads

- **What happened:** Saving a Codex route-target provider synchronously rewrote the full MultiRouter catalog, cache, config, and managed Agent files before the save promise resolved.
- **Root cause:** Provider mutation owned both SQLite persistence and derived live projection publication; enrichment also cloned official instruction payloads into matching third-party model entries.
- **What we did:** Added a persistence-only mutation path, returned after database/profile reconciliation, and queued active-router projection publication on Tauri's blocking worker. Third-party enrichment now replaces inherited `base_instructions` with a compact parser-safe value and removes inherited `model_messages`, while official routes retain official metadata. Added focused regressions.
- **Evidence:** `cargo check --manifest-path src-tauri/Cargo.toml` passed; the existing MultiRouter mutation suite passed 17/17. `pnpm typecheck` passed. The new catalog test could not execute because the normal `src-tauri\target\debug\cc-switch.exe` was locked (OS error 5); no process was terminated. Full suites and live runtime verification were not run.
- **What NOT to do again:** Do not add timeouts or truncate request history to mask projection latency; keep SQLite commit and derived publication as separate ownership boundaries, and do not copy full official harness prompts into third-party catalog entries.

## 2026-09-12 - Bound Codex reasoning resolution after large automatic catalog refreshes

- **What happened:** Opening a saved Codex provider backed by OpenRouter could make the entire application unresponsive when automatic model refresh returned roughly 500 models, despite the catalog editors already paging their visible rows.
- **Root cause:** The catalog viewport bounded mounted controls to ten rows, but `CodexFormFields` independently reacted to every catalog change by launching one `resolve_codex_model_reasoning_capability` Tauri IPC request per model inside a single `Promise.all`. A 500-model automatic refresh therefore produced 500 concurrent IPC calls, each carrying the full catalog settings payload; pagination did not own or bound this hidden fan-out.
- **What we did:** Made `CodexCatalogViewport` report its currently mounted page after render. Reasoning resolution now processes only those visible models, sequentially, and stops stale page work after cancellation. The complete catalog remains persisted and searchable; changing pages resolves that page on demand through the existing cache. Added an automatic empty-to-500 OpenRouter refresh regression requiring exactly ten initial reasoning calls.
- **Reference verdict:** Fetched current `BigStrongSun/ccswitchmulti` and `farion1231/cc-switch` heads and reviewed recent provider-form history plus relevant open/closed model-catalog and dropdown pull requests. Both references retain whole-catalog reasoning or dropdown work and contain no fix for this fork's per-model Tauri reasoning-resolution fan-out, so the fix was implemented at the local viewport/resolver ownership boundary.
- **Evidence:** The new 500-model automatic-refresh regression passed 1/1 and completed its test body in 266 ms; before the fix the same boundary attempted all 500 resolutions. `CodexCatalogViewport` passed 2/2, and adjacent auto-refresh/catalog suites passed 10/10. `pnpm typecheck` passed. The complete `CodexFormFields` file passed 62/63; the sole model-specific key-group test timed out at five seconds, then passed 1/1 when isolated with its unchanged 27.8-second runtime. Scoped Prettier passed for the viewport and test file but still reports the previously recorded whole-file formatting drift in `CodexFormFields.tsx`. Live Tauri acceptance was not performed after this source edit.
- **What NOT to do again:** Do not assume bounded DOM rows also bound effects triggered from the complete backing collection. Never fan out one IPC request per provider model concurrently, especially when every request serializes the full catalog; derive background work from the mounted page and process it with an explicit concurrency bound.

## 2026-09-12 - Refresh saved provider catalogs without blocking edit forms

- **What happened:** Saved provider edit forms could open with stale model inventories until the user manually refreshed them, while Codex catalogs could retain explicitly remote-bound models that the provider no longer advertised.
- **Root cause:** Model discovery was owned only by manual form actions. There was no shared stale-while-revalidate boundary for saved providers, no credential-safe request deduplication, and no conservative automatic reconciliation policy that distinguished remote-bound Codex rows from manual rows or incomplete grouped-credential results.
- **What we did:** Added a silent form-open refresh hook with a 15-minute in-memory cache, credential fingerprints instead of raw-key cache keys, concurrent request deduplication, and manual-refresh invalidation so manual results win. Enabled it only for saved Claude, Gemini, OpenCode, OpenClaw, Hermes, Copilot OAuth, Codex OAuth, and xAI OAuth providers. Codex now adds and updates fetched models, removes missing explicitly remote-bound rows only after a complete non-empty fetch, preserves manual rows and failed/empty/partial state, requires every grouped credential request to succeed before removal, and retains existing recent-version pruning.
- **Reference verdict:** Recent BigStrongSun/ccswitchmulti and farion1231/cc-switch history contained no equivalent universal saved-form refresh implementation. GitHub pull-request search could not be completed because `gh` was unauthenticated, so the local owning boundaries were implemented directly.
- **Evidence:** `pnpm typecheck`, `cargo check --manifest-path src-tauri/Cargo.toml`, and `git diff --check` passed. Focused coverage passed: auto-refresh hook 4/4, Codex catalog sync 4/4, automatic Codex reconciliation/removal/pruning 1/1, grouped-credential failure preservation 1/1, and Claude/OpenCode/ProviderForm consumers 31/31. Full `cargo test --manifest-path src-tauri/Cargo.toml` failed before execution because the running `src-tauri/target/debug/cc-switch.exe` was locked with OS error 5. The observed full frontend run passed the new hook 4/4 and catalog sync 4/4 but did not reach a final summary; it reproduced two `StreamRetryToggle` timeouts and one `LanguageSwitcher` timeout, then remained live without output until the agent stopped its own test process. Isolated reruns also failed `StreamRetryToggle` 2/2 and `LanguageSwitcher` 1/1 with 5-second timeouts. Those components are outside this feature's changed files.
- **What NOT to do again:** Do not block form rendering on model discovery, store raw credentials in cache identities, let stale automatic results overwrite a manual refresh, or delete Codex catalog rows after an empty, failed, partial, or manually authored result. Do not report the full suites as passing while the executable lock, deterministic unrelated timeouts, and frontend runner non-exit remain unresolved.

## 2026-09-12 - Correction: manual summary is distinct from compaction recommendation

- **What happened:** A follow-up audit showed the earlier threshold-based recommendation change addressed compaction labeling, not the user's requested manual summary handoff.
- **Root cause:** The actual failure was the manual summary `turn/start` re-entering the proxy while its source-session handoff guard was active, causing the observed 409. The recommendation field was a separate diagnostic/UI contract.
- **What we did:** Reverted the unrelated threshold-to-compaction recommendation change. Retained the manual-summary preflight bypass and thread-identity validation, which are the fixes that own the 409 and wrong-session risks.
- **Evidence:** Archived session `01a0948e-4b53-7921-be95-845a7157bb19` records the exact 409 during the manual summary turn; focused manual handoff tests passed 7/7, and the manual-summary bypass test passed 1/1 before this correction.
- **What NOT to do again:** Do not describe compaction recommendation or native compaction behavior as proof that manual summary plus fresh root session works.

## 2026-09-12 - Recommend summarize for oversized text history

- **What happened:** The current session repeatedly exceeded the configured byte threshold but never exposed the summarize action because its estimated token count stayed below the token ceiling.
- **Root cause:** Request Health only marked `compaction_recommended` for token-limit overflow or sustained-growth anomalies, even though the owning signal for this workflow is oversized non-media history.
- **What we did:** Include `threshold_exceeded` in the summarize recommendation and `compaction_recommended` decision for Codex text-history requests, while preserving `continue_once` for media-dominated payloads and excluding explicit compaction requests.
- **Evidence:** `oversized_text_history_recommends_summarize_before_token_ceiling` passed 1/1; handoff JS tests passed 7/7; `cargo check --manifest-path src-tauri/Cargo.toml` passed.
- **What NOT to do again:** Do not require token-limit overflow before offering summarize for a request already proven oversized by the configured byte threshold.

## 2026-09-12 - Allow the internal summary turn through Request Health

- **What happened:** Live logs showed oversized Codex requests continuing normally, while earlier summary handoffs could fail with `Manual summary turn failed` or leave retries blocked with 409.
- **Root cause:** The manual summary turn reused the oversized source context and re-entered the same Request Health handoff guard, so the recovery turn could recursively block itself.
- **What we did:** Detect the two stable manual-summary prompt sentences at the Request Health boundary and exempt only that internal summary turn from preflight review. Ordinary retries for the source session remain blocked while the handoff guard is active.
- **Evidence:** `manual_summary_turn_bypasses_active_handoff_guard` passed 1/1; `active_summary_handoff_rejects_a_codex_retry_before_risk_recalculation` passed 1/1; session extraction passed 23/23; `tests/lib/codexSummaryHandoff.test.ts` passed 7/7; `cargo check --manifest-path src-tauri/Cargo.toml` passed. Live runtime was not retested because the running executable predates this source change.
- **What NOT to do again:** Do not send the recovery summary through the same oversized-request decision loop without an explicit internal marker and regression test.

## 2026-09-12 - Prevent summarize handoff from targeting request IDs

- **What happened:** A Sublyx `/responses` request returned 409 while summary handoff was active, and the handoff could be started from the wrong Codex identity.
- **Root cause:** Codex session extraction accepted `x-client-request-id` and arbitrary long `session-id` values as stable thread identities. The handoff then passed that value to Codex Desktop as the source thread ID.
- **What we did:** Restrict Codex client-provided identities to `thread-id`, valid UUID/ULID-compatible session headers, `x-codex-window-id`, and validated `metadata.session_id`; explicitly ignore `x-client-request-id` for handoff identity. Added regressions for request-ID and invalid account-session headers.
- **Evidence:** `proxy::session::tests::` passed 23/23; both summary-handoff guard tests passed 1/1; `tests/lib/codexSummaryHandoff.test.ts` passed 7/7; `cargo check --manifest-path src-tauri/Cargo.toml` passed.
- **What NOT to do again:** Do not treat per-request correlation IDs or unvalidated account/session labels as Codex thread IDs. Live acceptance still requires restarting the normal development app and exercising the current session.

## 2026-09-12 - Formatting update broke brittle summarize-handoff assertion

- **What happened:** The latest formatting-only commit changed the indentation of the generated Codex compatibility template and caused the Rust summarize-and-restart ordering test to fail.
- **Root cause:** The test searched for an exact multiline whitespace shape instead of the semantic manual-summary marker; the handoff implementation itself was unchanged.
- **What we did:** Updated the assertion to locate the stable summary-request text while retaining ordering checks against interruption, fresh-root creation, and handoff turn startup.
- **Evidence:** `pnpm exec vitest run tests/lib/codexSummaryHandoff.test.ts --maxWorkers=2 --minWorkers=1` passed 7/7; the focused Rust test passed 1/1; `cargo check --manifest-path src-tauri/Cargo.toml` passed; `git diff --check` passed.
- **What NOT to do again:** Do not use formatter-sensitive multiline snapshots for generated-script protocol ordering; assert stable semantic markers and behavior.

## 2026-09-12 - Bound upgrade preflight and Codex renderer discovery

- **What happened:** The two reference repositories contained two applicable hang/performance fixes beyond the provider-form loader correction: original commit `fd14f9c4f` bounded CLI upgrade conflict probes and showed their pending state, while BigStrongSun commit `c53cda76a` removed unconditional periodic React graph scans from the Codex renderer compatibility heartbeat.
- **Root cause:** Upgrade preflight called `resolve_path_default(tool, None)` and untimed `--version` children, so an interactive shell or command shim could keep its loader pending forever. Separately, this fork's extracted `codex_app_compat_template.js` searched every DOM element for the conversation runtime before module discovery on every 1.5-second heartbeat.
- **What we did:** Cherry-picked `fd14f9c4f` exactly as `eb14ef332`. Adapted `c53cda76a` to the fork's extracted JavaScript template as `ae4f7c164`: cached-module discovery now runs first, React fallback discovery is serialized, idle-sliced, and backed off for 30 seconds, manual summary actions can force a bounded fallback when necessary, and scheduler versioning retires the previous interval/observer. Rejected BigStrongSun `4aa2b9e18` because its 13-file runtime-refresh subsystem does not exist locally and is not a loader-only transfer; rejected original `076c2744c`/`7e152d758` because local model dropdowns already supersede them with deferred bounded rendering; `8155c4357` targets a Pi test file absent from this fork.
- **Evidence:** Immediately after the cherry-pick, `cargo check --manifest-path src-tauri/Cargo.toml` and `pnpm typecheck` passed; `commands::misc::tests` passed 82/82. After the renderer adaptation, `cargo check` and `pnpm typecheck` passed; `codex_desktop::tests` passed 46/46, including generated JavaScript syntax, current conversation-runtime discovery, manual summary handoff, and the new scheduler regression. `node --check` and scoped Prettier passed for the template; `git diff --check` passed. Full-suite results are recorded below when complete.
- **What NOT to do again:** Do not run upgrade discovery children without deadlines, start global work without a visible pending owner, or perform a full DOM/React fallback scan before a cached module lookup on every heartbeat. Do not cherry-pick upstream architecture wholesale when this fork moved the owning implementation to another boundary.

## 2026-09-12 - Close deferred global loading tokens when content becomes ready

- **What happened:** Opening the Sublyx Codex provider editor revealed the form but left the global loading banner visible permanently. The same leak affected every `DeferredContent` and `DeferredRender` consumer, including add/edit provider forms and deferred large model lists/editors. The audit also found that the clean starting tree did not typecheck around official reasoning metadata.
- **Root cause:** Both deferred primitives called `beginLoading()` before the paint boundary but released its token only from effect cleanup. Reaching the ready state does not unmount the primitive, so the manual task remained in `GlobalLoadingProvider` until the entire deferred component unmounted. Separately, Rust emits reasoning sources such as `official`, `provider_config`, `detection`, and `library`, but the shared TypeScript contract excluded them; the official-catalog test also froze its effort list as readonly even though the serialized contract is mutable.
- **What we did:** Release the idempotent loading token in the timer callback immediately after marking deferred content ready, while retaining cleanup release for cancellation/unmount. Added regressions for both primitives under the real global loading provider. Aligned the TypeScript reasoning-source union with backend values and typed the official OAuth fixture against that contract. Audited 147 frontend loading references, 61 `useQuery` sites, all 13 manual loading initiators, and every deferred primitive consumer. BigStrongSun `e6605f318` and original `d695a2d77` contain no equivalent deferred-loader fix; relevant recent commits and open/closed GitHub search results did not identify a competing implementation.
- **Evidence:** The first `pnpm typecheck` failed at `CodexRouterWorkspacePage.test.ts:4543` on the readonly effort fixture; after fixing the owning contract and fixture, `pnpm typecheck` passed. The focused official-reasoning catalog regression passed 1/1. `DeferredContent` plus `GlobalLoadingProvider` passed 7/7; `EditProviderDialog` passed 6/6; `ProviderForm.codexPreset` passed 14/14; `OpenCodeFormFields` passed 12/12; `SkillsPageInstall` passed 7/7; `LargeModelEditors` passed 2/2; `SettingsDialog` plus `WorkspaceFileEditor` passed 12/12; and `PagedModelList` passed 1/1. The combined loader-consumer run was interrupted after it stopped producing output. A separate `ModelDropdown` run failed 2/3 because both interactive dropdown cases timed out; the literal two-row case completed only when rerun alone but took 19.4 seconds, while the 430-model case still timed out with a 30-second limit after 100.9 seconds of recorded test work. Full frontend/backend suites and live Tauri runtime were not tested.
- **What NOT to do again:** Do not treat effect cleanup as task completion when a component remains mounted after its loading phase, and do not claim broad loader verification is green while a consumer test still times out.

## 2026-09-10 - Make the Codex Desktop picker follow live router metadata

- **What happened:** Model order already re-injected on catalog fingerprint changes, but Codex Desktop could temporarily lose provider labels, nested MultiRouter reasoning metadata never reached the picker aliases, official OAuth refreshes dropped reasoning metadata, and takeover hardcoded fresh-thread effort to `medium` with no Router UI.
- **Root cause:** Provider identity had no stable fallback/dual alias at the projection boundary; the Desktop adapter only read top-level reasoning fields while the router SSOT stores `reasoning.defaultEffort` and `reasoning.supportedEfforts`; `FetchedModel` discarded the official OAuth capability; and `model_reasoning_effort` was not owned by a persisted routing preference.
- **What we did:** Fell back empty provider names to provider id and emit both provider aliases, promoted nested reasoning into Desktop descriptors, carried authoritative OAuth reasoning through Rust/Tauri/TypeScript catalog refresh, and added a localized Model Order control persisted as `codexRouting.defaultReasoningEffort` and projected into fresh-thread TOML. This adapts BigStrongSun commit `3dbc85462`; farion upstream has catalog discovery but not this fork's Desktop live-injection lifecycle.
- **Evidence:** `cargo check` and `pnpm typecheck` passed; targeted Rust coverage passed 22/22; Router Workspace passed 93/93; full Rust library passed 3,737 tests with 6 ignored; full frontend passed 1,461 tests with one preset-form timeout, and the complete failed file then passed 14/14. Full `cargo test` could not replace the running `src-tauri/target/debug/cc-switch.exe` (OS error 5). After the user restarted `pnpm dev`, the rebuilt process logged a successful 61-model injection; a read-only CDP inspection of the live renderer confirmed Astra and Sol both carried `providerName`/`provider_name = Sublyx`, `defaultReasoningEffort = low`, and the full supported effort descriptors.
- **What NOT to do again:** Do not hardcode a global MultiRouter effort or reconstruct official reasoning from model names; preserve provider identity and capability metadata at the catalog producer, then let the guardian re-inject the finalized immutable projection.

## 2026-09-10 - Close preset and catalog regression audit

- **What happened:** The final audit found eight frontend failures in Codex preset, catalog synchronization, and grouped API-key UI coverage; no cherry-pick or merge was performed.
- **Root cause:** A vendor compatibility port changed DeepSeek's documented top-level reasoning parameter and intentionally moved Zhipu to the official GLM-5.3 Responses catalog, while local tests still described the retired GLM-5.2 Chat preset. Separately, empty grouped credentials exposed controls prematurely, and catalog sync attempted to hydrate image metadata across an explicit image exclusion.
- **What we did:** Restored DeepSeek's `reasoning_effort`, aligned preset tests with Zhipu GLM-5.3 `/api/v1` Responses behavior, hid rotation/key-add controls until a group has a non-empty key, and preserved explicit input-capability exclusions during catalog reconciliation.
- **Evidence:** Full `pnpm test:unit` passed 181 files / 1460 tests; targeted preset/catalog/provider tests passed 90/90; `pnpm typecheck`, `cargo check --manifest-path src-tauri/Cargo.toml`, and `cargo test --manifest-path src-tauri/Cargo.toml --lib` with `LOCALAPPDATA=$env:TEMP` passed 3733/3733 library tests (6 ignored). Full backend `cargo test` was attempted but could not replace the running `src-tauri/target/debug/cc-switch.exe` and returned OS error 5 before integration execution.
- **What NOT to do again:** Do not restore retired vendor behavior just to satisfy stale tests, and do not bypass explicit user capability exclusions while filling missing catalog metadata.

## 2026-09-09 - Complete request-health media accounting and notification opt-out contracts

- **What happened:** Finished the in-progress request-health media handling across Rust diagnostics, TypeScript contracts, the settings panel, and all four locales.
- **Root cause:** Image detection had been expanded in the backend without completing the serialized frontend contract; the new Windows notification opt-out and media recommendation fields were therefore not represented in the UI or fixtures.
- **What we did:** Added `windowsNotificationsEnabled`, media byte/item/dominance fields, dominant category, and recommended action to shared types and settings UI; added localized labels/action text; removed the unused recursive image detector; added a regression proving nested image payload bytes are counted and image-dominated oversized requests do not arm the history preflight review.
- **Evidence:** `cargo check --manifest-path src-tauri/Cargo.toml` passed; `cargo fmt --manifest-path src-tauri/Cargo.toml --all -- --check` passed; `git diff --check` passed; request-health Rust tests passed 14/14; focused frontend tests passed 96/96; `pnpm typecheck` passed. Full `pnpm test:unit` passed 177 files / 1452 tests and failed 4 files / 8 tests in pre-existing dirty preset/catalog/key-group areas. Full `cargo test --manifest-path src-tauri/Cargo.toml` could not replace the running `src-tauri/target/debug/cc-switch.exe` and failed with OS error 5 before suite execution.
- **What NOT to do again:** Do not add serialized backend fields without updating both shared frontend types and all request-health fixtures/locales; do not bypass the normal executable lock with an alternate target or by killing the running process.

## 2026-09-09 - Reference commit and PR reaudit; updater and streaming fixes

- **What happened:** Reaudited BigStrongSun/ccswitchmulti at `dbf365608` and farion1231/cc-switch at `f3b18df12`, including recent commits and open/closed PRs. Integrated two focused original-repository fixes in the current dirty checkout without a merge or cherry-pick.
- **Root cause:** Update checks discarded string/object rejection details; empty `reasoning_content` placeholders opened thinking blocks and split otherwise continuous text.
- **What we did:** Adapted original PR #6482 (`bd1265d2a`) using the existing `extractErrorMessage` helper in updater state and the settings toast. Adapted original PR #7227 by ignoring empty reasoning before block transitions, with regression coverage for empty placeholders and real reasoning followed by text.
- **Reference verdict:** BigStrongSun plugin-registry repair (#69/#61) and much routing/auth work are already represented locally. Catalog retention/sorting (#24/#26) overlaps dirty local policy; usage redesign (#19), unverified-provider creation (`aab43413f`), and terminal Responses failure envelopes (`e28f6c7a7`) require separate contract review. Original #7212 (universal child metadata retention) and #7175 (retry-safe migration) remain promising but unintegrated candidates requiring focused tests. Deferred broad image proxy (#7177), WebView2 (#7240), and cross-cutting retry/usage changes; original multiple-key #7188 must not replace the fork's grouped-key ownership model.
- **Evidence:** `pnpm typecheck` passed; targeted updater tests passed 4/4, adjacent settings/provider tests 9/9, and streaming tests 13/13. Scoped Prettier/rustfmt and `git diff --check` passed. Full `pnpm test:unit`: 1452 passed, 8 failed across 4 files (CodexFormFields, ProviderForm.codexPreset, codexProviderPresets.reasoning, codexCatalogSync), matching previously journaled failure areas. Full `cargo test --manifest-path src-tauri/Cargo.toml` failed before suite completion because replacing `src-tauri/target/debug/cc-switch.exe` returned access denied (OS error 5). Library fallback `cargo test --manifest-path src-tauri/Cargo.toml --lib`: 3732 passed, 0 failed, 6 ignored. Full integration/executable coverage and rebuilt live runtime remain unverified. Full-suite attempt logs: `audit-20260909-frontend.log` and `audit-20260909-backend.log`.
- **What NOT to do again:** Do not treat empty reasoning placeholders as semantic block transitions, discard non-Error updater diagnostics, claim library coverage proves full integration acceptance, or bypass the running executable lock with an alternate build target.

## 2026-09-08 - Reaudit references; exclude Claude workflow journals

- **What happened:** A fresh audit of `upstream/main` and `original/main` found most relevant fixes already present locally, including Codex auth alignment, UTF-8-safe URL masking, and catalog modality handling. The original repository also had a clean Claude session-scanner fix for workflow journals.
- **Root cause:** Claude workflow runs store `subagents/workflows/wf_*/journal.jsonl` beside session data; the scanner excluded `agent-*` files but treated these journals as ghost sessions.
- **What we did:** Applied the upstream `2510a4e2a` change to exclude `journal.jsonl` by filename and retain its regression test. No branch, worktree, or cherry-pick was created.
- **Reference verdict:** Upstream BigStrongSun `5eaf58e04` Codex OAuth identity upgrade work is substantially represented by current local auth-state alignment; the larger `aab43413f` unverified-provider flow needs a separate product decision. Original `e724270db` image-edit proxy work is broader than a safe patch-only transfer. Provider/preset and release commits were not applicable to this consolidation batch.
- **Evidence:** `cargo test --manifest-path src-tauri/Cargo.toml --lib session_manager::providers::claude::tests::` passed 13/13. `git diff --check` passed. Full suites were not rerun for this small backend change.
- **What NOT to do again:** Do not import broad upstream commits wholesale when the fork already owns equivalent behavior or the commit crosses unrelated architecture boundaries.

## 2026-09-08 - Return consolidation work to main; require permission for Git isolation

- **What happened:** The user rejected the unrequested consolidation branch/worktree and explicitly required one local branch, with all work preserved on `main`. This corrects the worktree location recorded in the consolidation entry below.
- **Root cause:** The agent treated the prior concurrency guidance as permission to create Git isolation without asking. The user requires explicit permission for each new branch, worktree, clone, or alternate checkout.
- **What we did:** Updated AGENTS.md to work in the current checkout by default and ask for a normal development rebuild when blocked. Transferred all consolidation changes into `main`, retaining the existing routed-credential test and both sets of journal entries. Removed only the extra worktree's junctions, preserving their shared dependency/build targets, then removed the extra worktree and deleted `codex/consolidate-shared-logic`. All source changes remain uncommitted.
- **Evidence:** All 42 non-overlapping transferred files matched their source after newline normalization. All 14 non-overlapping pre-existing dirty files retained their SHA-256 hashes; the original journal content was also hash-verified after removing the added entry. Reverse patch checks verified both sets of Codex changes remained present. On `main`, `pnpm typecheck`, `cargo check --manifest-path src-tauri/Cargo.toml`, and `git diff --check` passed; six targeted frontend files passed 128 tests and the Codex provider module passed 132 tests. `git branch --list` shows only `main`; `git worktree list` shows only `H:/repos/ccswitchmulti-fork`; the extra worktree directory no longer exists. Full suites and live runtime were not rerun for this transfer; earlier full-suite failures and the executable lock remain recorded below.
- **What NOT to do again:** Do not infer permission to create branches or worktrees from implementation, audit, concurrent-work, or test requests. Ask the user to rebuild when required; do not bypass locks through Git isolation or alternate build targets.

## 2026-09-08 - Consolidate duplicated helpers and conflicting route discovery

- **What happened:** The repetition audit found three independent credential writers, duplicated sync scheduling and frontend helpers, inconsistent external API route discovery, and local release scripts conflicting with the Actions-only release policy.
- **Root cause:** Copies of the same policy could drift: Windows credential writers deleted the destination before rename, while external API discovery did not recognize the array-form routing schema accepted by runtime. S3 and WebDAV require independent suppression state even when their scheduling algorithm is shared.
- **What we did:** In the isolated `codex/consolidate-shared-logic` worktree, reuse the existing atomic replacement/recovery implementation with private Unix temporary-file permissions for all three auth stores; share route-schema and target-ID parsing while preserving legacy matching priority; share sync scheduling with per-transport state; consolidate theme subscriptions, common-snippet migration, models.dev catalog filtering, usage relative time, and MCP JSON IO. Replace local release/export/hook entry points with explicit retirement errors. The active dirty checkout was not edited.
- **Reference verdict:** Inspected BigStrongSun/ccswitchmulti at `26abe211` and farion1231/cc-switch at `f3b18df1`, recent commits, open/closed PRs, and nearby auth/sync/theme history. Both references still contained Copilot delete-before-rename persistence; no safe writer was available to port. Their sync history supported retaining the existing shared upload mutex and table filter. External API discovery is fork-specific. Adapted local owning helpers; no merge or cherry-pick.
- **Evidence:** Final `cargo check --manifest-path src-tauri/Cargo.toml`, `pnpm typecheck`, scoped rustfmt/Prettier, and `git diff --check` passed. `cargo test --manifest-path src-tauri/Cargo.toml --lib` passed 3724 tests, with 6 ignored. Release-script Pester tests passed 16/16. Full `pnpm test:unit` passed 1424 tests and failed 8 across four files: `codexProviderPresets.reasoning`, `CodexFormFields`, `ProviderForm.codexPreset`, and `codexCatalogSync`, matching the failures recorded in the September 6 journal. An earlier `--lib config::tests::` substring-filtered run passed 372 and failed `codex_model_catalog_projects_declared_glm_reasoning_capability` on malformed cache JSON; the failure did not recur in the full library run and its cause is unconfirmed. Full `cargo test` was blocked before test execution by OS error 5 removing `src-tauri/target/debug/cc-switch.exe`. Integration/executable tests, Unix permission behavior, and rebuilt live runtime remain unverified. Full-suite logs are retained locally as `consolidation-frontend-tests.log`, `consolidation-backend-tests.log`, and `consolidation-backend-lib-tests.log`.
- **What NOT to do again:** Do not restore delete-before-rename credential persistence, infer route schemas separately from runtime, merge transport suppression counters, or delete legacy snippets before persistence succeeds. Do not revive local release production or bypass the running executable lock with another target directory. A passing library suite does not establish integration or live-runtime acceptance.

## 2026-09-08 - Retain recent model releases with a four-choice family minimum

- **What happened:** The live catalog audit exposed latest-only pruning that removed GPT-5.6 when GPT-6 existed and compared specialized models against unrelated newer branches. The user requires previous releases and at least four distinct choices per family when available.
- **Root cause:** A single maximum numeric version per broad family conflated release retention, model specialties, offering aliases, and dated revisions. Tests explicitly encoded the unwanted latest-only behavior.
- **What we did:** Keep two distinct release versions per branch, then backfill complete release tiers to four distinct choices per family. Free/batch/contributor offerings, equivalent versions, mapped aliases, and dated snapshots do not fill the minimum. Compare snapshots only within the same model identity and date precision; keep undated aliases and unknown names. Preserve suffix specialties, provider namespaces, and credential-group ownership. Treat the irregular Grok 4.20 series independently so it cannot displace Grok 4.5. Updated all four locales and added a repeatable live audit with per-row reasons and exact source/raw-response evidence in `scripts/audit-catalog-pruning.cjs`.
- **Reference verdict:** Reviewed recent commits, open/closed catalog PR search results, nearby form history, and current `CodexFormFields.tsx` in BigStrongSun/ccswitchmulti and farion1231/cc-switch. Neither inspected form implements this retention policy; nearby work concerns protocol selection, aliases and reasoning metadata. Implemented locally without porting unrelated changes. xAI documents the irregular [Grok 4.20](https://docs.x.ai/developers/models/grok-4.20) series separately.
- **Evidence:** `pnpm typecheck` passed. Targeted catalog/form tests: 94 passed, 2 failed across 5 files; all 22 pruning regressions and the new saved-catalog button regression passed. Remaining failures match previously journaled Rotation visibility and refill-modality expectations; React act warnings also occurred. `node scripts/audit-catalog-pruning.cjs` passed family minimum, order independence, idempotence, partition, unknown retention, and original-audit examples on all three live endpoints: OpenRouter 428 total / 369 kept / 59 removed / 166 unclassified; Zen 70 / 58 / 12 / 1; Go 35 / 33 / 2 / 1. Full frontend/backend suites and live Tauri runtime were not run for this frontend coding increment.
- **What NOT to do again:** Do not count batch/free duplicates as independent choices, let four newest-version offerings crowd out the previous release, infer cross-specialty replacement, or label retained unknown models as verified latest. Fewer than four endpoint choices is a catalog limitation, not permission to invent models.

## 2026-09-08 - Preserve isolated key-group ownership through MultiRouter

- **What happened:** Reaudit showed direct isolated provider routing carried `apiKeyGroupId`, but the MultiRouter workspace rebuilt source catalog rows through a narrower draft type that omitted the group identity.
- **Root cause:** `catalogDraftFromSourceModel` copied model, upstream, capability, and display metadata but not `apiKeyGroupId` / `apiKeyGroupGenerated`. Routed provider materialization then used the router catalog, so the proxy could not resolve the isolated credential and would fall back to the provider's primary key.
- **What we did:** Preserve group identity and generated-row ownership in the MultiRouter catalog, then verify routed materialization selects the intended group key before the visible alias is translated to the common upstream model.
- **Evidence:** `pnpm typecheck` passed. The focused MultiRouter projection test passed 1/1; API-key catalog/load tests passed 10/10; direct grouped-key backend tests passed 3/3; and the routed isolated-key backend regression passed 1/1. A broader CodexFormFields run had 68 passes and one failure caused by a concurrent UI change exposing the fixed-group Rotation dropdown against an older expectation.
- **What NOT to do again:** Do not add routing identity metadata to the provider catalog without adding it to every MultiRouter catalog draft/copy boundary and a routed-auth regression.

## 2026-09-08 - Reaudit terminal quota delivery and reference repositories

- **What happened:** Rechecking the Sublyx quota fix showed three upstream status variants: 429 with a structured daily-limit reason, 403 with plain `daily usage limit exceeded`, and 402 with the original gRPC-style envelope. Earlier 402 logs had been incorrectly interpreted as proof of CCSwitch normalization.
- **Root cause:** The first correction inspected only 429, then 403; it did not treat an upstream 402 body as needing normalization. Upstream status logs alone also could not prove the client-facing response body.
- **What we did:** Normalize recognized terminal quota bodies from 402, 403, or 429 into the same HTTP 402 OpenAI `insufficient_quota` envelope; preserve unrelated responses; canonicalize reason codes; and add `terminal_quota_normalized` evidence with upstream/client status and normalized type/code. Re-fetched and audited current `BigStrongSun/ccswitchmulti` and `farion1231/cc-switch` commits and recent PRs.
- **Reference verdict:** Do not merge either reference branch wholesale. Adapt upstream `e28f6c7a7` for its `response.failed` terminal SSE envelope but retain this fork's bounded retry budget. Port original `e724270db` plus the still-open PR #7177 follow-ups for Images edits after a focused conflict audit. Consider small independent updater-error commit `bd1265d2a`. Do not port PR #7188's manually selected multiple-key schema over the fork's isolated/pool credential ownership. UTF-8 URL masking, Windows test-home isolation, OAuth parallel calls, and rich input modalities already have equivalent local fixes.
- **Evidence:** `cargo check --manifest-path src-tauri/Cargo.toml` passed. Five focused forwarder tests passed individually: terminal 429, terminal 403, terminal 402, transient 429 recovery, and Sublyx text-envelope parsing. `git diff --check` passed. The running debug executable predates the source and lacks the new event marker, so live acceptance is not claimed.
- **What NOT to do again:** Do not infer a client-facing rewrite from an `upstream_status` log, and do not assume one provider uses a single HTTP status for the same quota condition.

## 2026-09-06 - Preserve terminal daily quota errors across Codex retries

- **What happened:** Sublyx returned `DAILY_LIMIT_EXCEEDED` with `daily usage limit exceeded`, but Codex displayed only `exceeded retry limit, last status: 429 Too Many Requests`.
- **Root cause:** CCSwitch correctly stopped its own replay for recognized quota exhaustion but returned the original HTTP 429. Codex treats every 429 as transient, retries at its client layer, and replaces the upstream body with its generic retry-limit message.
- **What we did:** Normalize only recognized terminal daily/billing quota 429 responses to HTTP 402 with an OpenAI-style `insufficient_quota` envelope. Preserve the provider message and reason, remove `Retry-After`, and leave unknown or transient 429 handling unchanged.
- **Follow-up:** September 7 logs showed Sublyx also returns the same terminal quota condition as HTTP 403. The forwarder now inspects 403 bodies, normalizes recognized quota exhaustion to the same 402 envelope without retrying, and preserves unrelated 403 responses.
- **Correction:** The September 7 `status=402` router entries retained Sublyx's original gRPC-style body, proving that 402 was another upstream variant rather than proof of prior normalization. Recognized upstream 402 quota bodies are now normalized too, and `terminal_quota_normalized` records the upstream status plus the exact client-facing status/type/code.
- **Evidence:** September 6 router and application logs recorded the original Sublyx `reason="DAILY_LIMIT_EXCEEDED" message="daily usage limit exceeded"` while Codex rollout events recorded `response_too_many_failed_attempts`. Both reference repositories and open/closed issue history were audited; no existing response-boundary fix was available. `cargo check --manifest-path src-tauri/Cargo.toml` passed. The terminal-quota text-envelope test, terminal retry test, and transient-429 recovery test each passed 1/1; the terminal retry test initially exposed and then passed after correcting underscore-form message normalization. `git diff --check` passed.
- **What NOT to do again:** Do not return a terminal quota exhaustion as a generic retryable 429 to Codex, and do not disable retries for unclassified 429 responses.

## 2026-09-06 - Summarized-session verification after fresh-session handoff

- **What happened:** Final verification for the flattened summarized-session job contract was recovered in a fresh task after the original full-suite runners disappeared.
- **Root cause:** The source fix is isolated and its focused coverage is green, while the shared dirty checkout contains unrelated frontend preset, catalog, and Hermes loader changes; the live pre-fix `cc-switch.exe` also prevents Cargo from replacing the debug executable.
- **What we did:** Reaudited the six notification/handoff files and all removed-contract references, reran focused frontend and Rust tests, and attempted both full suites without stopping the live process or creating another target directory.
- **Evidence:** `tests/lib/codexSummaryHandoff.test.ts` passed 7/7; `codex_desktop::tests` passed 44/44; `proxy::request_health::tests` passed 13/13; `cargo check --manifest-path src-tauri/Cargo.toml`, scoped Prettier, and `git diff --check` passed. The full frontend suite passed 170 files / 1408 tests and failed 5 files / 9 tests in `codexProviderPresets.reasoning`, `CodexFormFields`, `LargeModelEditors` (undefined `runWithLoading`), `ProviderForm.codexPreset`, and `codexCatalogSync`; `pnpm typecheck` failed on the same unrelated dirty Hermes scope error. Full Cargo reached compilation but failed with OS error 5 replacing the running `src-tauri/target/debug/cc-switch.exe`. Live Windows toast delivery remains unverified until the normal development process is rebuilt.
- **What NOT to do again:** Do not attribute shared-checkout failures to this notification contract, kill the live proxy to clear a build lock, or claim native toast acceptance from source tests.

## 2026-09-06 - Explicit Windows taskbar window icon

- **What happened:** A user screenshot showed a present taskbar button with the generic Windows application icon after `pnpm dev`.
- **Root cause:** The running main window had a small icon but no large icon (`WM_GETICON`: SMALL=258741199, BIG=0; class icons both zero). Tauri/wry delegates its window icon to Tao's small-icon API; the separate taskbar/large slot was never assigned. This is a confirmed assignment gap; whether it fully explains the screenshot still requires rebuilt-runtime acceptance. Extracted 32px PNGs from the configured ICO and running debug EXE had the same SHA256, `3E1F633E16DBD922D6A7A4538B1450276803745DDCC20C0BE5F8AB3875A9F3E5`, so the embedded asset was intact.
- **What we did:** Register a Windows-only window-ready plugin that loads Tauri's embedded icon resource (32512) and assigns `WM_SETICON/ICON_BIG` for each window, including recreation. Keep the shared resource alive under Windows ownership and leave the small icon and window visibility intact. Provider-logo diagnosis is separate: `e883528ba` deliberately removed automatic website-favicon inference; specific affected provider names remain unconfirmed.
- **Reference verdict:** Audited BigStrongSun/ccswitchmulti at `a169bf585` and farion1231/cc-switch at `db3461280`, recent icon history and relevant PRs. BigStrongSun's resource tracking and ICO fixes (`d7f012678`, `fe4e531b7`) are already inherited. Original [PR #5236](https://github.com/farion1231/cc-switch/pull/5236), `23ea773226749ec8615e7db1fbd6ca38bf275f51`, changes skip-taskbar behavior after reopening; rejected because this screenshot shows an existing button with a generic icon. No cherry-pick performed.
- **Evidence:** Changed-source `cargo check --manifest-path src-tauri/Cargo.toml` passed. `cargo test --manifest-path src-tauri/Cargo.toml --lib windows_window_icon::tests` passed: 1 test, 0 failures, after waiting for the normal target lock. The test verifies large-icon assignment, preserved small icon, and unchanged hidden visibility; it does not verify the production executable resource or shell rendering. Scoped `rustfmt --check` and `git diff --check` passed. Earlier provider resolver run passed 12 tests against unchanged provider source. No failed patch checks; full suites and rebuilt live-window acceptance have not run for this fix.
- **What NOT to do again:** Do not infer a deleted icon asset from a generic taskbar icon, alter Tao's documented small-icon contract, cherry-pick skip-taskbar changes for an icon-only symptom, clear shell caches as a root-cause fix, or use the pre-fix running EXE as acceptance evidence.

## 2026-09-06 - Unified summarized-session job and Windows notification ownership

- **What happened:** Reassessment found that a successful fresh-session job could be reported as failed because the injected renderer stored IDs under `job.result` while the dirty Rust poller read them from the job root. Windows result toasts also duplicated identity setup assumptions, used English-only text, and silently discarded delivery failures.
- **Root cause:** JavaScript and Rust had competing completion shapes (`status` plus nested `result.completed`), and notification ownership was split between the Request Health module and duplicated forwarder error handling.
- **What we did:** Flattened completed job data into one authoritative job object, removed the redundant summary result wrapper, retained Rust's root-field reads, centralized WinRT identity registration and localized result strings, and made Request Health own notification delivery-failure logging. Both current reference repositories were audited; neither contains this fork-specific handoff, while original PR #1635 is a separate Claude hook notification service and was not ported.
- **Evidence:** Production-JavaScript handoff tests passed 7/7, including exact flat job-shape assertions. `codex_desktop::tests` passed 44/44; `proxy::request_health::tests` passed 13/13; `cargo check --manifest-path src-tauri/Cargo.toml`, `pnpm typecheck`, scoped Prettier, and `git diff --check` passed. BigStrongSun main was `a169bf585b` and original main was `db34612807` at audit time. The live `cc-switch.exe` started before the final backend edit, so native Windows notification delivery remains unverified until the normal development process is rebuilt.
- **What NOT to do again:** Do not change only one side of an injected job contract, represent completion twice, discard WinRT delivery errors, or call source tests proof of a live Windows toast.

## 2026-09-06 - Reaudited global loading coverage across pages

- **What happened:** Direct page API actions were bypassing the global loading context even though query fetches and mutations were tracked automatically.
- **Root cause:** `runWithLoading` was used only in selected dialogs/hooks; standalone actions in OpenAI-compatible, Hermes memory, MCP, Codex usage, workspace, and history repair could leave users without the global loader. Deferred list rendering used the loader visual but was not registered as a global task.
- **What we did:** Registered deferred large-list rendering with `beginLoading`, and wrapped the identified direct API actions with `runWithLoading`. Reaudited async page operations and duplicate state writers, retaining existing local pending states for button-level UX.
- **Evidence:** `pnpm typecheck` passed; focused loading/page/model tests passed (including OpenCode 12/12, large editors 2/2, global loading 4/4, deep link 2/2, settings dialog 10/10); `git diff --check` passed. Full frontend suite still has unrelated existing preset/Codex expectation failures; full Rust tests remain blocked by the live `cc-switch.exe` lock.
- **What NOT to do again:** Do not assume React Query coverage includes direct IPC/API calls, and do not call a deferred loader global merely because it renders a spinner.

## 2026-09-06 - Full suite rerun after in-list loader fix

- **What happened:** Final frontend verification was rerun against the actual 400+ model loader implementation.
- **Root cause:** The loader/list fixes were not responsible for the remaining suite failures; they are existing dirty-tree expectation or preset/capability mismatches.
- **What we did:** Ran the full frontend suite after the latest source and test changes; retained the live process and normal build target constraints for Rust.
- **Evidence:** `pnpm test:unit -- --reporter=dot` completed with 171 passing files / 1407 passing tests and 4 failing files / 8 failing tests: DeepSeek reasoning parameter, Zhipu GLM preset aliases, Codex rotation visibility, Codex preset endpoint, and catalog hydration expectations. `pnpm typecheck` and focused 19/19 model-loader tests pass. Full Cargo tests remain blocked by the live `cc-switch.exe` target lock.
- **What NOT to do again:** Do not conflate unrelated preset/expectation failures with the model-list loader regression; investigate them as a separate batch.

## 2026-09-06 - Paint loaders inside large provider model lists

- **What happened:** Provider forms with 400+ OpenRouter/OpenCode models still blocked before users could see the existing page loader.
- **Root cause:** Pagination limited the final DOM but model row controls were still constructed in the same synchronous render, so the loading shell could not paint. OpenCode also mounted a CodeMirror editor containing the entire model catalog alongside the structured list.
- **What we did:** Added deferred render factories that commit a loader before constructing large row trees, applied them to OpenCode and the shared OpenClaw/Hermes/dropdown pager, reduced OpenCode pages from 40 to 20 rows, and collapse the large OpenCode raw JSON editor until requested. Opening the raw editor also paints a loader before CodeMirror mounts.
- **Evidence:** `pnpm typecheck` passed. Focused Vitest passed 5 files / 19 tests, including 431-model OpenCode, OpenClaw, Hermes, shared pager, and dropdown cases. The OpenCode regression asserts the in-list loader, 20 mounted rows, and search access to model 430.
- **What NOT to do again:** Do not call a list optimized merely because the final DOM is paged; expensive row construction and duplicate full-catalog editors must also sit behind a paint boundary.

## 2026-09-06 - Final verification boundary results

- **What happened:** Full verification was attempted after the competing-writer fixes.
- **Root cause:** One existing Codex form expectation conflicts with the dirty-tree behavior that exposes the rotation selector; the Rust test binary was locked by a running `cc-switch.exe`.
- **What we did:** Ran the complete frontend unit command and the complete Cargo test command without killing the live process or creating an alternate target.
- **Evidence:** Frontend suite reported one failure in `tests/components/CodexFormFields.test.tsx` and otherwise passed; `cargo test --manifest-path src-tauri/Cargo.toml` reached test build then failed with `Access is denied` removing `src-tauri/target/debug/cc-switch.exe`. Targeted tests, `pnpm typecheck`, `cargo check`, and `git diff --check` passed.
- **What NOT to do again:** Do not kill a live `cc-switch.exe` to clear a Cargo lock, and do not report the full suite as green when the Codex expectation still fails.

## 2026-09-06 - Fixed competing async and state writers

- **What happened:** The overwrite audit findings were converted into owned-layer fixes.
- **Root cause:** CodeMirror listeners captured stale callbacks and treated external hydration as user edits; provider raw JSON and structured model state did not synchronize; settings refetches replaced dirty drafts; daily-memory and deep-link async completions had no identity gate; settings full replacements wrote outside the shared lock and used truncate writes.
- **What we did:** Added callback refs and hydration annotations to both editors, valid-JSON rehydration for OpenCode/OpenClaw/Hermes structured state, dirty-field preservation during settings refetch, request sequence guards for daily-memory reads and deep-link imports, and atomic locked settings persistence.
- **Evidence:** `pnpm typecheck` passed; focused frontend runs passed 8 settings-hook tests, 2 large-model tests, and 11 loading/deep-link/editor-adjacent tests; `cargo check --manifest-path src-tauri/Cargo.toml` passed; `git diff --check` passed. Full frontend/backend suites and live Tauri runtime were not tested.
- **What NOT to do again:** Do not let external editor hydration call form callbacks, let stale async completions publish state, or persist full settings snapshots outside the settings lock.

## 2026-09-06 - Competing writers audit finds remaining overwrite paths

- **What happened:** Re-auditing beyond loading indicators found actual competing state writers. Findings remain open; application behavior was not changed in this audit.
- **Root cause:** Provider raw JSON and structured model lists own separate drafts; CodeMirror listeners capture old callbacks and echo hydration; settings refetch replaces local edits; backend full-settings writes persist outside the lock used by field mutations. Daily-memory and deep-link completions lack request identity guards.
- **What we did:** Traced ownership boundaries, reproduced frontend defects, and retained the [audit and probe fixture](incidents/2026-09-06-competing-writers-audit.md). Both references were inspected; original upstream 84e75ad2 has an applicable JSON callback-ref fix but does not settle every finding.
- **Evidence:** Temporary actual-component/hook Vitest probes: first run 6/6 assertions failed, expanded run 8/8 failed on the identified defects. Fixture moved outside normal test discovery afterward. Backend and asynchronous event findings are source-level, not runtime-tested. No application edits, backend/full suites, typecheck, or live Tauri inspection in this audit.
- **What NOT to do again:** Do not claim loaders fix competing writers, blindly wire two-way effects, or equate a per-widget pending flag with serialized settings persistence.

## 2026-09-06 - Follow-up loading audit and async editor identity

- **What happened:** Follow-up to the large-model entry below found additional pending work without feedback and editor identity races.
- **Root cause:** Workspace reads had no cancellation/error gate; provider live readiness survived provider changes; ProviderForm discarded common-config loading flags. Usage filters still mounted every model, and direct export/deep-link work bypassed query loading tracking.
- **What we did:** Keyed editor sessions by identity, ignored cancelled file reads, blocked saving failed reads, gated form initialization, bounded usage filters, and exposed export/deep-link/retry/reverse-migration pending states. Preserved encoded filter values so a literal model named `all` remains selectable.
- **Evidence:** Router suite now passes 90/90 with a real virtualizer and explicit jsdom viewport. Seven related files passed 33/33 tests; migration tests passed 2/2, picker tests 3/3, and delayed common-config regression 1/1. Broader Codex consumer rerun passed 67/72: four failures expect older GLM preset values, one expects rotation hidden despite the pre-existing CodexFormFields edit. Typecheck still reports the pre-existing unused `onPruneOutdated` parameter. Desktop/mobile Chromium picker checks passed with 20 mounted options, 280px JSON editor, and no overflow. Scoped formatting passed. No backend/full suites or live Tauri verification.
- **What NOT to do again:** Do not assume editor identity remains constant while open, allow failed reads to become writable empty documents, or treat passing targeted tests as proof that every UI is fixed. See the [follow-up audit](incidents/2026-09-06-large-model-loading.md#follow-up-audit).

## 2026-09-06 - Bound large model rendering and expose pending work

- **What happened:** 400+ model catalogs could stall provider forms and model choosers while loading feedback was absent or hidden by cached data.
- **Root cause:** Eager model-option/editor rendering and unbounded raw JSON editor height created synchronous work; initial observed queries were absent from the global indicator, and edit forms mounted before live configuration settled.
- **What we did:** Added searchable bounded model lists, deferred form/menu mounting with a loading shell, explicit JSON editor heights, initial-query/manual loading coverage, and refresh spinner precedence. Virtualized the router ordering catalog while retaining cross-viewport dragging, and indexed provider/model ownership instead of repeatedly scanning catalogs. Reference repositories had useful searchable UI but no complete bounded-rendering solution. See [audit and scope](incidents/2026-09-06-large-model-loading.md).
- **Evidence:** Targeted frontend runs passed 16 files / 145 tests; the 2 large-editor tests passed again after their typing correction. Headless Chromium checked 431-model editors, shared picker, JSON viewport, and router keyboard/cross-viewport drag ordering at desktop/mobile sizes. Scoped formatting and `git diff --check` passed. Final `pnpm typecheck` failed only at the pre-existing dirty `CodexProviderReadinessSection.tsx:68` unused `onPruneOutdated` parameter. Existing test warnings remain. Live Tauri, backend/full suites, and complete router save workflows were not tested.
- **What NOT to do again:** Do not add spinners while retaining unbounded synchronous rendering, truncate persisted catalogs to improve performance, remove cross-catalog drag semantics, or claim component browser checks verify the running Tauri process.

## 2026-09-05 - Separate Codex API-key groups from pooled rotation

- **What happened:** A Sublyx `gpt-5.6-sol` request could use a secondary subscription key when primary and secondary groups listed the same model.
- **Root cause:** Group lookup matched only the final model string and silently chose the first equal-specificity group. Group labels, IDs, and `upstreamModel` were not part of authentication identity.
- **What we did:** Added an explicit `isolated` (default) versus `round_robin` group mode. Isolated mode generates group-qualified catalog models carrying `apiKeyGroupId`; the proxy authenticates from the visible qualified model and sends the shared upstream model. Round-robin mode intentionally pools the fallback key and equally specific matching group keys with a stable cursor and de-duplication. Legacy configurations default to isolated behavior, and isolated group projection forces the model menu on.
- **Evidence:** `pnpm typecheck` passed; `cargo check --manifest-path src-tauri/Cargo.toml` passed; focused frontend run passed 4 files / 72 tests; focused Rust grouped-key run passed 3 tests; scoped Prettier and `git diff --check` passed. `cargo fmt --check` still reports the pre-existing module-order change in dirty `src-tauri/src/proxy/providers/mod.rs`; it was not rewritten.
- **What NOT to do again:** Do not treat a display label or array position as credential ownership, and do not silently first-match duplicate model assignments.

## 2026-09-05 - Ported catalog and Moonshot compatibility fixes

- **What happened:** Re-evaluated both reference repositories and ported the low-risk parts of original commits `bc4ed66d3`, `db41d7018`, and `e47b5fca1`.
- **Root cause:** Older Codex builds can reject refreshed catalogs missing `supports_parallel_tool_calls`; Moonshot/Kimi Chat rejects `$ref` siblings; Zhipu's current GLM Coding Plan Responses endpoint is `/api/v1`, not the legacy Chat path.
- **What we did:** Backfilled the required catalog field, added a host-scoped idempotent `$ref`→`allOf` rewrite only on Responses→Chat Moonshot/Kimi requests, added legacy Zhipu host fallback for native Responses catalog selection, and updated CN/EN GLM presets to GLM-5.3/GLM-5-Turbo on `/api/v1`.
- **Evidence:** Focused Rust tests passed: 2 catalog tests, 1 catalog-profile test, and 3 Moonshot schema tests. Focused frontend preset tests passed: 9/9. `cargo check` and targeted rustfmt checks passed.
- **What NOT to do again:** Do not cherry-pick the reference commits wholesale; their provider, catalog, and proxy architectures differ from this fork.

## 2026-09-05 - Correct clipped model catalog expansion

- **What happened:** The user reported that expanding the redesigned model list showed no usable editor. This corrects the UI acceptance evidence in the earlier "Preserve model modalities and scale provider catalogs" entry.
- **Root cause:** The virtualized list sized its scroll viewport from collapsed row count (only 52px for one filtered result), while the expanded full editor could be hundreds of pixels tall. The earlier browser harness substituted a tiny input for the real editor and therefore did not validate the actual form. Reasoning rows also gained an unnecessary second disclosure layer.
- **What we did:** Replace absolute virtual rows with ten-row pagination and normal-flow expansion, retaining stable model IDs and filter/selection semantics. Label editor fields in a responsive two-column layout. Render reasoning summaries directly within pagination so their existing Configure control opens the editor in one click. Add Model clears filters, reveals the new model's page, and focuses its editor. Keep the grouped input-capability control with yellow Unknown and blue known states. The earlier reference-repository audit found no matching editor solution to port.
- **Evidence:** Five targeted frontend files: 84 tests passed. Scoped Prettier and `git diff --check` passed. Headless Chromium mounted the actual CodexFormFields with 431 synthetic models at 1280x900 and 375x812: later-page edits, single-result expansion with a hit-tested visible context input, collapse/reopen persistence, yellow Unknown, one-click reasoning, and adding while filtered passed without horizontal overflow. Harness and inspected screenshots: `C:/Users/felix/AppData/Local/Temp/ccswitch-model-metadata-20260905/full-form-qa.mjs` and `full-form-*.png`. Initial test/browser selectors mismatched translation keys/pluralized labels; corrected reruns passed. Node localStorage and existing React act warnings remain. Browser IPC was mocked; this is renderer verification, not live Tauri or backend acceptance. Full suites were not run for this frontend correction.
- **Type check:** `pnpm typecheck` passed after the final source changes.
- **What NOT to do again:** Do not accept placeholder-editor screenshots as evidence for the full ProviderForm, or place expanding forms inside viewports sized only for collapsed rows.

## 2026-09-05 - Preserve model modalities and scale provider catalogs

- **What happened:** Follow-up to the OpenRouter audit below: the user requested the complete importer and large-list UI fix.
- **Root cause:** Online models.dev enrichment was gated only by missing context and copied only context; capability normalization and reference hydration collapsed full modality lists into text/image. Catalog and reasoning editors mounted every model, making 400+ entries expensive to manipulate.
- **What we did:** Enrich missing modalities and image support from the endpoint-matched models.dev provider, even when context exists. Require a URL path boundary, prefer the longest matching API prefix, reject ambiguous suffixes, and preserve upstream declarations and user overrides. Keep full file/audio/video declarations through normalization, preset hydration, and image toggles. Add capability/usage filters, view-only sorting, persistent bulk actions, and virtualized expandable catalog/reasoning rows for lists over 20 entries. Keep focused editors mounted during scrolling. Unknown remains yellow in the three-segment editor; known states remain blue. Source provenance fields were not added to the persistent catalog schema.
- **Reference verdict:** Rechecked both repositories' recent CodexFormFields history and open/closed catalog PRs. Original upstream PR #6750 fixes generated vendor-catalog modality inheritance, not fetching or large-list editing; its fail-open behavior was not ported. BigStrongSun's nearby work concerns protocol evidence and alias/projection behavior. Adapted the repository's existing TanStack virtualization dependency locally.
- **Evidence:** Model-fetch Rust tests: 41 passed. Capability Rust tests: 5 passed. Six targeted frontend files: 80 passed; after the final reasoning-viewport change, both affected component files reran with 59 passed. `cargo check --manifest-path src-tauri/Cargo.toml`, `pnpm typecheck`, scoped Prettier/rustfmt, and `git diff --check` passed. Headless Chromium exercised 431 rows at 1280x800 and 375x812: 15 initially rendered, last model reachable, selection/editing worked, focused edits survived scrolling, no horizontal overflow. Screenshots and browser harness are beside the API response files in the audit's temporary evidence directory. An initial jsdom viewport test failed on zero dimensions and passed after supplying geometry; initial browser harness imports failed and passed after correcting Vite paths. React act/localStorage warnings and browser HMR WebSocket local-network warnings remain. Full suites and live Tauri acceptance were not run; user rebuild requested under rule 10-C.
- **What NOT to do again:** Do not replace rich modality lists with binary defaults, fill conflicting metadata over an explicit declaration, infer capabilities across provider boundaries, or virtualize an editor without preserving focused in-progress input.

## 2026-09-05 - OpenRouter publishes modalities that our importer discarded

- **What happened:** Manual public API requests returned 431 OpenRouter models with explicit input modalities, versus 71 OpenCode Zen and 35 Go models with only ID/bookkeeping fields. models.dev separately had exact capability matches for 71/71 Zen and 34/35 Go IDs. [API audit and response hashes](incidents/2026-09-05-model-modality-api-audit.md).
- **Root cause:** The model-fetch parser omitted OpenRouter's `architecture` container. Missing UI capabilities therefore did not prove absent upstream metadata. The online models.dev fallback also currently imports context limits only; local preset bundles have a separate capability enrichment path.
- **What we did:** Added architecture parsing with input-versus-output regression coverage. Kept the grouped Unknown segment read-only and made its selected state yellow while known states remain blue. Audited both reference repositories; neither supplied a matching parser fix. Online enrichment policy and the full model-list redesign remain outside this patch.
- **Evidence:** `cargo test --manifest-path src-tauri/Cargo.toml --lib services::model_fetch::tests`: 40 passed; `cargo test --manifest-path src-tauri/Cargo.toml --lib model_capabilities::tests`: 5 passed; `pnpm test:unit tests/components/CodexFormFields.test.tsx tests/components/ProviderForm.codexCatalog.test.ts`: 66 passed. `cargo check --manifest-path src-tauri/Cargo.toml`, `pnpm typecheck`, scoped Prettier, scoped rustfmt, and `git diff --check` passed. Frontend tests emitted React act and Node localStorage warnings. No failed checks in this patch; full suites and live desktop acceptance were not run. The running executable predates the parser fix and requires a user rebuild before runtime verification.
- **What NOT to do again:** Do not infer absence of provider metadata from an Unknown badge, confuse image output with image input, or describe all non-image models as text-only: this OpenRouter snapshot included 12 models accepting files/audio plus text without images. Inspect the actual response and each importer boundary first.

## 2026-09-05 - Fresh handoff acknowledgement must not wait on its own creator

- **What happened:** The user-authorized current-session trigger saved a native summary at 11:13:04 and created root task `01a06f8e-3112-77f0-a3da-c2b661584900`. The runner reported a fresh-handoff timeout at 11:15:06, although the new task's first turn subsequently completed after 203434 ms.
- **Root cause:** The handoff prompt allowed immediate actions named in the summary. The transferred summary told the receiving task to inspect and wait for the runner, while that runner was waiting for the receiving task's acknowledgement to complete. The receiving task's recorded commands and sleeps demonstrate this circular wait; a saved summary alone was not proof that the orchestrator completed successfully.
- **What we did:** Restricted the initial handoff prompt to acknowledgement only, explicitly excluding tools, unfinished actions, and waiting for the runner. Retained the summary for future turns and did not increase timeouts or repeat native mutations. This is a prompt-boundary correction, not a guarantee against arbitrary model latency or noncompliance.
- **Evidence:** The runner JSONL recorded a new 11713-character persisted summary and fresh-session start. The new rollout has matching root session/task IDs and no fork parent; app task inspection reports its first turn completed. The 5 production-JavaScript handoff tests passed, including the acknowledgement-only prompt contract; `pnpm typecheck`, scoped Prettier, and `git diff --check` passed. Full-suite verification was started separately. The direct trigger bypassed Windows notification dispatch and Rust review ownership. The prompt change has not been live-tested and requires the user's normal `pnpm dev` restart before runtime acceptance.
- **Final verification:** `pnpm test:unit` finished with 164 files passed / 2 failed, 1383 tests passed / 2 timed out (5000 ms): maintained preset identity in `ProviderForm.codexPreset.test.tsx` and malformed-profile batch removal in `CodexSubagentV2ProfileEditor.test.tsx`. Both cases passed when rerun alone (2 passed, 141 skipped); the full run remains failed. Full `cargo test --manifest-path src-tauri/Cargo.toml` failed before execution because Windows denied removal of the running `target/debug/cc-switch.exe` (os error 5); the proxy was not stopped. `cargo check --manifest-path src-tauri/Cargo.toml` passed, and `cargo test --manifest-path src-tauri/Cargo.toml --lib codex_desktop::tests` passed 44/44. No full backend acceptance or live corrected-prompt acceptance is claimed.
- **What NOT to do again:** Do not authorize execution of summarized action instructions during an acknowledgement the creator is waiting for; do not equate a timeout with absence of a created task, retrigger blindly, or claim direct CDP invocation verifies the notification path.

## 2026-09-05 - Subscription-group credentials are fixed model bindings

- **What happened:** Sublyx credentials for separate subscription groups (Astra versus the regular subscription) were presented as rotating credentials, without a per-key model assignment workflow.
- **Root cause:** New groups defaulted to round robin; catalog discovery aggregated keys without assigning discovered models to their owning group. Parsing controlled comma-separated inputs also removed separators while typing. An added concurrent-discovery regression reproduced a lost assignment when two callbacks reused the same render's group array.
- **What we did:** New groups default to a fixed key, expose per-key discovery and separate exact-model/prefix assignments, and do not show rotation/add-key controls for the fixed single-key setup. Unassigned fixed groups no longer catch all requests. Existing legacy rotation behavior is preserved. Per-group discovery discards stale responses, and the parent merges concurrent completions against the latest group list. Both reference repositories' recent commits, all-state PR listings, and nearby implementation history were audited; neither supplies this fork-specific fix.
- **Evidence:** `pnpm exec vitest run tests/components/CodexKeyGroupModels.test.tsx tests/components/CodexFormFields.test.tsx tests/components/ProviderForm.codexPreset.test.tsx tests/components/RequestHealthPanel.test.tsx tests/lib/codexSummaryHandoff.test.ts --maxWorkers=2 --minWorkers=1` passed 5 files / 83 tests after correcting test fixtures and the reproduced assignment race. `cargo test --manifest-path src-tauri/Cargo.toml --lib proxy::providers::codex::tests` passed 130 tests; `--lib codex_desktop::tests` passed 44; `--lib proxy::request_health::tests` passed 13. `cargo check` and `cargo fmt --check` with the repository manifest passed. Changed TS/TSX Prettier checks and JSON/new-key checks for all four locales passed. Existing React act/MSW and Node localStorage warnings remain non-failing. No live credentials or settings were changed; runtime acceptance and full suites were not run.
- **Final type verification:** `pnpm typecheck` and `git diff --check` passed after correcting the new test fixtures. No failing targeted tests remain.
- **What NOT to do again:** Do not treat distinct subscription-group credentials as interchangeable rotation keys, make an unassigned fixed group a catch-all, or overwrite another group's completed discovery with stale React props.

## 2026-09-05 - Summary handoff evidence is not live acceptance

- **What happened:** The user reported that Windows Request Health's summarize-and-restart still did not work. Ignoring the notification remains fail-closed: dismissal or review expiry blocks; the default review timeout is 60 seconds.
- **Root cause:** The recorded September 4 failures at 15:35, 16:26, and 16:29 timed out waiting for the blocked source turn to become idle; the corresponding rollouts contained no compaction entry. These failures predate commit `165dca043817aabf303d801b6cb0c1e855243653`, which owns the handoff lifecycle and interrupts the active source turn. No later live attempt was established from the inspected logs, so the present runtime complaint remains unverified.
- **What we did:** Added executable tests against the production injected JavaScript for exact-turn interruption, waiting for idle, interrupt failure/timeout, root-session creation from summary-only input, and failed handoff reporting. Did not add speculative runtime changes or replay any user conversation.
- **Evidence:** All 5 executable summary-flow tests passed, alongside 44 desktop and 13 Request Health Rust tests above. The running debug executable was newer than the prior lifecycle correction, but predates this task's credential-routing edits. These unit tests do not establish live Desktop/CDP success. User restart of normal `pnpm dev` is required before runtime verification of the changed routing source.
- **What NOT to do again:** Do not call a mocked handoff or source-order assertion proof of a successful live new session, blame a stale binary without timestamp evidence, or kill the user's running proxy to test a source edit.

## 2026-09-05 - UI async work has a centralized delayed activity indicator

- **What happened:** Async feedback was implemented independently across the UI, so direct promise handlers could leave the application looking hung even when the WebView event loop remained responsive. The large-catalog investigation also exposed eager fetched-model menus copied across multiple provider forms.
- **Root cause:** TanStack mutations, direct Tauri API promises, and synchronous render work had no shared activity boundary. Most screens had local pending state, but first-run acknowledgement, workspace scans, skill-repository changes, provider add/edit flows, usage confirmation/formatting, and Codex history migration/restore had no universal fallback. OpenClaw, Hermes, Claude, and Gemini form paths retained eager model dropdown implementations. Neither current `BigStrongSun/ccswitchmulti` nor `farion1231/cc-switch` provides a centralized loader to port.
- **What we did:** Added a root `GlobalLoadingProvider` with a 300 ms anti-flicker delay, automatic observation of TanStack mutations, overlap-safe manual `runWithLoading`/`beginLoading` APIs, and a non-blocking top activity indicator. Wired the uncovered direct promise paths and consolidated all provider fetched-model selectors onto the lazy shared `ModelDropdown`. Kept local progress/cancel UI authoritative and did not treat a loader as a fix for main-thread stalls.
- **Evidence:** Static audit found **67** production TSX files with async handlers: **66** have local progress state or `useGlobalLoading`; the only uncovered file is the intentionally background-only native theme sync. All **64** `useMutation` sites are observed automatically, and no eager `fetchedModels` map/reduce remains in provider forms. `pnpm exec vitest run tests/components/GlobalLoadingProvider.test.tsx tests/components/AddProviderDialog.test.tsx tests/components/EditProviderDialog.test.tsx tests/components/OpenCodeFormFields.test.tsx tests/components/ModelDropdown.test.tsx tests/components/ClaudeFormFields.test.tsx tests/components/ClaudeDesktopProviderForm.test.tsx tests/components/CodexFormFields.test.tsx tests/components/ProviderForm.codexPreset.test.tsx --maxWorkers=2 --minWorkers=1` passed **9/9 files and 108/108 tests**; `pnpm typecheck`, changed-file Prettier checks, and `git diff --check` passed. Existing `CodexFormFields` React `act(...)` and missing-MSW-handler warnings remain non-failing.
- **What NOT to do again:** Do not add an untracked async user action without local pending UI or `runWithLoading`; do not make passive polling trigger a blocking overlay; and do not mask synchronous rendering stalls with a spinner instead of bounding the work.

## 2026-09-05 - Large OpenCode catalogs no longer block the provider form

- **What happened:** Opening an OpenRouter/OpenCode provider with roughly 430 models made the entire Tauri WebView unresponsive, and editing a model name repeatedly stalled the form.
- **Root cause:** `OpenCodeFormFields` synchronously mounted every editable model row. After model discovery, every row also instantiated a `ModelDropdown` that eagerly grouped and mapped the entire fetched catalog, producing quadratic work (about 430 x 430 model choices). Model display names additionally cloned and pretty-serialized the complete catalog on every keystroke. Current `BigStrongSun/ccswitchmulti` and `farion1231/cc-switch` branches and related recent PR history retain the same unbounded row map; there was no reference fix to port.
- **What we did:** Bounded the editor to 40 rows per page, added searchable access to the complete catalog and icon pagination, committed display-name edits on blur, and made shared fetched-model dropdown contents lazy so choices are built only for the open menu. Updated all four locales and added a 430-model regression test.
- **Evidence:** `pnpm exec vitest run tests/components/OpenCodeFormFields.test.tsx tests/components/ModelDropdown.test.tsx tests/components/CodexFormFields.test.tsx tests/components/ClaudeDesktopProviderForm.test.tsx --maxWorkers=2 --minWorkers=1` passed **4/4 files and 78/78 tests** (the existing `CodexFormFields` React `act(...)` warnings remain); `pnpm typecheck` passed; changed TS/TSX files passed Prettier check; all locale JSON parsed with the five new keys present in all four languages; `git diff --check` passed.
- **What NOT to do again:** Do not render provider-sized editable collections without a hard DOM bound, create a full fetched-model menu once per row while those menus are closed, or serialize the entire provider catalog on each text keystroke.

## 2026-09-05 - Probe concurrency WIP ported onto the consolidated main

- **What happened:** The pre-consolidation probe-concurrency WIP (4 files, leased by a stale `/root` session with no running process) blocked the fast-forward of `main`. The user chose to port it rather than discard it.
- **Root cause:** The WIP was written against `d8a974855`; the incoming consolidation rewrote the same `runner.rs` dispatch region and added `probe_all_transports`, so the branch-dispatch hunks collided on apply.
- **What we did:** Stashed the WIP, fast-forwarded `main` to `fd28a9002`, re-applied via `git stash apply` (never `pop`), and merged both intents in `runner.rs`: `tokio::join!` runs the two transport branches concurrently when `probe_all_transports` is set, while assigned-transport probing runs directly. The auto-merged command layer (dedupe by lease key + `buffer_unordered(3)`) and store waiter (180s `Notify` queue) needed no changes.
- **Evidence:** `cargo check` exit 0; `cargo test --lib -- protocol_compatibility store` = **190 passed, 0 failed, 1 ignored**; named runs of the new store waiter tests and `deep_probe_starts_responses_and_chat_before_either_baseline_finishes` passed.
- **What NOT to do again:** Do not leave WIP leased or stashed across a consolidation of the same files; port with `git stash apply` so the stash entry survives a failed merge.

## 2026-09-05 - Repository consolidated to a single `main` branch

- **What happened:** Per user directive, the repo now carries exactly one branch (`main`). Deleted local branches `codex/integrated-fixes-2026-09-01` (`2ebc22694`, AGENTS.md-only scratch commit), `codex/pre-consolidation-backup` (`680a09a37`, pre-rewrite backup whose content was verified identical and pushed during the consolidation), and `codex/upstream-integration-2026-09-03` (`63c25e7a9`); fork remote branches including 5 Dependabot refs and the stale pre-consolidation line `codex/sublyx-responses-review-20260902` (`125f971b6`); the duplicate `multirouter` remote (same URL as origin); and stale worktrees (`ccswitchmulti-release-verify-20260829`, plus the pruned `ccswitchmulti-upstream-20260903` registration).
- **Root cause:** Branch sprawl accumulated from integration audits, a retained history-rewrite backup, Dependabot PR branches, and a second remote mirroring the same GitHub repository under a different name.
- **What we did:** Cherry-picked the 5 unmerged upstream-integration commits onto origin/main in a detached temp worktree (main worktree checkout was blocked by leased dirty files), resolved the journal conflict by re-inserting the audit entry in chronological position, verified, pushed, then deleted everything else. Every deleted tip SHA is recorded in [the consolidation ledger](incidents/2026-09-05-branch-consolidation-ledger.md).
- **Evidence:** `cargo check` exit 0; `cargo test --lib -- protocol_compatibility session_usage hermes` = **250 passed, 0 failed, 1 ignored**; `codexMultiRouterWizard.test.ts` **8/8**; `pnpm typecheck` exit 0; push to origin/main succeeded.
- **What NOT to do again:** Do not keep pre-rewrite backup branches past the verified-push boundary; do not add a second remote for the same URL; record deleted tip SHAs before deletion.

## 2026-09-04 - Development builds no longer claim installed-app auto-launch ownership

- **What happened:** Running a development build could reconcile the shared auto-launch setting against its own executable and replace or remove the installed CCSwitchMulti startup registration.
- **Root cause:** Development and installed builds share the application name and settings file, while reconciliation treated the current executable as authoritative without distinguishing build mode or registry ownership.
- **What we did:** Debug builds now refuse to enable auto-launch, skip normal startup reconciliation, and remove a stale Run value only when it resolves to that exact development executable. Release reconciliation separately distinguishes an absent Run value from a mismatched executable while continuing to respect an explicit Task Manager disable.
- **Evidence:** `cargo test --manifest-path src-tauri/Cargo.toml --lib auto_launch` passed **6/6**; `cargo check --manifest-path src-tauri/Cargo.toml` passed; `git diff --check` passed.
- **What NOT to do again:** Do not let a development executable create or take ownership of an installed application's startup registration, and do not interpret an absent Run value as an owned value that should be deleted.

## 2026-09-04 - Summary handoff now owns and interrupts the blocked Codex turn

- **What happened:** Request Health detected sustained context growth and the user chose "Summarize + new session," but no fresh session was created; the background job timed out waiting for the source thread to become idle.
- **Root cause:** The handoff started before the blocked HTTP request had finished ending the Codex turn. Codex immediately replayed the identical request, sustained-growth recalculation allowed that duplicate upstream, and the new 15-second passive idle wait expired while the replay kept the source thread active.
- **What we did:** Added a session-scoped RAII handoff guard that blocks every normal Codex retry until the handoff completes or fails. The renderer now reads the active `inProgress` turn, calls the app-server `turn/interrupt` contract with its exact `threadId` and `turnId`, waits for the source thread to leave `active`, and only then starts native compaction and the fresh-root-session transfer. Neither `BigStrongSun/ccswitchmulti` nor `farion1231/cc-switch` contains this fork-specific handoff; farion PR #5536 addresses Chat-provider compaction wire translation instead.
- **Evidence:** Live logs showed the first handoff request at `16:26:22.673`, an identical `521502`-byte client replay at `16:26:23.097`, and timeout at `16:26:39.633`; client/post-transform/retry/upstream-wire sizes were `521502/521451/521502/521451`, proving no proxy inflation. Installed Codex `0.153.2` generated schema confirmed `turn/interrupt` requires `threadId` and `turnId`. `cargo test --manifest-path src-tauri/Cargo.toml --lib request_health` passed **15/15**; `cargo test --manifest-path src-tauri/Cargo.toml --lib codex_desktop` passed **45/45**; `cargo check --manifest-path src-tauri/Cargo.toml` passed; generated JavaScript syntax and scoped `git diff --check` passed.
- **What NOT to do again:** Do not passively wait for a proxy-blocked turn to become idle, let retry-risk recalculation clear a handoff already chosen by the user, or increase the timeout while the retry producer remains active.

## 2026-09-04 - Provider icons use a curated local identity registry

- **What happened:** Sublyx rendered from a low-resolution remote favicon in the provider form, while legacy provider cards on the main page could have no icon at all.
- **Root cause:** Form save paths and card rendering used separate fallback logic; the generic fallback persisted Google favicon proxy URLs as provider identity, and Sublyx was special-cased to its 80x80-site favicon instead of the shared bundled icon system.
- **What we did:** Added a bundled Sublyx SVG recreation based on the official 80x80 PNG, registered it in the extracted icon catalog, and centralized explicit-icon, curated-domain, provider-name, and nested-config inference. Main-page cards now resolve legacy records through the same helper; add/edit/form flows persist the stable icon key. Unknown providers fall back to initials instead of third-party favicon URLs. Neither `BigStrongSun/ccswitchmulti` nor `farion1231/cc-switch` had a Sublyx implementation to port; their relevant precedent favors bundled curated brand assets.
- **Evidence:** The official `https://sublyx.org/logo.png` measured 80x80 pixels and 6,996 bytes with SHA-256 `719AE6E7D2FC705CA05FB3F893A8EDA2D9EFA6F908434E1F7562C6F7A7CC4CA4`. Targeted Vitest passed **6/6 files and 44/44 tests**; `pnpm typecheck` passed; scoped `git diff --check` passed.
- **What NOT to do again:** Do not persist Google favicon proxy URLs or raw `.ico` files as provider identity, and do not add form-only icon inference without applying the same resolver to legacy card rendering.

## 2026-09-04 - Reintegration candidate passed the final delivery gate

- **What happened:** Completed the final verification after selecting the integrated-fixes tree and correcting two stale frontend test contracts.
- **Root cause:** The Light probe test selected its mode before opening the dialog that owns the selector. A separate interaction-heavy Sub-Agent V2 test had a five-second timeout even though it consistently required about 5.1–5.8 seconds on this Windows host and also failed on the old `main`.
- **What we did:** Moved the Light selection after opening the confirmation dialog and gave only the known long Sub-Agent interaction a 15-second timeout.
- **Evidence:** `cargo check --manifest-path src-tauri/Cargo.toml` passed; full `cargo test --manifest-path src-tauri/Cargo.toml` passed; `pnpm typecheck` passed; final `pnpm test:unit` passed **162/162 files and 1361/1361 tests**.
- **What NOT to do again:** Do not encode UI interaction order that contradicts component ownership, and do not use a global timeout increase when one explicitly complex test needs a documented local budget.

## 2026-09-04 - Promote integrated fixes as the canonical main line

- **What happened:** Audited `main`, `codex/integrated-fixes-2026-09-01`, and `codex/sublyx-responses-review-20260902` before consolidating the repository onto one canonical branch.
- **Root cause:** `main` and the integrated branch had independently rewritten histories sharing only the original repository root. The review branch was created from the stale `main` tip and its first patch attempted to restore save-time automatic protocol probing, which the integrated line had deliberately replaced with persistence-only provider saves and explicit verification.
- **What we did:** Preserved all original tips under dated backup refs; selected the integrated tree as canonical; rejected the stale review-branch patch after reviewing every conflict hunk; fixed the Light-probe test to select its mode inside the confirmation dialog; increased the timeout only for an existing interaction-heavy Sub-Agent V2 test that exceeded five seconds on both old `main` and the integrated candidate.
- **Evidence:** Before promotion, `cargo check --manifest-path src-tauri/Cargo.toml` passed; full `cargo test --manifest-path src-tauri/Cargo.toml` passed; `pnpm typecheck` passed. The first frontend run passed **1359/1361**, with the Light selector sequencing failure and one load-related App timeout; the App test passed alone in **14.98s**. After the Light test correction, its isolated run passed **1/1**. The Sub-Agent timeout reproduced on both old `main` and the integrated candidate at approximately **5.1s**, proving it was not introduced by the promotion.
- **What NOT to do again:** Do not merge branches with independently rewritten histories as if they were linear, do not restore automatic network probes to ordinary provider saves, and do not discard a review branch until its unique patches have been audited against the current architecture.

## 2026-09-04 - Correction: remove the full inline Light/Deep readiness panel

- **What happened:** The initial UI cleanup removed only the explanatory Readiness content but incorrectly left the Light/Deep selector visible in the screenshot area.
- **Root cause:** I misread the requested focus region as the text/status subsection instead of the entire nested panel.
- **What we did:** Removed the complete inline Light/Deep panel from `CodexProviderReadinessSection`; probe mode remains available in the confirmation dialog and the live validation summary remains visible.
- **Evidence:** `pnpm exec vitest run tests/components/CodexProviderReadinessSection.test.tsx --silent --reporter=verbose` passed **7/7**; `pnpm typecheck` passed.
- **What NOT to do again:** When a user identifies a whole visual section in a screenshot, remove the entire bounded section, not just its copy.

## 2026-09-04 - Codex readiness UI now keeps probe status without redundant guidance

- **What happened:** The Codex provider form repeated connection-readiness guidance and a “Verify connection first” badge below the probe mode selector, while the actual verification action and result were already available above/in the live status region.
- **Root cause:** The readiness component combined action controls, explanatory copy, and derived status into one panel, duplicating information and making the protocol probe flow look blocked even when per-model protocol metadata was already available.
- **What we did:** Removed the redundant Readiness explanation/badge, retained Light/Deep probe controls and accessible live probe summaries, and removed the obsolete maintained-preset prop from that component. Audited Codex Desktop model-picker projection and added a regression assertion that per-model `apiFormat` and `apiFormatSource` survive injection, so a provider default of Responses does not erase a probed Chat override.
- **Evidence:** `pnpm exec vitest run tests/components/CodexProviderReadinessSection.test.tsx tests/components/CodexFormFields.test.tsx --silent --reporter=verbose` passed **63/63**; `pnpm typecheck` passed. Rust targeted test compilation was blocked at link cleanup because `src-tauri/target/debug/cc-switch.exe` was in use (`Access is denied`, OS error 5); no process was terminated.
- **What NOT to do again:** Do not infer upstream protocol exchangeability from the provider default alone; preserve and inspect per-model catalog `apiFormat` metadata, and do not reintroduce duplicate readiness copy when the probe result is already exposed as a live status.

## 2026-09-03 - HTTP proxy status bypassed live uptime and target projection

- **What happened:** The live `/status` endpoint reported `uptime_seconds: 0` and an empty `active_targets` list even though the takeover listener had been running for hours and was actively routing Codex traffic.
- **Root cause:** `handlers::get_status` cloned the stored `ProxyStatus` directly. The live uptime calculation and current-provider projection existed only in `ProxyServer::get_status`, so HTTP callers bypassed both derived fields.
- **What we did:** Moved live status derivation into `ProxyState::status_snapshot` and made both the HTTP handler and `ProxyServer::get_status` use the same path.
- **Evidence:** Before the fix, live `/status` returned PID `57492`, `running: true`, `total_requests: 463`, but `uptime_seconds: 0` and no active targets. `cargo check --manifest-path src-tauri/Cargo.toml` passed, and `status_snapshot_reports_uptime_and_active_targets` passed **1/1**.
- **What NOT to do again:** Do not expose a stored status snapshot from one API while another API adds live derived fields; all status consumers must share the same projection.

## 2026-09-03 - Windows Request Health now transfers a summary into a fresh root session

- **What happened:** The Windows action labelled “Compact + new session” still ran native compaction followed by `thread/fork`, so the new task inherited the old session tree and did not provide the input-token reset the user required.
- **Root cause:** The implementation treated a fork of compacted state as equivalent to an independent session. The app-server exposes these as different ownership contracts: `thread/fork` copies ancestry, while `thread/start` creates a new root. The compacted handoff text is persisted in `compacted.payload.message` even though `thread/read` exposes only a `contextCompaction` marker.
- **What we did:** Replaced the action with “Summarize + new session.” After the original oversized request is blocked, the renderer waits for the source turn to become idle and runs native compaction. The backend requires a newly persisted handoff summary, then the live Desktop request client creates a root `thread/start`, verifies there is no fork ancestry and that `sessionId == thread.id`, submits only the summary through `turn/start`, and waits for that turn to complete. Added backward-compatible deserialization for the old setting name and documented the workflow.
- **Evidence:** Official OpenAI App Server documentation and the locally generated Codex 0.153 experimental schema confirmed `thread/start`, `turn/start`, and the root-thread fields. `cargo check` passed; Codex Desktop tests passed **45/45**; Request Health tests passed **11/11**; focused Request Health/workspace frontend tests passed **93/93**; `pnpm typecheck` and JavaScript syntax checking passed. Runtime Windows acceptance still requires the normal development binary to be rebuilt from this source.
- **What NOT to do again:** Do not label `thread/fork` as a fresh session, reuse an older persisted compaction summary, start compaction while the rejected source turn is still active, or report success before the new handoff turn reaches `completed`.

## 2026-09-03 - Reference-repository audit rule and Windows review dismissal semantics

- **What happened:** Audited the Windows Request Health notification behavior and checked recent upstream work in both `BigStrongSun/ccswitchmulti` and `farion1231/cc-switch` before considering further feature changes.
- **Root cause:** The notification is intentionally fail-closed: dismissal and unknown activation resolve the pending review as `Block`, while only the explicit Continue action releases the exact hash-bound request. The checkout also contains a large unrelated dirty batch, so automatic upstream integration would violate the merge boundary and risk overwriting in-progress work.
- **What we did:** Confirmed the dismissal path in `src-tauri/src/proxy/request_health.rs`; fetched `upstream` and `original`; reviewed recent commits and pull requests; added an AGENTS rule requiring both repositories to be audited before proposing or implementing future features or fixes.
- **Evidence:** `git fetch upstream --prune; git fetch original --prune` completed; `git log HEAD..upstream/main --oneline -30` listed the incoming BigStrongSun history; `git log HEAD..original/main --oneline -30` listed the incoming original history; GitHub API inspection reviewed BigStrongSun PR #69 and original PRs #7091, #7093, #6984, #7060, #7085, #7044, #7077, and #7066. No merge or cherry-pick was performed.
- **What NOT to do again:** Do not treat notification dismissal as permission to send, do not merge upstream into a dirty feature batch without auditing every incoming file, and do not use either reference repository as automatic authority without checking ownership and tests.

## 2026-09-03 - Compact/fork action had no visible completion signal

- **What happened:** Request Health launched native compaction and fork after the Windows approval, but the user could not tell whether the background operation completed or failed.
- **Root cause:** The forwarder intentionally detached the long-running CDP job and only wrote completion/failure to logs; the initial approval toast was the only user-facing signal.
- **What we did:** Added a native Windows completion toast naming the new session, plus a failure toast preserving the source session and diagnostic reason.
- **Evidence:** `cargo check --manifest-path src-tauri/Cargo.toml`, `cargo test --manifest-path src-tauri/Cargo.toml --lib request_health` (**13/13**), and `pnpm typecheck` passed.
- **What NOT to do again:** Do not treat detached background work as observable just because it is logged; every user-triggered long-running action needs a completion or failure signal.

## 2026-09-03 - Protocol probe mode selection was present but unreadable in dark theme

- **What happened:** The provider verification confirmation modal rendered Light and Deep controls, but the active mode was difficult to see in dark theme.
- **Root cause:** Both the modal and readiness-section segmented controls used a dark blue `dark:bg-blue-950/70` active background while inheriting a low-contrast ghost-button treatment.
- **What we did:** Kept the controls in the confirmation modal, strengthened the active state to blue with white text in both surfaces, and added a regression assertion for the dark-theme classes.
- **Evidence:** Focused frontend verification passed **66 tests** across Codex form, readiness, and Request Health components; `pnpm typecheck` passed; protocol backend tests passed **75/75** and Request Health backend tests passed **13/13**.
- **What NOT to do again:** Do not treat presence in the DOM as sufficient UI verification; selected-state contrast must be checked explicitly in both light and dark themes.

## 2026-09-03 - Live compact/fork canary exposed and corrected duplicate renderer helper

- **What happened:** After rebuilding the compact/restart changes, the proxy became healthy and new threads defaulted to medium, but the Codex guardian could not inject the renderer compatibility layer.
- **Root cause:** The structured compaction counter had concurrently moved into `codex_compaction_item_core_script`, while the template also declared `const countCompactionItems`; the fully rendered JavaScript therefore failed parsing due to a duplicate lexical declaration. Existing tests checked placeholder rendering and isolated cores but did not parse the complete generated script.
- **What we did:** Removed the duplicate template declaration, kept the structured core implementation, and added a QuickJS syntax regression for the complete generated compatibility script. Hot-injected the exact corrected source into the live renderer, created a disposable persisted thread without an effort override, completed one small turn, ran native manual compaction, forked the compacted state, inspected both thread records, and archived both canary threads.
- **Evidence:** Live source thread `01a066cc-9ae0-7fd0-90d7-48bebe2d438f` reported `reasoningEffort=medium`; fork `01a066cc-fdba-7f61-929a-906dec5926db` also reported `medium`, contained two turns, and the renderer job status was `completed`. The normal turn was HTTP 200 with client/transformed/wire sizes `106963/106467/106467`; manual compaction was HTTP 200 with `60641/60196/60196`. Full Codex Desktop tests passed **43/43**, targeted Prettier and `pnpm typecheck` passed.
- **What NOT to do again:** Do not declare helpers independently in both an injected core and its template, and do not test template substitution without parsing the fully assembled JavaScript.

## 2026-09-03 - Dirty-batch completion audit restored takeover ownership and closed hidden UI contracts

- **What happened:** A final dirty-tree audit found four incomplete behaviors despite focused tests passing: invalid environment variable names could be persisted and silently skipped; native compaction completion could treat arbitrary serialized text as protocol state; hiding one visible model stopped after the first matching routed provider; and the takeover writer could fall through to the ordinary provider writer when the live `auth.json` contained real OAuth material instead of `PROXY_MANAGED`.
- **Root cause:** Validation existed below the settings persistence boundary but was never called. Compaction polling did not constrain its completion signal to exact native item types. Model visibility was implemented as a first-match mutation even though the picker catalog aggregates routes. Most importantly, takeover ownership was inferred from the supplied auth value rather than from the explicit takeover call path, so a later generic login-preservation rewrite could overwrite the already-selected auth facade.
- **What we did:** Validate environment injection before saving and reject invalid/duplicate UI rows; count exact native compaction item types with cycle-safe structured traversal; update every matching routed provider when hiding a visible alias; render authoritative cache/fresh-input diagnostics in Request Health; and make `write_codex_takeover_live_for_provider` unconditionally config-only so it preserves `auth.json` and the chosen facade. Updated stale protocol/reasoning fixtures to exercise the documented read-compatibility and reasoning-fidelity contracts.
- **Evidence:** `pnpm typecheck` passed; full frontend passed **162 files / 1,360 tests**; full Rust library passed **3,692 passed / 6 ignored / 0 failed**; focused Env Injection passed **8/8**, settings save passed **10/10**, Request Health passed **11/11**, Codex Desktop passed **42/42**, protocol selection passed **9/9**, error mapping passed **8/8**, and the two takeover regressions passed **1/1 each**. The exact all-target `cargo test --manifest-path src-tauri/Cargo.toml` was attempted but Windows refused to replace the running normal-target `src-tauri/target/debug/cc-switch.exe` (`Access is denied`, PID `57652`); the process was not terminated.
- **What NOT to do again:** Do not persist settings before validating owned live-file mutations, infer protocol completion from serialized text, stop a global picker mutation after one provider, or decide takeover ownership from the contents of preserved OAuth material. The takeover call path owns config-only writes.

## 2026-09-03 - Compact/restart no longer outlives its CDP command, and new threads default to medium

- **What happened:** Choosing Request Health's compact/new-session action blocked the oversized turn but left Codex disconnected, while fresh MultiRouter sessions started at high reasoning effort.
- **Root cause:** The backend awaited a renderer compaction promise that could run for 90 seconds through a CDP command hard-limited to 4 seconds, so the command timed out before the renderer could report the fork. The current template also referenced a missing `countCompactionItems` helper. Separately, MultiRouter takeover preserved a stale top-level `model_reasoning_effort = "high"` from the live config instead of owning a new-thread default.
- **What we did:** Renderer compaction now runs as a deduplicated background job and exposes short status polls; after one renderer accepts the job, polling failure cannot start a duplicate job elsewhere. The native fork carries a medium effort override, and MultiRouter takeover writes `model_reasoning_effort = "medium"` as the fresh-thread default. Restored structured compaction-item counting for `compaction`, `context_compaction`, and `contextCompaction`.
- **Evidence:** The affected large session showed client bodies of 1,333,515-1,333,826 bytes, transformed/wire bodies of 1,331,258-1,331,443 bytes, and identical retry bodies/hashes; the proxy added no request bytes. The live failure logged `timed out waiting for CDP command Runtime.evaluate`, proving the 4-second/90-second lifetime mismatch. Later 321-339 KB requests received repeated upstream HTTP 502 responses with unchanged client/transformed/wire sizes, an external upstream failure rather than proxy inflation. Codex Desktop tests passed **42/42**, Request Health tests **11/11**, proxy config tests **7/7**; `cargo check`, `pnpm typecheck`, targeted Prettier, and `git diff --check` passed.
- **What NOT to do again:** Do not await long renderer-owned work inside one short CDP command, retry a started compact job through another renderer, infer proxy growth from unchanged replay bodies, or let an old provider snapshot decide the MultiRouter new-thread reasoning default.

## 2026-09-03 - Request Health first-turn approval and native compact/new-session action

- **What happened:** Request Health could only prompt after three sustained-growth samples, so the first oversized Codex request went upstream without a decision; no compact-and-new-session option existed.
- **Root cause:** The committed diagnostic path hard-coded `blocked = false`, while the dirty Windows preflight required `SESSION_REVIEW_RISKS`. Automatic hidden app-server handoff had previously been removed because it did not own the Desktop session lifecycle.
- **What we did:** Added an explicit review mode with `FirstLargeRequest` as the default, preserved `SustainedGrowth` and `Off`, and added a configurable Compact + new session notification action. The action blocks the oversized payload, asks the live Desktop renderer's owned request client to run native compaction, waits for completion, forks the compacted thread, and refreshes the native catalog. No second app-server, rollout-file deletion, or source-thread cleanup is performed.
- **Evidence:** `cargo check`, `pnpm typecheck`, focused Request Health Rust tests **11/11**, focused Codex Desktop script test **1/1**, frontend Request Health/workspace tests **91/91**, Rustfmt, targeted Prettier, and `git diff --check` passed. BigStrongSun `upstream/main` advanced to `dab191353f`; no Request Health or compact/new-session implementation exists there. PR #75 explicitly records oversized-request preflight as unimplemented.
- **What NOT to do again:** Do not restore hidden app-server orchestration or allow a compact action to dispatch the original oversized payload. Keep compaction and forking inside the live Desktop-owned request client and fail closed when the renderer bridge is unavailable.

## 2026-09-03 - Re-audit against Codex 0.153 schema found no safe compatibility removals

- **What happened:** Continued the post-reinstall audit to determine whether the upgraded Codex CLI made existing schema compatibility fixes redundant.
- **Root cause:** The installed PATH CLI is `codex-cli 0.153.0`, while the Desktop-bundled app-server is `0.153.0-alpha.5`; the currently shipped `guardianv2` feature is still explicitly marked `under development`, so its representation remains versioned/unstable. The current app-server model schema also still has meaningful defaults and required fields at the renderer boundary.
- **What we did:** Compared the live CLI feature/model output and generated app-server JSON schema with CCSwitch projections. Confirmed `ModelListResponse` requires `defaultReasoningEffort`, `description`, `displayName`, `hidden`, `id`, `isDefault`, `model`, and `supportedReasoningEfforts`; missing `inputModalities` defaults to `text,image`; `multiAgentVersion`, `serviceTiers`, and `supportsPersonality` remain active fields; `additionalSpeedTiers` is deprecated but still part of the compatibility schema. Reviewed Guardian normalization, shared-cache/catalog repair, MCP shape repair, multi-agent reserved-schema protection, auth ownership alignment, and third-party protocol transforms.
- **Evidence:** `codex --version` returned `codex-cli 0.153.0`; `codex features list` reported `guardianv2 under development false`, `multi_agent_v2 stable true`, `personality stable true`; `codex debug models --bundled` returned current model metadata including `input_modalities`, `multi_agent_version`, and service tiers; `codex app-server generate-json-schema` produced `v2/ModelListResponse.json` with the required/default fields listed above. Post-reinstall runtime evidence in the session summary recorded zero Guardian errors across the reopened thread and app-server logs. No source files were changed by this audit.
- **What NOT to do again:** Do not remove Guardian disk/request-local protections solely because CLI versions now match; do not remove cache/catalog or inline metadata projection because the new app-server still consumes those fields and shared caches can be rewritten by another Codex installation. Only revisit the narrow renderer retry after a rebuilt CCSwitch runtime demonstrates the current Desktop no longer produces the polluted request shape over repeated resume/restart cycles.

## 2026-09-03 - Deferred BigStrongSun features were ported at their owning boundaries

- **What happened:** The ownership-safe CLI environment injection and model hide/restore work were useful, but their upstream commits were based on older settings and MultiRouter UI shapes; the updater audit also found one remaining release helper pointed at the retired `BigStrongSun/ccswitchmulti` repository.
- **Root cause:** A wholesale cherry-pick would overwrite newer fork behavior. Environment injection must survive every Claude/Codex live writer without entering provider snapshots, model visibility must preserve aliases and refreshed metadata, and updater asset URLs must use the same maintained repository as the runtime endpoint.
- **What we did:** Ported the versioned per-key ownership ledger, compare-and-swap reconciliation, rollback/status/retry IPC, localized Settings UI, and live-writer reapplication/strip boundaries. Adapted model hide/restore to the current display/sort/protocol-aware workspace with canonical alias resolution and a tested catalog visibility helper. Kept the current xAI sanitizer because the old whole-float helper no longer exists. Pointed the export helper and Cargo repository metadata at `tngflx/ccswitchmultirouter`.
- **Evidence:** `cargo check` passed; environment injection Rust tests passed **8/8**; settings command tests passed **10/10**; xAI sanitizer tests passed **13/13**; `pnpm typecheck` passed; environment/settings/updater frontend tests passed **23/23**; the full Codex workspace component file passed **89/89**; updater Pester tests passed **20/20**; Rustfmt, targeted Prettier, and `git diff --check` passed. Existing React `act(...)` warnings remained in two Request Health tests.
- **What NOT to do again:** Do not infer ownership from equal environment values, persist injected values into provider records, resurrect hidden models during catalog refresh, apply the old xAI integer patch after its producer was removed, or generate updater URLs from a repository different from the configured runtime endpoint.

## 2026-09-03 - Live rebuild confirmed Guardian isolation and startup ordering

- **What happened:** The earlier Guardian incident entry initially stopped at source-level verification because the running binary predated the fix. The delayed development launcher rebuilt during the same investigation.
- **Root cause:** The renderer regression and startup-order race were real; the recurring proxy interruption was additionally explained by `pnpm dev`'s intentional delayed backend replacement. After a one-hour quiet period following backend edits, it force-replaces the development process tree, which removes the listener briefly and leaves the old run marker classified as `UncleanExit`.
- **What we did:** Verified the rebuilt executable was newer than the changed renderer template and startup code. Confirmed from logs that the new process restored Codex takeover at `08:43:27` before launching Codex Desktop at `08:43:48`. Performed one read-only CDP inspection of the available renderer.
- **Evidence:** The local `/health` endpoint returned healthy with takeover on port `15721`; bundled Codex `0.152.1` still parsed `guardianv2 = false`. CDP reported `patchPresent=true`, Statsig patch version `2`, and `legacyStatsigWrappers=false`. The inspected avatar-overlay renderer had not discovered a request client, so the exact request-fallback wrapper remains covered by QuickJS tests rather than live thread/resume evidence. The user must reopen the affected thread to complete that acceptance check.
- **What NOT to do again:** Do not describe normal delayed development rebuilds as an in-process proxy detach. Use `pnpm dev -- --no-rebuild` while the development binary is serving as an active long-lived proxy, and do not claim live thread/resume acceptance until the affected screen is retried.

## 2026-09-03 - Guardian v2 recurrence was produced by renderer Statsig pollution

- **What happened:** Codex Desktop again rejected new/resumed threads with `FeatureToml in features.guardianv2` even though the live file had `guardianv2 = false`; repeated CCSwitch development restarts also created proxy listener handoff failures.
- **Root cause:** The current renderer template had regressed to the pre-`0aa10552` behavior and applied model-whitelist fields to every Statsig dynamic config, including Guardian config `2553103476`. The app-server rejected that request-local object while the stable disk TOML remained valid. Automatic Desktop launch also still occurred before takeover recovery, allowing a stale runtime snapshot. The separate proxy interruption was confirmed as process overlap on fixed port `15721`, not request-body inflation.
- **What we did:** Ported BigStrongSun's renderer fix into the template architecture: scope model mutation to `107580212`, scrub only legacy model fields from Guardian, retry once only for the exact Guardian `FeatureToml` error, and bump the live wrapper version. Moved automatic Codex launch after successful takeover recovery and Guardian enforcement. Full investigation: [incident report](incidents/2026-09-03-guardianv2-renderer-proxy-lifecycle.md).
- **Evidence:** Live Desktop-bundled Codex `0.152.1` parsed the scalar file and reported Guardian disabled; September 3 logs show the `07:53:06` port bind failure and `07:59:17` recovery. Guardian tests passed **7/7**; all `codex_desktop` tests passed **40/40**; `cargo check`, `cargo fmt --check`, and scoped `git diff --check` passed. The rebuilt renderer path is not runtime-verified because the active development binary predates the source change.
- **What NOT to do again:** Do not keep rewriting disk TOML when the invalid value is produced in renderer request overrides, do not patch every Statsig config as if it were the model catalog, and do not launch Desktop before proxy takeover recovery completes.

## 2026-09-03 - Request Health control is a fail-closed Windows notification preflight

- **What happened:** The first cache-health implementation raised an in-app toast only after a large request had already completed upstream, so it could warn but could not control dispatch. The required control surface is the Windows notification itself, never a renderer toast.
- **Root cause:** Authoritative cache usage exists only after a response, while safe control must happen before the next finalized request reaches `upstream_send`. The renderer event path was therefore both too late and the wrong ownership boundary.
- **What we did:** Sustained uncached growth now arms the affected session for its next non-compaction Codex request. After provider transformation and optional wire compression, but before any send or reconnect factory can dispatch, the proxy freezes the wire bytes and waits on a backend one-shot resolved only by native WinRT `Continue once` or `Block` actions. Approval is bound to a single-use random token, client request trace, session, SHA-256 wire-body hash, and hashed provider/method/URL dispatch scope. Exact already-approved transport retries are exempt; changed bodies, targets, sessions, or traces are not. Dismissal, timeout, notification setup/show failure, client-future cancellation, and unknown activation all fail closed. The renderer Request Health toast was removed.
- **Evidence:** `cargo check --manifest-path src-tauri/Cargo.toml` passed; focused Request Health tests passed **8/8**; focused proxy error-mapping tests passed **8/8**; `pnpm typecheck` passed; focused Request Health/workspace frontend tests passed **89/89**; Rustfmt, targeted Prettier, and `git diff --check` passed. The live WinRT display/activation path was not tested because the running development binary predates this source and runtime proof requires a normal rebuild plus a deliberately armed session.
- **What NOT to do again:** Do not put request-release authority in a renderer toast, infer current-request control from post-response usage, release an unbound or reusable approval, treat notification failure as permission to send, or claim native activation is runtime-verified without testing the rebuilt binary.

## 2026-09-03 - Selective BigStrongSun and original cc-switch integration audit

- **What happened:** Refreshed BigStrongSun main and open PRs plus original cc-switch main, then audited the new commits against this fork's recent squashed integrations and active dirty work.
- **Root cause:** Patch ancestry overstated the useful delta: several upstream fixes already existed under local SHAs, while other candidates depended on older probe, routing, or UI ownership models. The primary checkout also gained concurrent uncommitted changes in `runner.rs` and related backend files during the audit.
- **What we did:** Built an isolated integration branch with BigStrongSun's Sub-Agent V2 alias rekey and terminal Responses tool-item fix, plus original cc-switch's Hermes release detection and stable resumed-rollout usage identity. Deferred the environment-injection, model hide/restore, and xAI series to dedicated adaptations. Full verdicts are in [the incident report](incidents/2026-09-03-upstream-cherry-pick-audit.md).
- **Evidence:** `cargo check` and `pnpm typecheck` passed; focused tests passed 8/8, 1/1, 3/3, 1/1, and 1/1. Final frontend was **160 files passed, 1 failed; 1,352 passed, 1 failed** on an unchanged baseline placeholder test. Final backend was **3,670 passed, 4 failed, 6 ignored** on unchanged baseline tests outside the integrated files.
- **What NOT to do again:** Do not equate remote ahead counts with missing behavior, resolve PR #75 or the xAI series by taking a whole side, or land the isolated branch over the moving `runner.rs` checkout without explicit coordination.

## 2026-09-02 - Request Health now distinguishes cached history from sustained fresh-input growth

- **What happened:** Request Health could only compare finalized JSON bytes with a `bytes / 4` estimate. It warned about every large request but could not tell whether a long Codex history was receiving useful upstream cache hits or repeatedly charging fresh input.
- **Root cause:** The proxy already parsed `input_tokens` and `cache_read_tokens` from upstream responses, but that authoritative usage was never correlated back to the request-health diagnostic. The UI therefore implied a token ceiling without the data required to make a cache-health judgement.
- **What we did:** Added one per-request correlation ID shared between request preparation and response processing; Request Health now records actual, cached, and fresh input tokens plus cache-hit ratio. After three same-session large Codex turns, it emits a non-blocking alert only when fresh input rises by at least 5% per turn and cache hits stay below 10%. The renderer shows a persistent in-app toast; no proxy-generated compaction, stop, retry, fork, or Windows action prompt was added.
- **Evidence:** `cargo check --manifest-path src-tauri/Cargo.toml` passed; focused `proxy::request_health::tests` passed **6/6**, including the new warm-up/growth regression; `pnpm typecheck` passed; `pnpm vitest run tests/components/RequestHealthPanel.test.tsx` passed **2/2**; `git diff --check` passed.
- **What NOT to do again:** Do not infer cache effectiveness from request bytes, turn a post-dispatch signal into a request blocker, or offer an interactive continue/block decision after an upstream request has already begun.

## 2026-09-03 - Protocol selection must score verified reasoning fidelity before native-format preference

- **What happened:** The Codex protocol probe recorded a reasoning semantic for each successful Chat and Responses branch, but equal-capability branches always selected native Responses.
- **Root cause:** `select_transport_outcome_with_reasoning` discarded the `ReasoningSemantic` value when building its comparison key, contradicting the documented selection order: continuation, forced tool, streaming, readable reasoning fidelity, then native Responses.
- **What we did:** Added a fidelity rank (`Readable > Summary > Opaque/None`) before the native-Responses tie-break and corrected the two stale regression expectations so equally capable Chat is selected when it has readable reasoning and Responses has only summary or opaque reasoning. Updated the preset-selector placeholder test to exercise the actual no-icon path; valid provider URLs now intentionally infer a favicon.
- **Evidence:** `cargo test --manifest-path src-tauri/Cargo.toml protocol_compatibility::selection_tests --lib` passed **9/9**; `pnpm exec vitest run tests/components/CodexFormFields.test.tsx --maxWorkers=1 --minWorkers=1` passed **55/55**; `pnpm test:unit` passed **161 files / 1,350 tests**; `pnpm typecheck` passed before later concurrent backend edits. A later full `cargo test` could not replace the live `src-tauri/target/debug/cc-switch.exe` (`Access is denied`), and later `cargo check` is blocked by concurrent `RequestContext` changes missing `request_health_trace` at `handler_context.rs:230`.
- **What NOT to do again:** Do not let a native Responses preference override verified readable reasoning when continuation, tool, and SSE behavior are otherwise tied. Do not treat a valid inferred favicon as an absent icon in UI tests.

## 2026-09-02 - Guardian v2 recurrence came from an unpinned Desktop/app-server schema boundary

- **What happened:** The `FeatureToml in features.guardianv2` resume failure returned after the earlier backup/write-boundary fixes, often only after Codex Desktop had been open for a while.
- **Root cause:** The earlier fix normalized a structured Guardian v2 value only when CCSwitchMulti happened to write `config.toml`. When the key was absent, Desktop package `26.831.1445.0` could later initialize the under-development feature as a structured table, while the installed `codex-cli 0.150.1` app-server expected the shared feature value to remain a scalar. Recovery replay amplified the problem but was not the original producer.
- **What we did:** Added a lifecycle compatibility invariant that pins `features.guardianv2 = false` when missing and repairs later structured rewrites through the existing locked atomic reconcile path. The Codex guardian checks that invariant while takeover is active. The official disable command confirmed the effective default but removed the explicit false value, so the current live installation was pinned through the same new atomic reconcile boundary.
- **Evidence:** `codex features list` identifies `guardianv2` as `under development` and reports `false`; the official disable command left the key absent, proving an effective default was not a durable schema pin; the one-off live reconcile wrote the explicit scalar without stopping Codex or CCSwitchMulti. Focused Guardian tests passed **4/4**, including missing-key pinning, simulated Desktop structured rewrite, backup recovery, and idempotence; Codex guardian tests passed **3/3**; `cargo check --manifest-path src-tauri/Cargo.toml` passed; scoped `git diff --check` passed.
- **What NOT to do again:** Do not treat recovery normalization as sufficient when an external process can recreate an incompatible schema after startup. Keep the compatibility scalar present for the full Desktop lifecycle, and do not repair the live Codex file through a raw writer.

## 2026-09-02 - Request inflation re-audit found no remaining proxy-side multiplication

- **What happened:** A second audit reviewed the live router trace and every request retry, Responses-to-Chat history, Lite fallback, and upstream serialization boundary for request inflation.
- **Root cause:** The observed growth is already present in Codex's client payload across turns; it is not being multiplied by the current CCSwitch proxy path. The earlier owned duplication risk in cached Responses-to-Chat exchange restoration is guarded by requiring context-free tool-output shorthand, and retries reuse the finalized request bytes.
- **What we did:** Added a mandatory four-point request-size accounting guideline to `AGENTS.md`: compare client, transformed, retry/replay, and wire payloads; prove ownership before changing behavior; require immutable retry payloads and idempotent history restoration.
- **Evidence:** Recent `codex-router.log` events on September 2, 2026 show transformed/upstream bodies smaller than client bodies by 466–682 bytes, including 400–700 KB requests; no current `oversized_compaction` scheduler exists; the focused history regression passes **10/10** (verification rerun for this audit is recorded in the task report).
- **What NOT to do again:** Do not treat monotonically growing client-provided history as proof of proxy inflation, and do not add truncation, retry suppression, or arbitrary caps without identifying the owning producer and testing that boundary.

## 2026-09-02 - Oversized compaction loop root cause confirmed and history replay bounded

- **What happened:** The September 2 live log showed an oversized Sublyx Codex turn reaching 1,001,804 bytes, followed by CCSwitch scheduling `oversized_compaction` and submitting a second 958,366-byte compaction request. A separate audit found the Responses-to-Chat history bridge could prepend a cached exchange even when the client had already supplied context.
- **Root cause:** Request Health treated a byte heuristic as permission to create a native compaction turn, but it did not own a continuation boundary; the resulting compaction request re-entered the same oversized path. Independently, `CodexChatHistoryStore::enrich_request` replayed cached semantic history whenever no tool output was present, including ordinary context-bearing requests, allowing duplicate history growth for Chat-compatible providers.
- **What we did:** Removed the oversized auto-compaction scheduler and its Desktop submission path (the existing bounded stream-recovery continuation remains). Restricted cached exchange restoration to context-free tool-output shorthand and added a regression proving a client-supplied next message is not prefixed with the cached exchange.
- **Evidence:** Historical router log lines at `09:45:59` show `oversized_compaction status=scheduled`, `status=submitted`, then `codex_request_kind=compaction` with `request_bytes=958366`; current source contains no `oversized_compaction` scheduling symbol. `cargo test --manifest-path src-tauri/Cargo.toml --lib proxy::providers::codex_chat_history::tests -- --nocapture` passed **10/10**.
- **What NOT to do again:** Do not let a diagnostic byte threshold create a compaction or continuation request. Do not restore cached semantic exchanges when the client already supplied context; only repair the minimal context-free tool-output form.

## 2026-09-02 - Force-repair rollback now normalizes Guardian v2

- **What happened:** A re-audit after upgrading Codex CLI to `0.150.1` found that force-repair failure rollback could still write the original Codex `config.toml` through the raw text writer.
- **Root cause:** The rollback closure in `services/provider/mod.rs` restored `original_live` directly, bypassing the shared `features.guardianv2` normalizer. A structured Guardian v2 table in that snapshot could therefore be reintroduced after a failed repair.
- **What we did:** Routed that rollback through `codex_config::write_codex_live_config_atomic`, matching the normal switch, proxy recovery, MCP, and `LiveSnapshot` write boundaries.
- **Evidence:** `codex --version` reported `codex-cli 0.150.1`; current `~/.codex/config.toml` has no `guardianv2` entry; current CCSwitch log has `0` `guardianv2`/`FeatureToml`/normalization matches; focused Guardian tests passed **3/3**, force-repair rollback passed **1/1**, Codex MCP tests passed **13/13**, provider live/snapshot tests passed **32/32**, `cargo check --manifest-path src-tauri/Cargo.toml` passed, and scoped `git diff --check` passed.
- **What NOT to do again:** Do not restore any Codex live `config.toml` snapshot with `write_text_file`; rollback paths must use the central normalizing atomic writer.

## 2026-09-02 - Guardian v2 schema normalization at every Codex live-write boundary

- **What happened:** Codex intermittently refused to resume with `data did not match any variant of untagged enum FeatureToml in features.guardianv2`.
- **Root cause:** CCSwitch restarted frequently and restored `proxy_live_backup` snapshots that could contain a structured `features.guardianv2` table, while Codex expects the feature value as a boolean. Several Codex config writers bypassed the shared validator, so a bad table could be replayed during crash recovery, MCP synchronization, snapshot rollback, or provider switching.
- **What we did:** Normalize any non-boolean `features.guardianv2` to `false` through the central Codex TOML writer, and route proxy recovery, Codex MCP writes, and `LiveSnapshot` restoration through that writer. Added regressions for direct writes, backup recovery, structured/table forms, and boolean pass-through.
- **Evidence:** Focused Guardian Rust tests passed **3/3**; Codex MCP tests passed **13/13**; Codex snapshot-restore tests passed **3/3**; `cargo check --manifest-path src-tauri/Cargo.toml` passed; `git diff --check` passed. Final `pnpm typecheck` failed in concurrent `src/config/codexProviderPresets.ts:1802` (`apiFormat` not in the current model type). Final `pnpm test:unit` completed **159 passed, 2 failed files; 1,348 passed, 2 failed tests**, both in concurrently edited provider preset/UI tests. Full `cargo test` could not replace the live normal-target `src-tauri/target/debug/cc-switch.exe` (`Access is denied`); the process was not terminated.
- **What NOT to do again:** Do not restore or rewrite Codex `config.toml` through a raw file writer; every live-config mutation must pass the shared normalizer and atomic write lock.

## 2026-09-02 - OpenRouter model protocol groups and mandatory reasoning

- **What happened:** Codex `/responses` requests for OpenRouter could still fail during Responses-to-Chat conversion with HTTP 400 `Reasoning is mandatory for this endpoint and cannot be disabled`, even after the provider form assigned protocol groups.
- **Root cause:** Request-local schema-v2 model protocol assignments were vulnerable to a later detected-transport reconciliation overwrite. Separately, the shared OpenRouter reasoning inference translated Codex `reasoning.effort=none` into `reasoning.effort=none` for every OpenRouter model, including models whose upstream metadata requires reasoning.
- **What we did:** Treat `codexResolvedApiFormat` on a resolved request provider as authoritative and skip stale probe transport replacement. Gate OpenRouter's outbound `reasoning:{effort:"none"}` behind an explicit disable contract; inferred platform behavior now omits the invalid disable signal while explicit user configuration remains able to send it. Updated focused UI tests for the relocated Light/Deep selector and populated model readiness state.
- **Evidence:** `cargo test --manifest-path src-tauri/Cargo.toml --lib proxy::providers::transform_codex_chat::tests::responses_request_to_chat_` passed **90/90**; the explicit v2 protocol-group transport regression passed **1/1**; focused frontend tests passed **62/62**; `pnpm typecheck` passed; `git diff --check` passed; live `codex exec --ephemeral --model openrouter/free` returned exactly `OPENROUTER_SMOKE_OK`; router log trace `b79db797-a24d-499a-8d1e-df21baa769ec` recorded `/responses` → `/chat/completions`, `responses_to_chat=true`, upstream HTTP 200.
- **What NOT to do again:** Do not let probe reconciliation override an already compiled per-model protocol group, and do not emit a generic reasoning-disable field to OpenRouter models without verified disable support.

## 2026-09-01 - Request Health no longer owns Codex session lifecycle

- **What happened:** An oversized Sublyx `/responses` request caused CCSwitchMulti to schedule hidden background compaction handoffs. The helper attempted to start a separate `codex app-server`, compact the source thread, fork a continuation, start a handoff turn, then archive and delete the old thread. No compaction appeared in Codex UI because all coordination notifications were consumed by the hidden helper. The helper also failed repeatedly because the installed CLI path was not discovered.
- **Root cause:** Request Health combined diagnostics with destructive session orchestration. A 384 KB request-size threshold was treated as a compaction recommendation, the auto-handoff setting defaulted on, and the coordinator checked a Desktop-owned thread through a separate app-server process. The installed Codex CLI now exposes `app-server daemon/proxy` rather than the old standalone `app-server --stdio` shape.
- **What we did:** Removed automatic scheduling from request forwarding; removed the compact/fork/start-turn Tauri command and UI controls; made Request Health diagnostic-only so it never blocks, compacts, forks, starts turns, archives, or deletes sessions; recommend native compaction only when the configured token ceiling is exceeded; preserved native Responses compaction routing; retained app-server deletion only for Session Manager and expanded executable discovery for installed Codex locations.
- **Evidence:** `cargo check --manifest-path src-tauri/Cargo.toml` passed; `cargo test --manifest-path src-tauri/Cargo.toml --lib proxy::request_health::tests` passed **5/5**; `cargo test --manifest-path src-tauri/Cargo.toml --lib codex_session_coordination::tests` passed **1/1**; compaction-related Rust tests passed **19/19**; focused frontend/IPC tests passed **90/90**; `pnpm typecheck` passed; `git diff --check` passed; a live 1.46 MB request was sent upstream and returned HTTP 200; zero `RequestHealthHandoff` events were recorded after the rebuilt process started at **15:32:14 on September 1, 2026**.
- **What NOT to do again:** Do not let a provider-neutral diagnostic layer create or delete Codex sessions, do not treat byte-size warnings as proof that compaction should happen, do not consume compaction events in a hidden helper while presenting the feature as native Codex behavior, and do not rely on stale `app-server --stdio` assumptions without checking the installed CLI contract.

## 2026-09-01 - Automatic compaction must not be preempted by Request Health

- **What happened:** A Sublyx Codex `/responses` turn was rejected locally at an estimated 251,407 tokens even though Request Health was configured with `autoCompactAndForkCodex=true`. The user never saw native compaction or the configured summarize-and-passover continuation.
- **Root cause:** Request Health scheduled the asynchronous handoff but still enforced its repeated-oversized-turn blocker. That blocker could reject the next turn before Codex emitted or completed its native compaction request; the background coordinator cannot recover a turn that was already rejected.
- **What we did:** Hard blocking now applies only when automatic compaction/handoff is explicitly disabled. With automatic recovery enabled, Request Health remains diagnostic and schedules coordination, while Codex is allowed to own the compaction boundary and continue the request path.
- **Evidence:** Added regression coverage for an oversized second turn with automatic compaction enabled; targeted Cargo test was attempted but could not replace the live normal-target `src-tauri/target/debug/cc-switch.exe` (`Access is denied`), so the new test has not executed in this environment.
- **What NOT to do again:** Do not combine an asynchronous compaction coordinator with a synchronous preflight rejection that prevents Codex from reaching its own compaction protocol.

## 2026-09-01 - Session deletion now uses one explicit, recoverable workflow

- **What happened:** The Session Manager exposed a destructive archive trash action beside ordinary session controls, while Codex deletion attempted app-server `thread/delete` before removing the local rollout file. A failed or partial RPC could leave the Codex catalog and filesystem inconsistent, making other history appear broken.
- **Root cause:** Deletion had two competing UI/API paths and treated app-server cleanup as a prerequisite even though the rollout file is the recoverable conversation source.
- **What we did:** Codex deletion now validates and removes the rollout first, then performs best-effort app-server cleanup with a warning on RPC failure. The archive trash control no longer deletes immediately; it enters the existing batch-selection mode with archived Codex sessions preselected, so one confirmation and one selection model cover individual, mixed, and archived deletions. Added the `selectArchivedTooltip` key to all four locales and updated the focused UI test.
- **Evidence:** `pnpm vitest run tests/components/SessionManagerPage.test.tsx` passed **17/17**; `cargo test --manifest-path src-tauri/Cargo.toml session_manager::tests --lib` passed **6/6**; `cargo check --manifest-path src-tauri/Cargo.toml`, `pnpm typecheck`, Prettier checks, and scoped `git diff --check` passed.
- **What NOT to do again:** Do not expose “delete all” beside ordinary session actions without entering explicit selection/confirmation, and do not delete an app-server thread before the local rollout has been safely removed or backed up.

## 2026-09-01 - Native compaction now carries a structured handoff envelope

- **What happened:** Native Codex compaction and fork continuation preserved Codex's internal context, but the continuation turn only received a generic “continue” instruction. That made the rollover less auditable than a manual summarize-and-continue handoff.
- **Root cause:** The coordinator had access to secret-free Request Health metadata but did not pass it across the `turn/start` boundary.
- **What we did:** Added a latest-diagnostic lookup by session and generated a structured handoff prompt containing session, provider/model, endpoint, token estimate, byte counts, structural findings, and largest request categories. Raw prompt text and tool content remain excluded. The coordinator injects this prompt into the continuation turn and falls back to the generic instruction when no diagnostic exists.
- **Evidence:** `cargo test --manifest-path src-tauri/Cargo.toml codex_session_coordination::tests --lib` passed **9/9** with **1** live-only ignored test; `cargo test --manifest-path src-tauri/Cargo.toml request_health::tests --lib` passed **6/6**; `pnpm typecheck` passed; `cargo fmt --manifest-path src-tauri/Cargo.toml --all -- --check` passed after formatting.
- **What NOT to do again:** Do not rely on an opaque generic continuation prompt when structured, already-redacted handoff metadata is available; do not include prompt or tool content in rollover diagnostics.

## 2026-09-01 - Same-trace provider retries no longer trip the oversized-turn gate

- **What happened:** Request Health could treat a provider retry of one logical Codex turn as a new turn and block it before the retry reached the provider.
- **Root cause:** Oversized history was keyed only by session ID; every forwarding attempt updated the session sample, so transport retries were indistinguishable from a later user turn.
- **What we did:** Stored the last logical request's trace ID with its token estimate. Repeated inspections with the same trace are ignored for shrink evaluation, while a new trace in the same session is compared against the previous turn. Added regression coverage for first-turn allowance, same-trace retry allowance, non-shrinking second-turn blocking, and reduced-turn allowance.
- **Evidence:** `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` passed; `cargo test --manifest-path src-tauri/Cargo.toml --lib proxy::request_health::tests` passed **6/6**; `cargo test --manifest-path src-tauri/Cargo.toml --lib codex_session_coordination::tests` passed **8/8** with **1** live-only ignored test; `pnpm vitest run tests/components/RequestHealthPanel.test.tsx` passed **2/2**; `pnpm typecheck` passed. The normal `cc-switch.exe` process remained running and was not terminated.
- **What NOT to do again:** Do not use session-only token history for a gate that runs per provider attempt, and do not infer a new user turn from a retry without a new trace/request identity.

## 2026-09-01 - Large requests required explicit handoff, and Subagents diagnostics multiplied full-plan work

- **What happened:** Request Health correctly reported Codex requests above 300K input tokens, but some requests remained roughly 1.43 MB and later cache misses made almost the entire replay fresh input. Opening the Subagents tab with the live 481-profile MultiRouter configuration could also hang the application.
- **Root cause:** The oversized sessions contained hundreds of semantic replay items, including one 248,868-byte user message. Safe optimization removed only known transport metadata, so diagnostics repeatedly reported `threshold_exceeded=true`, `compaction_recommended=true`, and `bytes_removed=0`; automatic compact-and-fork was disabled in `settings.json`, leaving detection without rollover. Separately, the Subagents tab launched one full-settings preview IPC per profile: 481 concurrent calls each resent and recompiled the 122,452-byte settings object, for roughly 59 MB of IPC payload and about 231,000 profile compilation units, while React rendered all 481 editors.
- **What we did:** Kept Request Health's conservative semantic-history boundary, added a visible warning when an oversized request removes zero bytes while automatic handoff is disabled, and covered that state directly. Subagents now debounces/coalesces diagnostics, computes authoritative statuses once, previews only the open profile after status resolution, excludes profile-only data from catalog capability refreshes, and renders profiles in bounded pages of 40. Automatic generated override text is a placeholder rather than controlled draft data, preventing delayed previews from contaminating user typing.
- **Evidence:** The screenshot's `00:34:31` row matched session `01a0585a-73fe-7c93-b47d-aaf336381625`: 317,129 fresh + 18,688 cached = 335,817 input tokens and a roughly 1.43 MB proxy body. Session `01a058af-e915-7162-b447-72529304fa05` contained the 248,868-byte user message corresponding to the 248,581-byte largest request item. Focused Subagents passed **130/130**; Request Health plus Settings passed **11/11**; backend Request Health passed **4/4**; `pnpm typecheck`, targeted Prettier, and scoped `git diff --check` passed. Windows recorded Codex `MoAppHang` events and CCSwitch recorded an unclean restart near the incident, but no direct `cc-switch.exe` crash event was found.
- **What NOT to do again:** Do not describe Request Health detection or metadata cleanup as semantic context compaction, leave automatic handoff disabled while expecting request size to shrink, issue one full-plan preview per profile, render an unbounded profile catalog, or place late automatic preview values directly into editable override state.

## 2026-08-31 - Oversized Codex rollover now cleans the source only after continuation success

- **What happened:** Oversized-request handoff could compact, fork, and start a continuation, but the completed source thread remained behind. Archived Codex sessions were also deleted by removing only rollout JSONL files, leaving app-server/SQLite records able to survive. Separately, the live Codex picker could receive reasoning metadata under different aliases or model-ID casing, and Request Health was easy to miss inside the Status subview.
- **Root cause:** Source cleanup was outside the handoff completion transaction; Session Manager bypassed Codex's authoritative `thread/delete`; the renderer patch assumed already-normalized reasoning aliases and case-sensitive identities; and Request Health had no direct action from the default link-status panel.
- **What we did:** After the continuation handoff turn reaches `completed`, the coordinator archives and permanently deletes the old source thread; failed/interrupted continuations never clean the source. Session Manager now uses app-server deletion with a validated local fallback and offers an exhaustive “delete all archived” action that continues after per-thread errors, including malformed rollout files. The picker projection and JavaScript patch normalize all supported reasoning aliases, match model identities case-insensitively, preserve established ordering, and report how many injected models expose effort choices. Status now has a direct Request Health action. The existing MultiRouter create/open/edit chooser was explicitly preserved.
- **Evidence:** Current Codex app-server `model/list` returned correct non-Sublyx effort metadata for Zmhub and OpenCode models. Focused Rust passed **34/34** picker tests, **8/8** coordination tests with **1** live-only ignore, and **72/72** Session Manager tests. Focused frontend passed **118/118** tests; `cargo check`, `pnpm typecheck`, `node --check` for the picker core, targeted formatting, and scoped `git diff --check` passed. Final frontend on the moving shared tree completed **158 files: 154 passed, 4 failed; 1327 passed, 8 failed**; an immediate rerun of the known failing areas produced **2 failed files / 5 failed tests**, confined to leased ProviderList label assertions and Sub-Agent V2 exact-payload/call-order assertions. Final backend was attempted but could not replace the running normal-target `src-tauri/target/debug/cc-switch.exe` (`Access is denied`); the process was not terminated.
- **What NOT to do again:** Do not delete a source thread before the continuation turn completes, do not treat rollout-file removal as authoritative Codex deletion, do not assume one renderer reasoning schema or exact ID casing, and do not overwrite the MultiRouter chooser when the requested change concerns diagnostics or picker behavior.

## 2026-08-31 - Unversioned Codex model caches now use the installed CLI version

- **What happened:** Startup skipped CCSwitchMulti's hot model-picker cache projection when `models_cache.json` existed without a top-level `client_version`, leaving custom MultiRouter models dependent on slower catalog reload paths and emitting a warning on every activation.
- **Root cause:** Cache synchronization treated the optional/missing version field as a hard prerequisite even though the same code already discovers and executes the installed Codex CLI for bundled model metadata.
- **What we did:** Preserve an existing cache version when available; otherwise discover and cache the installed Codex CLI version through the existing candidate list and use it for the CCSwitch-owned cache. If neither source exists, synchronization now degrades at debug level without claiming an application warning. Also refreshed Browserslist data, removed a stale user-level `openai-bundled` marketplace registration that conflicted with Codex's managed source, and shortened the cached `ngs-analysis` default prompt from 313 to 111 characters.
- **Evidence:** The focused cache-version regression passed **1/1**; `cargo check --manifest-path src-tauri/Cargo.toml` passed with one unrelated dead-code warning; `git diff --check -- src-tauri/src/codex_config.rs pnpm-lock.yaml` passed; `codex features list`, `codex plugin marketplace list`, and `codex plugin list` passed; `pnpm exec browserslist` ran without the stale-data warning; the NGS manifest parsed as JSON with one 111-character default prompt. `pnpm typecheck` failed on concurrent `String.replaceAll` usage in `src/components/sessions/SessionManagerPage.tsx:88`. The final frontend suite completed **158 files: 154 passed, 4 failed; 1327 tests passed, 8 failed**, all in concurrently edited MultiRouter wizard, ProviderList, and Sub-Agent V2 tests. The final backend suite was attempted but could not replace the running normal-target `src-tauri/target/debug/cc-switch.exe` (`Access is denied`); the process was not terminated.
- **What NOT to do again:** Do not require legacy cache metadata when the matching installed Codex version can be discovered, do not register the app-managed bundled marketplace persistently in user config, and do not treat warnings from concurrently changing files as verification of this cache path.

## 2026-08-31 - Dev rebuilds restore verified forced termination for proxy takeover

- **What happened:** The graceful-first launcher path correlated with stale proxy ownership and takeover failures during live development restarts. The requested behavior is to force-terminate the verified app tree on every rebuild.
- **Root cause:** Authenticated self-shutdown could leave the listener/process handover path dependent on terminal delivery and close-to-tray behavior. The replacement could start while the old generation or socket ownership was still observable.
- **What we did:** `stopVerifiedWindowsApp` now immediately runs `taskkill /PID <verified app PID> /T /F`, waits for the verified process to disappear, and then waits for every captured listener to become reusable. Removed the development shutdown token, marker file, environment variables, Rust marker consumer/background monitor, window-close hook, and dead `DevLauncherRestart` exit reason. PID reuse, executable identity, target-directory validation, and unknown-process refusal remain enforced. Launcher tests assert forced termination as the normal path.
- **Evidence:** `node --check scripts/dev-tauri-delayed.mjs` passed; launcher tests passed **40/40**; targeted Prettier and scoped Rustfmt passed; the exit-reason regression passed **1/1**; `cargo check --manifest-path src-tauri/Cargo.toml` passed; `git diff --check` passed. A symbol scan found no remaining development graceful-shutdown machinery. Live `/status` reported listener role `takeover`, PID `16032`, port `15721`, and `current_provider_id=codex-multirouter`, although request health was degraded by provider circuit-breaker state (`23` successes, `18` failures).
- **What NOT to do again:** Do not restore development shutdown tokens, polling monitors, authenticated close requests, or graceful-first rebuild behavior. Do not broaden forced termination beyond the fully verified CCSwitch app tree.

## 2026-08-31 - Live graceful-rebuild attempt was inconclusive because the supervisor generation changed

- **What happened:** A user-authorized live rebuild attempt was made against the running development session. The trigger was a reversible `rust-toolchain.toml` content edit, with a hidden observer intended to record the old and replacement app identities.
- **Root cause:** The handoff PID (`5360`) was stale by the time the trigger ran. Before the trigger, the ledger already showed `5360 -> 29096 -> 16032` at `21:42:57`, `21:54:43`, and `21:55:14` Singapore time. The supervisor also changed from PID `56576` to PID `14648` at `21:55:10`. The observer therefore treated an existing replacement as the test result, and the trigger overlapped an externally restarted supervisor generation.
- **What we did:** Restored `rust-toolchain.toml` byte-for-byte after the observed rotation and removed the temporary work lease. No running process was manually terminated.
- **Evidence:** `app-exit-events.jsonl` recorded `abnormal_exit_detected` for PID `29096` with previous PID `5360`, then PID `16032` with previous PID `29096`; no new `dev_launcher_restart` event was recorded. Current process ownership is `node 14648 -> cargo 29372 -> cc-switch 16032`. The original file SHA-256 was restored to `0ABE45038B7A3234EB7D050AC4DF8C0683CA44396DAEADD9A61CF12DBD5D5DF3`.
- **What NOT to do again:** Do not use a stale handoff PID as the live-test baseline, and do not call a PID change proof when the supervisor generation changed before the trigger. Capture the current supervisor/app identities immediately before triggering and abort the proof if either generation changes unexpectedly.

## 2026-08-31 - Dev entry points now share one lifecycle and preserve exit/lease identity

- **What happened:** A full development-script audit found five conflicting behaviors: the debug app recorded `dev_launcher_restart` before cleanup and then recorded `user_requested_exit` again; `dev:immediate` bypassed the verified supervisor; the launcher explicitly accepted alternate Cargo target directories; `rust-toolchain.toml` changes were absent from the backend content baseline; and PowerShell 7 JSON date conversion caused an active guardian maintenance lease to be overwritten.
- **Root cause:** Exit intent was recorded at the request boundary instead of carried through the shared cleanup boundary. Development modes owned separate restart implementations. Process ownership trusted configurable output roots. The watcher modeled only `src-tauri` inputs. Guardian timestamp validation cast typed `DateTime` values to culture-formatted strings, dropping fractional seconds from process start identity.
- **What we did:** Added one pending exit-reason state and record a single clean exit only after shared cleanup completes. Routed `dev:immediate` through the same supervisor with `--delay 0`. Development now rejects `CARGO_TARGET_DIR` and any Cargo configuration resolving outside `src-tauri/target`. Added the repository `rust-toolchain.toml` to both the content snapshot and live watcher. Guardian UTC conversion now preserves `DateTime`/`DateTimeOffset` precision, with a sub-second regression test.
- **Evidence:** Launcher tests passed **40/40**; guardian Pester tests first exposed the active-lease failure at **16 passed / 1 failed**, then passed **18/18** after the fix. Exit-reason and authenticated-marker Rust tests passed **1/1 each**; `cargo check --manifest-path src-tauri/Cargo.toml`, both dev help entry points, targeted Prettier, scoped rustfmt, and `git diff --check` passed. The cache maintenance script inspected the normal `src-tauri/target` at **93.75 GiB** without cleaning it. A natural launcher rebuild cleanly stopped PID `53376`, released/restored the proxy, and started PID `37500`; the exiting old generation still produced the previously diagnosed duplicate reasons, while the new generation contains the single-record fix. The new single-record path was not forced through another restart. Full `cargo test` was attempted but could not replace the running `src-tauri/target/debug/cc-switch.exe` (`Access is denied`).
- **What NOT to do again:** Do not record clean exit before cleanup, maintain separate safe/unsafe dev supervisors, honor alternate target roots, omit repository-level Cargo inputs from rebuild tracking, or stringify typed process timestamps before identity comparison. `watch-ccswitchmulti.ps1 -PlanOnly` remains operationally unverified because this host's AMSI provider blocks the unsigned wrapper as malicious content under both PowerShell engines; resolve that through signing/reputation or explicit security policy, not code obfuscation.

## 2026-08-31 - Correction: delayed rebuilds use authenticated clean exit before force fallback

- **What happened:** The delayed launcher temporarily restarted every verified development generation with immediate `taskkill /T /F`. Cargo consequently printed `process didn't exit successfully`, and CCSwitch recorded an unclean exit even though the supervisor intentionally continued with a rebuild.
- **Root cause:** A listener-handover fix replaced the existing authenticated shutdown-marker path instead of retaining it as the normal path and limiting verified tree termination to recovery.
- **What we did:** Restored the per-launch random-token shutdown marker and app-side consumer. Rebuilds now request authenticated self-shutdown, wait for the verified process and owned listeners to exit cleanly, and use PID-identity-checked tree termination only after a bounded timeout. The five-minute quiet period, timestamped edit diagnostics, `--no-rebuild`, and stuck-fallback reporting remain intact.
- **Evidence:** `node --check scripts/dev-tauri-delayed.mjs` passed; launcher tests passed **38/38**, including no-taskkill graceful exit, verified tree fallback after timeout, and stuck-tree reporting; authenticated marker consumption passed **1/1**; `cargo check --manifest-path src-tauri/Cargo.toml`, targeted Prettier, scoped rustfmt, and `git diff --check` passed. The already-running launcher loaded the previous JavaScript generation, so the corrected graceful path was not forced live; no running `cc-switch.exe` was terminated.
- **What NOT to do again:** Do not make forced termination the normal rebuild path, remove authenticated cleanup while fixing listener ownership, or describe Cargo's forced-child exit as a compilation failure.

## 2026-08-31 - Correction: preserve only proven standalone Codex delegation outputs

- **What happened:** Strict Responses providers rejected historical `function_call_output` items whose call IDs had no corresponding call, including persisted `codex_app.send_message_to_thread` delegation deliveries.
- **Root cause:** Codex Desktop can persist these task-coordination deliveries as standalone `fco_*` output items. They are contextual messages, but replaying them as tool outputs violates the Responses call/output dependency contract.
- **What we did:** Added a protocol-boundary repair limited to orphaned `codex_app.send_message_to_thread` outputs with `fco_*` identities. Their payload is preserved as ordinary user context. Matched outputs and unknown orphan producers remain unchanged and diagnosable.
- **Evidence:** The persisted rollout was audited and contained the standalone delegation outputs without earlier matching calls; the focused repair regression passed **1/1**; Request Health tests passed **4/4**; `cargo check --manifest-path src-tauri/Cargo.toml` and scoped rustfmt passed.
- **What NOT to do again:** Do not delete arbitrary orphan outputs, synthesize fake calls, or broaden this repair beyond a producer proven from persisted rollout evidence.

## 2026-08-31 - Correction: compact/fork handoff must wait for continuation completion

- **What happened:** The coordinator could create a continuation and submit `turn/start`, then immediately drop its temporary app-server process. The continuation turn became `interrupted`, and its handoff message was not durably completed.
- **Root cause:** Successful submission was treated as successful handoff even though the temporary app-server owned the running continuation turn.
- **What we did:** Keep the app-server alive until the matching `turn/completed` notification arrives for both the continuation thread and returned turn ID. Only `completed` is accepted; `failed` and `interrupted` are reported as handoff failures. Current `item/completed` context-compaction notifications and the legacy `thread/compacted` notification are both accepted.
- **Evidence:** Coordinator tests passed **6**, with the explicit live-only test ignored by default; an opt-in live run created continuation `01a057be-6c24-7b12-b2cc-6517ea94c84c`, persisted the handoff message, and completed turn `01a057be-6d8d-7862-b551-c31e3eb5c4b7`; `cargo check --manifest-path src-tauri/Cargo.toml` passed.
- **What NOT to do again:** Do not report handoff success after `turn/start` acknowledgement alone or terminate the owning app-server before the matching terminal notification.

## 2026-08-31 - Delayed dev launcher now explains and can suppress backend rebuilds

- **What happened:** `pnpm dev` could repeatedly restart the Tauri backend while unfinished Rust changes were still being edited, with no reliable timestamp showing which edit caused the restart.
- **Root cause:** The custom delayed launcher intentionally watches backend inputs and schedules a restart after the quiet period; the behavior was content-correct but opaque, and there was no opt-out mode.
- **What we did:** Added `--no-rebuild` plus `TAURI_DEV_NO_REBUILD=1`, preserving frontend HMR and backend content detection while suppressing every backend restart. Genuine backend edits now log UTC detection times and paths; rebuild logs include the trigger time and latest detected edit. Reverted edits continue to cancel a pending rebuild.
- **Evidence:** `node --check scripts/dev-tauri-delayed.mjs` passed; launcher Node tests passed **37/37**; targeted Prettier and `git diff --check` passed; `pnpm typecheck` passed; `cargo check --manifest-path src-tauri/Cargo.toml` passed. Full frontend completed **158 files: 154 passed, 4 failed; 1326 passed, 8 failed**, with failures in concurrently edited UI tests. Full backend was attempted but failed before completion because the live `src-tauri\\target\\debug\\cc-switch.exe` was locked (`Access is denied`).
- **What NOT to do again:** Do not infer that `pnpm dev` is only Vite HMR; it also owns a delayed Tauri restart loop. Use `pnpm dev -- --no-rebuild` while editing unstable backend code, and use the timestamped logs before changing watcher logic again.

## 2026-08-31 - MultiRouter UI separated plan management from provider creation

- **What happened:** The live dashboard mixed opening an existing MultiRouter plan, creating a new routing plan, and adding a provider source. OpenCode Go and OpenCode Zen appeared conceptually tied to MultiRouter even though they are ordinary provider presets.
- **Root cause:** The entry modal and AddProviderDialog reused MultiRouter wording for provider-source creation, while the wizard showed all saved sources in one undifferentiated list.
- **What we did:** Existing plans now open the workspace dashboard directly; new-plan creation is secondary. AddProviderDialog is labeled “Add Codex model source”; OpenCode Go/Zen remain provider presets only. The wizard source step separates Selected sources from Available sources and shows endpoint, model count, and protocol/fallback summaries.
- **Evidence:** Live Tauri `PrintWindow` capture showed the dashboard title “MultiRouter dashboard” and the normal `OpenCode Go` provider card. Focused tests passed **29/29** across wizard, AddProviderDialog, and App integration; `pnpm typecheck` passed.
- **What NOT to do again:** Do not make provider presets look like router modes, send users through an edit wizard to open an existing dashboard plan, or put provider credentials/protocol ownership inside the routing-plan editor.

## 2026-08-31 - Desktop inspection now has a bounded manual-handoff rule

- **What happened:** Background desktop inspection successfully captured the live Tauri UI, but repeated attempts to navigate a WebView after hot reload consumed time and user attention after the target state was no longer reachable reliably.
- **Root cause:** The existing non-disruptive rule prohibited foreground disruption but did not cap retries or require immediate manual handoff after failed background manipulation.
- **What we did:** Added `AGENTS.md` rules 22-B and 22-C: one primary stealth attempt, one evidence-based transient recovery, then mandatory manual handoff; after the user opens the requested screen, inspection returns to read-only capture. Consolidated duplicated verification and release prose so rules 2, 8, 9, 10, and 18-22 remain authoritative.
- **Evidence:** A read-only `PrintWindow` capture of the user-opened live Model ordering tab showed the fallback warning, `Responses · per model`, and `Responses · fallback` labels.
- **What NOT to do again:** Do not loop background coordinate guesses, repeatedly rediscover renderer handles, or keep navigating after one evidence-based recovery fails; ask the user to open the exact screen.

## 2026-08-31 - Proxy takeover force-terminates verified competing CCSwitch trees

- **What happened:** Delayed development rebuilds could leave the replacement CCSwitch generation retrying Codex takeover 12 times behind `PORT_OWNERSHIP_GUARD` while the old process still owned port `15721`. The launcher also explicitly prohibited `taskkill /T`, and the runtime takeover guard only waited for a verified compatible instance to exit voluntarily.
- **Root cause:** Both ownership layers were intentionally conservative after identifying the old listener: the dev launcher preferred authenticated graceful shutdown, while `set_takeover_for_app` returned an error even after the listener PID, executable identity, process start time, config scope, and runtime protocol all matched. That preserved the old process tree long enough to block immediate takeover.
- **What we did:** Reversed that policy at both owning layers. The Windows dev launcher now force-terminates the verified `cc-switch.exe` tree with `taskkill /PID <pid> /T /F`, then waits for every captured listener to become bindable. Runtime takeover now force-terminates a fully verified competing CCSwitch tree, retries binding for up to two seconds, and still refuses to kill unknown or partially verified port owners. Removed the unused dev graceful-shutdown marker setup from the launcher.
- **Evidence:** `node --check scripts/dev-tauri-delayed.mjs` passed; launcher Node tests passed **36/36**; targeted Prettier and `git diff --check` passed; `cargo check --manifest-path src-tauri/Cargo.toml` passed; process-identity tests passed **5/5**; proxy-service tests passed **92/92**. Live rebuild replaced PID `37668` with PID `49924`; the old PID disappeared, the replacement owned `127.0.0.1:15721`, and the `12:07:03` startup restored Codex takeover without any `PORT_OWNERSHIP_GUARD` or retry-loop log entries.
- **What NOT to do again:** Do not identify a compatible listener and then merely wait for it to leave, do not restore the blanket prohibition on `/T`, and do not broaden termination to unknown port owners. Windows may re-parent Codex Desktop outside the CCSwitch tree; global name-based Codex termination is not required to release the proxy listener and would kill unrelated Codex sessions.

## 2026-08-31 - Live mixed-protocol UI audit exposed missing protocol provenance

- **What happened:** A non-activating audit of the running Tauri window confirmed that Model ordering consistently used full provider labels, but the existing `zmhub-responses` provider still appeared as a legacy single-protocol source. Database inspection found 26 catalog models with no model-level protocol fields, all inheriting the provider-level Responses fallback. The compiler computed protocol provenance, but projection discarded `apiFormatSource`.
- **Root cause:** Existing provider data had never been re-probed into per-model protocol groups, and the projection boundary serialized `apiFormat` without its source. The UI therefore could not distinguish a true per-model classification from a provider fallback.
- **What we did:** Preserved `apiFormatSource` through Rust projection and frontend catalog construction; Model ordering now labels transports as `Responses/Chat · default/per model`; mixed-provider readiness now renders two explicit subgroup blocks; the detection dialog replaces unbounded comma-separated model text with counted, scrollable model chips. Added `AGENTS.md` rule 22-A requiring non-disruptive desktop inspection and forbidding a localhost renderer from being reported as the live Tauri UI.
- **Evidence:** Targeted frontend tests passed **140/140** across `CodexFormFields`, `CodexProviderReadinessSection`, and `CodexRouterWorkspacePage`; targeted Rust projection test passed **1/1**; `pnpm typecheck`, Prettier checks, scoped rustfmt check, and `git diff --check` passed. The live Tauri window was inspected using `PrintWindow` and background window messages without cursor movement or foreground activation after rule 22-A was introduced.
- **What NOT to do again:** Do not equate an effective protocol with an explicit per-model classification, drop provenance at projection boundaries, infer provider identity by destructive string rewriting, or claim that a standalone Vite browser tab is the running desktop application.

## 2026-08-31 - Mixed Codex protocol classification made authoritative per catalog row

- **What happened:** The one-provider mixed-protocol flow could preserve stale `apiFormat` fields for catalog rows that were no longer present in either the Responses or Chat detection group; the add-provider save helper also matched model IDs case-sensitively.
- **Root cause:** Detection results were applied only when a protocol was found, so old row metadata remained authoritative by accident. The save path did not share the catalog identity normalization used by the form path.
- **What we did:** Fresh protocol detection now stamps both canonical camelCase and legacy snake_case protocol fields for matched rows and removes all protocol metadata from unmatched rows. The one-provider save path now normalizes model identities case-insensitively and clears stale fields instead of falling back to old classifications.
- **Evidence:** `pnpm exec vitest run tests/components/AddProviderDialog.test.tsx tests/components/CodexFormFields.test.tsx` passed **2 files, 53 tests**; `pnpm typecheck` passed; Prettier checks and `git diff --check` passed.
- **What NOT to do again:** Do not merge protocol groups by raw display/model strings or leave prior per-model transport metadata in place after a fresh probe.

## 2026-08-31 - Whole-codebase duplicate/conflict audit found cross-layer contract gaps

- **What happened:** A repository-wide audit found duplicate Codex provider catalog entries, a frontend Tauri command with no handler registration, and frontend provider metadata fields that Rust silently dropped during round-trip serialization.
- **Root cause:** The Codex preset catalog contained seven repeated top-level names with stale endpoint/model definitions; `update_toml_common_config_snippet` was implemented and invoked but omitted from `generate_handler!`; `ProviderMeta` did not model the frontend's `apiFormatSource` and `codexPresetId` fields.
- **What we did:** Removed only the seven stale duplicate preset objects, retained the separate OpenCode Zen preset, registered the missing command, added both metadata fields with camelCase serde names, and added regression tests for preset uniqueness, Zen visibility, metadata round-tripping, and literal invoke/handler parity.
- **Evidence:** Targeted Vitest passed **34/34** across the preset, provider-form, chat-preset, and command-registration files; targeted Rust tests passed **3/3** for metadata round-trip and TOML snippet updates; `pnpm typecheck` passed; `cargo check --manifest-path src-tauri/Cargo.toml` passed; duplicate preset scan reported **0** duplicates; IPC scan reported **335** invokes, **358** handlers, **0** missing, **0** duplicate registrations. Full frontend ran **158 files: 157 passed, 1 failed; 1320 passed, 3 failed**, with all three failures confined to the concurrently edited `CodexSubagentV2ProfileEditor.test.tsx` exact-payload assertions. Full backend was blocked when Cargo could not replace the live `src-tauri/target/debug/cc-switch.exe` (`Access is denied`); the running dev process was not terminated.
- **What NOT to do again:** Do not assume catalog arrays are unique because the picker renders; do not add a Tauri command without an AST parity check against `generate_handler!`; do not add frontend metadata without verifying Rust serde round-trips; do not kill a live `cc-switch.exe` merely to force a full test run.

## 2026-08-30 - Selective upstream exit-recovery port and orphan cleanup

- **What happened:** The BigStrongSun `ccswitchmulti` PR #73 addressed a related recovery symptom: normal exits could be classified as unclean and takeover backups could be lost. Our fork also had Windows `ReplaceFileW` backup orphans, retry-amplified takeover outcomes, and a delayed-dev restart race.
- **Root cause:** PR #73's clean-exit classification was not present in this fork, while its older `proxy.rs` could not be cherry-picked safely across this fork's OAuth, MultiRouter, and recovery-outcome changes. Our own backup writer passed a temporary backup path to `ReplaceFileW` and did not remove it after success. The dev launcher previously raced `taskkill` against listener cleanup.
- **What we did:** Ported `CleanExit` classification and macOS `proc_pidpath`/`proc_pidinfo` identity handling manually. Adapted clean-backup preservation/rebuild into the current OAuth-aware proxy service, removed the obsolete narrow restore helper, and rejected malformed or proxy-placeholder backups as preservation sources. Retained our independent Windows backup cleanup, terminal quota detection, per-attempt outcome suppression, and authenticated polling shutdown.
- **Evidence:** App-exit tests passed **8/8**; process-identity tests passed **4/4**; proxy service tests passed **92/92**; `cargo check --manifest-path src-tauri/Cargo.toml` passed; `pnpm typecheck` passed; scoped `rustfmt --check` and `git diff --check` passed. Two natural dev rebuilds recorded `clean_exit` with `reason=dev_launcher_restart`; subsequent generations had **0** startup failures, **0** proxy-listener ownership outcomes, and **0** non-info outcomes. `/status` reported takeover on port `15721` with **4/4** successful requests. Exactly **1,318** historical files matching `*.tmp.<pid>.<timestamp>.<counter>.backup` were verified under `C:\Users\Felix\.cc-switch` and deleted; remaining count is **0**.
- **What NOT to do again:** Do not cherry-pick PR #73 wholesale; its backend CI was red at Clippy and its service code predates this fork's OAuth and MultiRouter contracts. Do not reintroduce immediate `taskkill` for normal dev rebuilds, preserve proxy placeholders as user backups, or treat a clean-exit event as crash-recovery evidence.

## 2026-08-30 - Dev rebuilds now hand proxy listeners over before relaunch

- **What happened:** A delayed `pnpm dev` rebuild could leave the replacement CCSwitch generation unable to restore Codex takeover while Codex Desktop remained open. Startup retried port `15721` twelve times, reported `PORT_OWNERSHIP_GUARD`/Windows error 10048, and only recovered after the old socket state eventually cleared or takeover was toggled manually.
- **Root cause:** The development launcher identified the correct Cargo-owned `cc-switch.exe` but always terminated it with `taskkill /F`. That bypassed `ExitRequested -> cleanup_before_exit()`, recorded every rebuild as an unclean exit, and started the replacement after WMI reported process disappearance without proving the old listener endpoint was bindable again. A plain non-forced close was also insufficient because the application intentionally converts ordinary close requests into hide-to-tray.
- **What we did:** Added a debug-only, per-launch random-token shutdown marker. Immediately before a verified non-forced close request, the launcher arms the marker; the app consumes it in `CloseRequested`, calls `app.exit(0)`, and therefore runs the existing proxy stop/Live-config restore path while preserving the persisted takeover switch. The launcher retains a PID-identity-checked forced fallback, never traverses through Codex descendants, captures every TCP listener owned by the verified app, and blocks the next generation until it can bind and release each captured endpoint itself.
- **Evidence:** `node --check` passed for both launcher files; `node --test scripts/dev-tauri-delayed.test.mjs` passed **36/36**; script Prettier and scoped `git diff --check` passed; `cargo test --manifest-path src-tauri/Cargo.toml dev_shutdown_marker_requires_the_exact_token_and_is_consumed --lib` passed **1/1**; `cargo check --manifest-path src-tauri/Cargo.toml` passed. Repository-wide `cargo fmt --check` remains red on pre-existing formatting drift in other dirty files. The already-running launcher loaded the previous JavaScript implementation, so the new end-to-end graceful handoff has not yet been live-tested; no user-owned `cc-switch.exe` or Codex process was manually terminated.
- **What NOT to do again:** Do not equate process disappearance with listener reusability, do not use `/F` as the normal dev-rebuild path, do not rely on an ordinary window close when close-to-tray is enabled, and do not “fix” this lifecycle race by only increasing startup takeover retries.

## 2026-08-30 - Correction: dev rebuilds require a build-input content baseline

- **What happened:** The delayed Tauri launcher still rebuilt the backend 50 seconds after the first successful launch even though no watched backend file had a final `LastWriteTime` during that interval.
- **Root cause:** The earlier watcher fix filtered raw events by file metadata without first recording what the successful build actually contained. The first event for any regular file was therefore still classified as a change, same-size rewrites were ambiguous, and deletion events were discarded. The rebuild gate also had no way to cancel a pending rebuild when edited contents returned to the build baseline.
- **What we did:** Replaced the metadata filter with a SHA-256 snapshot of all watched backend inputs. Batched watcher events now reconcile final file contents against the current build baseline, ignore unchanged files and transient paths, detect additions/deletions, avoid extending the quiet period for duplicate notifications, and cancel the queued rebuild when contents revert. The watcher is armed before the initial baseline is captured so real edits during the first Cargo build still queue exactly one follow-up build.
- **Evidence:** `node --check scripts/dev-tauri-delayed.mjs` passed; `node --test scripts/dev-tauri-delayed.test.mjs` passed **32/32**, including the exact first-launch/no-second-build regression and deletion coverage; script-only Prettier and scoped `git diff --check` passed; `pnpm dev -- --help` loaded the corrected default wrapper. A live first-launch restart was not run because active user-owned `cc-switch.exe` development processes remained running and rule 21 forbids terminating them without explicit permission.
- **What NOT to do again:** Do not infer a source edit from the first `fs.watch` event or from size/mtime alone; compare final contents with the inputs of the running build, and preserve deletion plus edit-revert semantics.

## 2026-08-30 - Startup log storm traced to rollout identity and idle-parent assumptions

- **What happened:** Startup emitted 118 Codex usage-sync warnings that forked rollout filename IDs disagreed with `session_meta.id`, 24 warnings that parent rollouts had not reached the child fork time, and repeated proxy-port recovery warnings during development restarts.
- **Root cause:** Codex uses a valid two-UUID filename form for forked task files: the first UUID is the root metadata ID and the trailing UUID is the actual child thread ID. The importer compared only the trailing UUID to root metadata and rejected valid files. It also incorrectly required a parent rollout to contain an event at or after the child fork timestamp, although an idle or archived parent may legitimately have no event near the later fork. Separately, raw `fs.watch` directory/unchanged events could be treated as backend source edits by the delayed development launcher and schedule an unnecessary second Tauri generation.
- **What we did:** Parsed forked rollout filenames as separate root-metadata and child-thread identities, retained the child ID for usage request identity, and accepted a complete readable parent token timeline even when its last event predates the fork. Added regression tests for both cases. Tightened the development watch path with regular-file content signatures so directory and unchanged events cannot schedule a rebuild. Added `AGENTS.md` rule 6-A requiring fixes at the invariant-owning layer and explicitly labeling unavoidable containment.
- **Evidence:** `services::session_usage_codex::tests::` passed **47 tests, 0 failed, 1 ignored**; both new regression tests passed individually **1/1**; delayed-launcher Node tests passed **27/27**; recovery-outcome tests passed **7/7**; `cargo check --manifest-path src-tauri/Cargo.toml` passed; scoped `git diff --check` passed. A live restart was not forced because a running `cc-switch.exe` was in use and rule 21 forbids terminating it without explicit permission.
- **Don't again:** do not assume every rollout filename contains only one semantic UUID, do not require an idle parent to emit at the exact fork boundary, do not treat raw filesystem watcher noise as proof of a source edit, and do not silence these warnings while leaving the importer or launcher invariant wrong.

## 2026-08-30 - Keep Codex picker provider labels compact across projection paths

- **What happened:** The workspace model-ordering view displayed compact provider tags such as `[ORter]`, while the injected Codex Desktop picker displayed full names such as `[OpenRouter]`.
- **Root cause:** The React ordering formatter and the Rust Desktop catalog projection independently rendered provider-model labels; only the React path applied the five-character compaction rule. The Rust path also stripped only full provider prefixes, allowing compact prefixes to be duplicated.
- **What we did:** Added one Rust compaction helper shared by MultiRouter projection and Desktop projection, aligned the React formatter with the same `OpenRouter -> ORter` and `OpenCode Zen -> OCzen` aliases, and made prefix stripping accept either full or compact labels. Stored provider names and canonical/upstream model IDs remain unchanged.
- **Evidence:** `cargo test --manifest-path src-tauri/Cargo.toml codex_desktop::tests:: --lib` passed 30/30; `cargo test --manifest-path src-tauri/Cargo.toml codex_multirouter::tests:: --lib` passed 2/2; focused Vitest workspace suite passed 84/84; `cargo check --manifest-path src-tauri/Cargo.toml` passed; `pnpm typecheck` passed.
- **What NOT to do again:** Do not maintain separate visible-label algorithms for the workspace and Desktop injection; never compact or rewrite the provider identity field itself.

## 2026-08-30 - Default gpt-5.6-sol sessions to medium reasoning

- **What happened:** New Codex sessions created from generic/custom or universal-provider flows could start `gpt-5.6-sol` at `high` effort, even though the desired default for Sublyx is `medium`.
- **Root cause:** Three creation paths hard-coded `high`: the generated third-party Codex template, the universal-provider default model object, and the Rust universal-provider serializer fallback.
- **What we did:** Made the default model-specific: `gpt-5.6-sol` now defaults to `medium`; other generated models retain `high`. Explicit `reasoningEffort` values and existing hand-authored provider configs are preserved.
- **Evidence:** Rust default-serialization test passed 1/1; focused Vitest config tests passed 11/11; `pnpm typecheck` passed. Full suites and live session verification were not run because unrelated concurrent edits and a running user-owned `cc-switch.exe` remain active.
- **What NOT to do again:** Do not globally rewrite explicit provider effort settings when changing the default; apply the fallback only when no effort was specified.

## 2026-08-30 - Preserve Codex picker reasoning efforts across model-list schema variants

- **What happened:** Codex Desktop model rows still had catalog-backed reasoning metadata, but the picker stopped exposing reasoning effort choices after the renderer/app-server model-list shape changed.
- **Root cause:** The projection exposed `supported_reasoning_levels` and the older `supportedReasoningEfforts` alias, while newer Desktop builds consume `supportedReasoningLevels`. Separately, the injected model-list patch rejected rows whose stable identity was carried as `id`, `slug`, or `name` instead of `model`, so it could not merge the enriched descriptor into those rows.
- **What we did:** Added `supportedReasoningLevels` from the same normalized source as the existing aliases. Hardened the passive picker patch to recognize `model`/`id`/`slug`/`name`, normalize the `model` key in-place, and merge the shared descriptor without changing probe scheduling or inventing capabilities.
- **Evidence:** `cargo check --manifest-path src-tauri/Cargo.toml` passed; the focused catalog projection test passed; a Node VM probe confirmed an `id`-identified row received `supportedReasoningEfforts` and `supportedReasoningLevels`. Full frontend/backend suites and live Codex visual verification were not run because unrelated concurrent edits and a running user-owned `cc-switch.exe` remain active.
- **What NOT to do again:** Do not trigger protocol probing to render picker metadata, and do not assume `model` is the only identity field in app-server model-list responses.

## 2026-08-30 - Codex picker spacing made lifecycle-persistent

- **What happened:** Codex Desktop model rows could collapse into a dense capped list until the user clicked Unlock model menu; the problem returned after CCSwitch restart or renderer rebuild, and the compatibility namespace had accumulated a meaningless `V5/V6` suffix.
- **Root cause:** The renderer's capped flex column lets injected menu items keep `flex-shrink: 1`. The spacing rule was only installed by explicit CDP injection, while the guardian itself started only after takeover lifecycle events. A running Codex process without a CDP port cannot be repaired in place.
- **What we did:** Renamed the in-memory namespace to stable `__ccSwitchCodexAppCompat`; added an idempotent model-picker style installer with a narrow Radix popper marker, `flex-shrink: 0 !important`, a `MutationObserver`, and the existing periodic rerun; made the Codex guardian start during normal CCSwitch initialization so CDP-enabled sessions are repaired after restart, new renderer targets, and catalog changes. Preserved the no-silent-termination behavior for Codex sessions started without CDP.
- **Evidence:** `cargo test --manifest-path src-tauri/Cargo.toml codex_desktop --lib` passed **31/31**; `pnpm typecheck` passed. `cargo fmt --check` remains red on an unrelated pre-existing formatting difference in `src-tauri/src/proxy/providers/openai_compat.rs`. Live visual verification was not run because the user-owned Codex and `cc-switch.exe` processes were left running and Codex has no reachable CDP port.
- **What NOT to do again:** Do not keep incrementing compatibility-key suffixes as a cache-busting ritual; do not rely on a manual unlock button for renderer lifecycle repairs; do not kill or silently relaunch a running Codex process that was started without CDP.

## 2026-08-30 - Codex takeover recovery stops replaying stale errors

- **What happened:** The Codex startup dialog kept offering “Open the logs, then enable proxy takeover again” after a later startup had already restored takeover successfully.
- **Root cause:** Startup recovery was asynchronous, so the UI could read old pending failures before the repair completed. The pending-outcome query had no notion of a later success superseding an earlier failure.
- **What we did:** Pending recovery queries now hide older takeover/port failures when a later successful takeover exists for the same app. Startup takeover retries transient listener contention, preserves the persisted enabled state, and no longer pretends another CCSwitch process's listener is this instance's router.
- **Evidence:** `cargo check --manifest-path src-tauri/Cargo.toml` passed; focused recovery tests passed **7/7**; `git diff --check` passed for owned files. Current `/status` reports the active `cc-switch.exe` owns `127.0.0.1:15721` with `listener_role=takeover`, `current_provider=Codex MultiRouter`, and no failed requests.
- **What NOT to do again:** Do not clear an enabled takeover toggle after a transient port race, and do not surface superseded recovery failures as current incidents.

## 2026-08-30 - MultiRouter no longer projects Chat adapter config as reasoning capability

- **What happened:** Every Codex request logged repeated reasoning-capability parse warnings for routed third-party models: `missing field upstream`.
- **Root cause:** The compiler treated provider-level `codexChatReasoning` transport-adapter settings as a full model `reasoning` capability. The two schemas serve different layers; the adapter object intentionally has no capability `upstream` block.
- **What we did:** Restricted provider-level capability projection to the explicit `reasoning` field. `codexChatReasoning` remains available to the request-conversion layer but is no longer written into generated model capability metadata.
- **Evidence:** Focused compiler regression passed **1/1**; `cargo check --manifest-path src-tauri/Cargo.toml` passed. The live 17:28 startup restored Codex takeover successfully, `/status` reported `100%` success with zero failed requests, and subsequent request logs contained no `reasoning declaration` warnings.
- **What NOT to do again:** Do not reuse similarly named reasoning fields across transport-adapter and model-capability schemas without an explicit conversion.

## 2026-08-30 - Codex startup takeover no longer loops on stale listener errors

- **What happened:** CCSwitch repeatedly displayed “Open the logs, then enable proxy takeover again” for Codex even when the current listener and requests were healthy.
- **Root cause:** A prior dev-watcher `cc-switch.exe` could still own port `15721` while the next instance started. The new instance treated a compatible listener as reusable, then proceeded without owning a server; startup recorded a takeover failure and cleared the persisted Codex toggle. Historical failures were never retired after a later successful takeover.
- **What we did:** Compatible listeners now block takeover until the current instance can bind its own server. Startup retries each enabled app takeover up to 12 times with 500 ms backoff, preserves the enabled state on failure, and lets future launches retry. Successful Codex startup acknowledges superseded listener/takeover errors so resolved failures no longer trigger the recovery dialog.
- **Evidence:** Current logs show the 16:36 startup owning `127.0.0.1:15721` with `/status` reporting `running=true`, `listener_role=takeover`, `current_provider=Codex MultiRouter`, and successful requests. `cargo check --manifest-path src-tauri/Cargo.toml` passed; focused recovery tests passed **6/6**; `git diff --check` passed for owned files. Live process termination was not performed.
- **What NOT to do again:** Do not reuse another process’s listener as if it were local state, do not clear a persisted takeover toggle after transient startup contention, and do not leave resolved recovery failures pending indefinitely.

## 2026-08-30 - OpenRouter inferred Responses formats conservatively fall back to Chat

- **What happened:** OpenRouter model routing could inherit `openai_responses` from inferred/provider metadata even though OpenRouter's normalized endpoint is Chat Completions by default and generic tool metadata does not prove native Responses support.
- **Root cause:** The MultiRouter compiler already honored model/provider `apiFormat`, but did not distinguish an inferred format from a manually selected or probe-verified format.
- **What we did:** Added a narrow OpenRouter guard in `effective_api_format`: only `apiFormatSource=inferred` is downgraded from `openai_responses` to `openai_chat`; explicit model/provider values and probe-backed Responses remain unchanged. Added compiler regressions for inferred fallback, manual override preservation, and probe selection.
- **Evidence:** `cargo test --manifest-path src-tauri/Cargo.toml codex_multirouter::compiler::tests::openrouter -- --nocapture` passed 3/3; the existing mixed model override test passed 1/1; `pnpm exec vitest run tests/config/codexChatProviderPresets.test.ts` passed 8/8; `pnpm typecheck` passed; scoped `rustfmt --check` passed; `git diff --check` passed. Repository-wide formatting/full suites were not run because unrelated concurrent edits and a running `cc-switch.exe` remain in the shared worktree.
- **What NOT to do again:** Do not infer native OpenRouter Responses support from generic model capabilities such as `tools`; require an explicit manual choice or successful per-model/route protocol probe.

## 2026-08-30 - Correction: Codex picker compression came from flex shrinking, not label brackets

- **What happened:** The Codex Desktop model picker rendered a large injected catalog as a dense stack with effectively no space between model names. An earlier 2026-08-29 journal entry incorrectly attributed this to brackets in `[Provider] Model` and claimed the picker had been fixed without inspecting or visually testing the real Codex renderer.
- **Root cause:** In installed Codex build `26.825.5331.0`, the model flyout is a capped flex column (`flex max-h-[250px] flex-col overflow-y-auto`). The injected catalog contains dozens of menu items, and those items retained the default `flex-shrink: 1`, so Chromium compressed every row to fit the 250px container instead of preserving normal row height and scrolling. The renderer source confirms the model options use `data-model-selected`; punctuation does not control row layout.
- **What we did:** Kept provider-prefixed picker labels in the requested `Provider · Model` form and stable routing IDs. Added a narrowly scoped renderer compatibility style that applies `flex-shrink: 0` only to menu items inside the model-option popper, and bumped the compatibility key to V6 so a renderer can install the corrected patch. Removed the false bracket-wrap explanation from the projection code and made the tests follow the compatibility constant.
- **Evidence:** Extracted the exact installed `app.asar` and inspected `webview/assets/app-initial-DWX_sBmZ.js`, including the model options container and its `data-model-selected` marker. `cargo test --manifest-path src-tauri/Cargo.toml --lib codex_desktop -- --nocapture` passed 31/31 after the final selector change. `cargo check --manifest-path src-tauri/Cargo.toml` passed. `git diff --check` passed for the two changed implementation files. Live visual verification was not performed because the running Codex instance has no CDP port and was not terminated.
- **What NOT to do again:** Do not infer native Codex layout behavior from CCSwitch previews or punctuation changes. Inspect the installed renderer and verify the actual picker; never claim a visual fix from catalog/unit tests alone.

## 2026-08-29 - Deep protocol probe can be stopped and light mode avoids expensive capability checks

- **What happened:** The Codex protocol probe modal disabled closing while the backend ran a serial per-model, two-transport deep transaction. There was no cancellation command, so a slow or blocked upstream forced the user to wait through every model and stage.
- **Root cause:** The probe command awaited the full runner directly, and the runner always executed baseline, SSE, reasoning, forced-tool, and continuation stages for both Responses and Chat. The UI only invalidated stale identity state; it could not abort the in-flight HTTP future.
- **What we did:** Added a backend-owned abort-handle registry keyed by probe ID, including a race-safe short-lived cancel-before-registration tombstone. The progress dialog now exposes Stop; cancellation drops the active task/request, releases its lease, discards partial results, and leaves protocol selection unchanged. Added Light/Deep mode control (Deep remains the default): Light performs only one minimal non-streaming baseline request per transport and returns Partial evidence without changing protocol or split-provider routing. Added all locale strings and focused regression tests.
- **Evidence:** `cargo check --manifest-path src-tauri/Cargo.toml` passed; backend cancellation test passed 1/1; Light runner test passed 1/1 and verified exactly 2 requests/model; `pnpm typecheck` passed; focused `CodexProtocolProbeProgressDialog` + `CodexFormFields` tests passed 54/54; owned-file Prettier and `git diff --check` passed. Repository-wide formatting/full suites were not run because concurrent sessions own unrelated files and the dev watcher keeps the Tauri binary active.
- **Don't again:** do not make a network probe modal non-dismissible without an actual backend cancellation path, and do not use baseline reachability evidence to auto-select protocol or create routing splits.

## 2026-08-29 - Marker-only exits no longer claim configuration recovery failed

- **What happened:** Every developer-watcher restart or externally terminated app run left `app-run-marker.json`, and the next launch showed a persistent warning saying the previous run did not exit cleanly and configuration recovery needed review even when every recovery step succeeded.
- **Root cause:** Startup persisted `UncleanExit` as a warning before checking recovery results. Marker presence proves only that the prior process missed a clean-exit hook; it does not prove configuration loss. The recovery ledger then retained each unacknowledged false warning across generations.
- **What we did:** Kept marker-only exits in the diagnostic event log but downgraded their startup classification and log entry to informational. On each new ledger generation, historical `startup_classification/uncleanExit` warnings are retired and acknowledged. Confirmed crashes, active duplicate instances, and actual live-config or takeover failures retain warning/error severity.
- **Evidence:** Focused recovery-outcome tests passed 5/5; app-exit-monitor tests passed 5/5; the exact regression test passed 1/1; `cargo check --manifest-path src-tauri/Cargo.toml` passed; scoped Rustfmt passed. The live Tauri watcher rebuilt and relaunched generation 49: marker-only `uncleanExit` was `info`, live-config recovery and takeover all succeeded, `pending_non_info=0`, proxy health returned HTTP 200, and port 15721 remained owned by the relaunched debug app. Repository-wide `cargo fmt --check` remains inconclusive because concurrent unowned `src-tauri/src/proxy/providers/openai_compat.rs` needs formatting.
- **Don't again:** do not equate an uncleared process marker with failed configuration recovery, and do not publish a recovery-review warning before an actual recovery operation reports failure.

## 2026-08-29 - Codex model picker drops bracket-injected prefixes, switch to `·` separator

- **What happened:** The Codex desktop model picker wrapped each model row across multiple lines and looked cramped because the catalog was being projected with a `[Provider] Model` prefix in `displayName`. Bracket characters in those in-line labels were triggering the picker renderer to break rows on the bracket boundaries, producing the dense, hard-to-read layout shown in the clipboard capture.
- **Root cause:** Commit `d2308ad8` ("fix(codex): address model display, catalog, and lite api bugs") had rewritten `project_codex_model_descriptor` in `src-tauri/src/codex_desktop.rs` so the picker received `[Provider] Model` strings instead of plain model names, and had also stamped bracket-style defaults into all four i18n locale files for the workspace display-style selector. The two `[ ]` characters in the in-line label were getting picked up as wrap hints by the Codex picker, while a sibling template `modelThenProvider` ("Model · Provider") using a middle dot rendered cleanly.
- **What we did:** Removed the `displayNameStyle`-driven bracket injection in `project_codex_model_descriptor`; `displayName` is now the plain model name, and `providerName` is sent as a separate field that the Codex picker renders in its own badge column (Q1). Replaced the bracket-prefixed default in the `codexRouterWorkspace.providerThenModel` i18n template across `en.json`, `zh.json`, `zh-TW.json`, and `ja.json` with the same `·` separator used by the sibling `modelThenProvider` template, so any locale that ever surfaces the default text is visually consistent. Provider prefix still goes everywhere — it is just no longer in-line with the model name. Q2 (delete the unused `CodexModelDisplayStyle` enum, `displayNameStyle`/`modelDisplayStyle` fields, and the dropdown itself) is intentionally deferred per user direction "fix the first step first then only think about this".
- **Evidence:** `cargo test --manifest-path src-tauri/Cargo.toml --lib codex_desktop` passed 31/31, including `catalog_projection_labels_and_groups_provider_models_without_changing_ids` which now asserts plain `displayName` ("Beta", "Alpha", "Zeta") and a separate `providerName` field. `pnpm typecheck` passed. Focused `CodexRouterWorkspacePage.test.ts` passed 84/84. `git diff` shows the only files touched are the four locale files; the `displayNameStyle` plumbing in `codex_desktop.rs` left by Q1 is intentionally kept until Q2.
- **Don't again:** do not re-introduce `[Provider]`-style inline prefixes in any field that the Codex native renderer may interpret as text; render provider identity as a separate field or use a separator character that does not trigger row wrap heuristics. Also: when changing i18n template defaults, update all four locales in the same commit (en/zh/zh-TW/ja).

- **Update (2026-08-29, same day):** The picker still rendered bracket-prefixed rows because Codex Desktop reads the persisted `displayName` from `cc-switch-model-catalog.json` and from the in-memory catalog. The Q1 "plain model name" change removed the visible provider prefix from the picker, but on-disk `cc-switch-model-catalog.json` written by older builds still contained `[Provider] Model` strings, and the user reaffirmed they want the provider prefix to stay visible. Updated `project_codex_model_descriptor` in `src-tauri/src/codex_desktop.rs` to re-render the picker label as `Provider · Model` (using the same middle dot separator the workspace display-style dropdown already uses), with the plain model name as the fallback when no provider is attached. The `strip_provider_prefix` helper still normalises any pre-existing `[Provider] Model` strings before re-formatting, so stale on-disk catalogs get cleaned on the next projection. The test in `codex_desktop::tests::catalog_projection_labels_and_groups_provider_models_without_changing_ids` was updated to assert the new `Provider · Model` format.
- **Evidence (follow-up):** `cargo test --manifest-path src-tauri/Cargo.toml --lib codex_desktop` passed 31/31 with the updated assertions; `cargo check --manifest-path src-tauri/Cargo.toml` and `pnpm typecheck` both clean. The JS-side `model_picker_patch_core_updates_existing_labels_and_provider_group_order` test still passes unmodified because it exercises the renderer-side data ordering with stale bracket-prefixed input, not the Rust projection format.

## 2026-08-28 - MultiRouter route workspace preserves provider model catalogs

- **What happened:** Opening Configure Multi-Model Routing automatically called provider model-list endpoints and wrote the results back to Provider records, so merely visiting the routes page could alter model catalogs curated in the Provider form.
- **Root cause:** The routes workspace had gained a mount-driven refresh transaction that treated entering the routes tab as permission to fetch `/models` and call `providersApi.update`. That duplicated the explicit `自动获取并写入模型列表` action in the wizard and violated the Provider form catalog's ownership boundary.
- **What we did:** Disabled model fetching and Provider writes on route-page entry, retained the explicit wizard synchronization button, and preserved the existing default catalog label format `[Provider] model`.
- **Evidence:** `src/components/codex/CodexRouterWorkspacePage.test.ts` passed 67/67; focused route-open preservation and explicit wizard synchronization tests passed 2/2; `pnpm typecheck`, targeted TypeScript Prettier, `git diff --check`, and `cargo check --manifest-path src-tauri/Cargo.toml` passed. Full frontend verification finished with 1,287 passing and 2 failing tests, both reproducible in the unrelated `CodexSubagentV2ProfileEditor.test.tsx` file. Full Rust tests could not complete because Windows first reported paging-file error 1455 while mapping `libcc_switch_lib.rlib`, then could not replace the currently running `src-tauri/target/debug/cc-switch.exe`; that process was deliberately left running. The journal-wide Prettier check remains red on pre-existing historical formatting outside this entry.
- **Don't again:** do not couple navigation into MultiRouter configuration with remote model discovery or persistence; model-list writes require an explicit user action.

## 2026-08-28 - v3.19.2-20 release notes, updater ownership, and toolchain alignment

- **What happened:** GitHub Release `v3.19.2-19` showed only the generic automatic-publication sentence even though the repository contained detailed audits and engineering history. The tag was built from two release-only commits outside `main`: updater ownership/signing changes plus a pnpm 10 workaround for Node 20. Meanwhile `main` already required pnpm 11.24.0, `.node-version` was below pnpm 11.24.0's actual Node requirement, and CI/release workflows mixed Node 20 with pnpm 9/10/11.
- **Root cause:** The Release workflow read only `docs/release-notes/${TAG}-zh.md`; no `v3.19.2-19-zh.md` existed, so it intentionally emitted the fallback body. Release-only tag commits were not merged back to `main`. pnpm 11.24.0 requires Node >=22.13, while the repository pinned 22.12.0 and Actions still installed Node 20. Rust stable advanced to 1.95 and exposed new clippy lints. A cold Settings integration assertion also used the default one-second wait, making the first MSW-backed query flaky.
- **What we did:** Integrated the valid updater commit `6d254f58` through `origin/main`, preserving its `tngflx/ccswitchmultirouter` updater/product/compare links, rotated updater public key, and signing-key ignores; superseded the inherited pnpm-10 workaround with the repository's pnpm 11 toolchain. Prepared `3.19.2-20` across package/Cargo/Tauri/lock sources, pinned Node 22.23.2 and pnpm 11.24.0 in every CI/release workflow, added package engine floors, and generated a 16,758-character release body from complete English, Simplified Chinese, and Japanese notes. The notes document the full upstream mapping (`a7e87d1d`, `584a1834`, `1a3ccd1a`, `8b38cba4`/`23a3df8e`, `b5395d80`, `c911c7e3`, `270a4ff3`, `bd15ea11`, and the manual security subset of `6243e20a`), fork integration boundaries, and explicitly deferred wholesale ports. Updated Rust code for 1.95 clippy compatibility, formatted the previously failing Universal Provider modal, and stabilized the cold Settings test.
- **Evidence:** `pnpm install --frozen-lockfile`, `pnpm typecheck`, full `pnpm format:check`, `cargo fmt --check`, and `cargo clippy -- -D warnings` passed. The release version contract matched `3.19.2-20` in all four sources; all three release-note files existed and assembled to 16,758 characters without the fallback sentence. Final frontend `pnpm test:unit` passed **157 files / 1,306 tests**. Final backend rerun with `CARGO_BUILD_JOBS=2` and `--test-threads=1` passed **3,608 library tests, 0 failed, 6 ignored**, plus **125/125 integration tests**. The first backend attempt was inconclusive before test execution because Windows returned paging-file error 1455 while rustc mapped the test rlib; no source change was made between that failure and the passing rerun.
- **Don't again:** do not publish a release tag from commits that never return to `main`; do not let the release body silently depend on one language-specific file; do not pair pnpm 11.24 with Node below 22.13; do not pin supplemental workflows to older package managers; and do not diagnose compiler paging-file exhaustion as a code regression.

## 2026-08-28 - Authoritative catalog reconciliation and complete MultiRouter UI audit

- **What happened:** The earlier free-model verification incorrectly treated append-only catalog sync as complete and did not visually audit the full English MultiRouter entry, model-source dialog, or mobile wizard. Providers could remove a model from `/models` while CCSwitchMulti kept the stale row, the wizard fetched only the fallback API key, filtered bulk actions left the temporary query active, and several English entry points fell back to Chinese.
- **Root cause:** Catalog refreshes merged discoveries without distinguishing a complete authoritative response from a partial multi-credential response. Remote-bound and manual rows had no explicit reconciliation policy. The form, wizard, and route workspace implemented separate fetch paths, and several `codexMultiRouter` / `codexConfig` locale keys were absent. The wizard shell also retained a fixed desktop sidebar at mobile width.
- **What we did:** Added one shared reconciliation rule: a complete response removes missing rows with an explicit upstream binding, while rows without that binding remain manual; partial credential success merges results but never prunes. Applied it to the provider form, four-step wizard, OAuth sync, and route workspace; the wizard now fetches the fallback plus every enabled grouped key. Search temporarily ignores status tabs, and Include/Exclude Filtered now applies exactly the visible matches, clears search, and opens the corresponding persisted status view. Localized the main CTA, model-source dialog, preset selector, and advanced labels in all four locales, bound workspace translations to the configured i18n instance, and made the wizard responsive with a two-by-two mobile step grid.
- **Evidence:** Focused catalog/router coverage passed **8 files, 217 tests**. `pnpm typecheck`, `cargo check --manifest-path src-tauri/Cargo.toml`, targeted Prettier, and `git diff --check` passed. Browser verification at desktop and **390x844** confirmed all four English wizard steps contain no Chinese fallback text, the source dialog and input placeholders are English, the mobile wizard has all steps reachable without crushed content, and a three-row catalog produced temporary `free` matches before returning to `Included 2` with an empty query. The serial backend suite passed **3,608 library tests, 0 failed, 6 ignored**, plus **125/125 integration tests**. The standard parallel frontend suite was inconclusive under scheduler contention; a lower-concurrency rerun cleared every changed/dependent heavy file, but repository-wide frontend verification remains red because `tests/integration/SettingsDialog.test.tsx` fails **1/5** even when run alone (the mocked General tab never receives `language:zh`).
- **Don't again:** do not call append-only discovery “sync”; do not prune after any credential fails or after an OAuth cache fallback; do not combine status tabs with an active text search; do not rely on locale-key parity without walking the real entry flow; and do not attribute broad-suite resource-starvation timeouts to product regressions without isolated reruns.

## 2026-08-28 - Free-model catalog workflow final verification

- **What happened:** The earlier catalog audit proved the focused workflow but left a readiness-identity test near Vitest's default timeout and did not yet have final full-suite evidence.
- **Root cause:** The readiness test intentionally executes nine complete verify, mutate, and invalidate cycles and consistently takes about 5.3 seconds, slightly longer than Vitest's default 5-second test timeout. This was test-runtime pressure, not a product defect. Model discovery itself remains bounded by what each upstream account endpoint returns for the supplied credentials.
- **What we did:** Kept the existing Model Catalog UI instead of adding a separate free-model category. Confirmed that sync uses the fallback credential plus every enabled grouped credential, deduplicates all returned models, preserves exclusion tombstones, and that local case-insensitive search for `free` covers model, upstream-model, and display-name identities before Include Filtered or Exclude Filtered updates downstream routing, projection, ordering, and spawn-agent candidates. Raised only the nine-cycle readiness test timeout to 15 seconds.
- **Evidence:** Focused catalog/routing coverage passed **6 files, 178 tests**; direct free-model workflow coverage passed **5/5**; Rust model-fetch coverage passed **39/39**; the formatted readiness test passed **1/1 in 5.32 seconds**; `pnpm typecheck`, `cargo check --manifest-path src-tauri/Cargo.toml`, Prettier, and `git diff --check` passed. Final `pnpm test:unit` passed **156 files, 1,297 tests**. Final backend suite passed **3,608 library tests, 0 failed, 6 ignored**, and every integration test binary; the command exited 0. No live provider account or real credential was used.
- **Don't again:** do not promise globally exhaustive free-model discovery beyond upstream account visibility, do not add a redundant free-model category when local catalog filtering already provides the workflow, and do not classify this deterministic nine-cycle test duration as a UI regression.

## 2026-08-27 - Deferred and rejected cherry-pick deep dive

- **What happened:** Audited every previously deferred/rejected upstream commit in the Codex 0.149 migration chain, OAuth/live-write series, WSL2/Pi fixes, and the BigStrongSun v3.19.2-18 release chain.
- **Root cause:** Whole commits from `farion1231/cc-switch` assume a simpler provider and OAuth ownership model than this fork's managed login markers, account pools, takeover recovery, grouped credentials, and MultiRouter projection.
- **What we did:** Rejected the nine 0.149 migration commits and `926af949` as behaviorally superseded; deferred `0ae561b8` because no WSL2 CI job exists here; rejected `5ca9459d` because Pi usage is not implemented; rejected the coupled `c2ec78dd` OAuth rewrite. Ported only independent invariants from `6243e20a` (JWT header validation), `bbe8bb93` (live bearer reconciliation), and `c5e4f705` (`requires_openai_auth` alignment for bearer/env-key tables). BigStrongSun probe/reasoning/profile commits remain represented by adapted fork code.
- **Evidence:** `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` passed; `cargo check --manifest-path src-tauri/Cargo.toml` passed; Codex config focused suite passed **217/217**; focused frontend tests passed **43/43**; `pnpm typecheck` passed. Full-suite verification remains the final step below this entry.
- **Don't again:** do not cherry-pick the 0.149 chain or OAuth rewrite wholesale; preserve the fork's ownership boundaries and port only a tested invariant with a matching call path.

## 2026-08-27 - Targeted verification during active development

- **What happened:** Full frontend and backend suites were being run after every incremental code or test edit, slowing feature iteration and repeatedly surfacing unrelated failures.
- **Root cause:** `AGENTS.md` rule 2 required the full suites after any code change, while the pre-push rule separately required the same verification again.
- **What we did:** Changed the repository policy to require direct feature tests plus tests for plausible dependents and shared contracts during active development. Full frontend/backend suites are now reserved for an explicit final feature-batch boundary, pre-push, release, or final merge/cherry-pick delivery.
- **Evidence:** Reviewed all `AGENTS.md` references to full suites, test suites, merges, cherry-picks, and pre-push verification; rules 2, 9, 14, and the Development Workflow testing guidance now describe one consistent two-phase policy.
- **Don't again:** do not treat every incremental assistant response as the end of the coding session or rerun both full suites when targeted verification covers the changed behavior and its affected contracts.

## 2026-08-27 - Full-suite frontend failures reproduced as parallel-run contention

- **What happened:** The first repository-wide frontend run reported 9 failed tests across App integration, Codex form readiness, preset selection, and Sub-Agent persistence while the MultiRouter changes were under verification.
- **Root cause:** Each reported test passed in isolation. The failures were caused by Vitest file-level parallelism contending over shared Tauri/MSW mocks and the jsdom process budget; several tests then exceeded their existing 5-30 second timeouts. This was not a deterministic regression in the changed MultiRouter behavior.
- **What we did:** Re-ran all four failing areas independently, then bounded Vitest to four workers (`pnpm test:unit` now runs `vitest run --maxWorkers=4 --minWorkers=1`) so global mock state and heavy React mounts do not exhaust the default worker budget.
- **Evidence:** Isolated App integration passed 12/12; CodexFormFields readiness test passed 1/1; Codex preset tests passed 12/12; the three Sub-Agent tests passed 3/3; final `pnpm test:unit` passed **155 files, 1,269 tests**. Full Rust suite passed **3,608 library tests plus every integration test binary**; `pnpm typecheck` passed.
- **Don't again:** do not classify a parallel Vitest timeout as a product regression without reproducing the test alone, and do not claim the default parallel command is green when only the serialized verification passed.

## 2026-08-27 - Canonical coverage for catalog refresh and API-key groups

- **What happened:** The catalog refresh, independent action spinners, and redesigned model-specific API-key-group controls needed coverage at the same component boundaries as the existing provider form tests.
- **Root cause:** The first focused tests covered refresh reconciliation and spinner ownership but did not exercise the complete grouped-key editor or prove that model sync uses only the fallback key plus enabled, deduplicated group keys.
- **What we did:** Extended the existing `renderCatalogHarness` instead of creating a duplicate harness. Added one canonical group lifecycle workflow covering add, label, enablement, rotation, multiple keys, exact models, prefixes, key deletion, and group deletion; added grouped credential selection coverage; strengthened the readiness test to assert both catalog actions lock while either request runs.
- **Evidence:** Focused catalog/readiness/form tests passed 44/44; `pnpm typecheck` passed; targeted Prettier check passed. Final full `pnpm test:unit` run had 1,261 passing and 8 failing tests across unrelated dirty-tree/settings/Sub-Agent areas; its one form readiness timeout passed alone (1/1). Final full Rust run had 3,601 passing, 1 environment-dependent `LOCALAPPDATA` failure, and 6 ignored; the failed test passed alone (1/1).
- **Don't again:** do not create a second form harness for grouped-key UI, do not treat spinner presence alone as sufficient action-state coverage, and do not claim repository-wide green status from focused tests.

## 2026-08-27 - MultiRouter save hang and model fetch regression

- **What happened:** MultiRouter saves reported `probe_in_progress`/`projection_readback_mismatch`, and the wizard's automatic model fetch did not add newly advertised models to providers that already had a catalog.
- **Root cause:** Every Codex provider IPC add/update implicitly ran the deep protocol preflight, even when the wizard had a separate explicit probe step; the wizard also passed `preserveExistingSelection`, whose documented behavior intentionally suppresses new rows. On this machine `models_cache.json` has no `client_version`, so cache sync is intentionally skipped but read-back treated that optional cache as a required owned file.
- **What we did:** Added an explicit `skipAutomaticProbe` IPC option used only by wizard catalog/plan writes, restored append-on-fetch for the wizard action while preserving the reusable selection-preserving helper, and made provider picker labels compact while retaining stable provider identity and upstream model IDs. Read-back now accepts an absent/unversioned optional Codex cache while still requiring catalog/config fingerprints.
- **Evidence:** `cargo check --manifest-path src-tauri/Cargo.toml` passed; focused wizard tests passed 32/32 and helper tests 35/35; workspace tests passed 77/77; catalog sync tests passed 4/4. A combined form suite still has one pre-existing timeout in the readiness-identity test; full suites remain to be run.
- **Don't again:** do not run deep network probes as a hidden side effect of a wizard save, do not use selection-preserving merge mode for an explicit “fetch and write” action, and do not make an intentionally skipped optional cache invalidate every projection save.

## 2026-08-27 - Catalog refresh and grouped API-key UX

- **What happened:** Model catalog repair updated only blank fields, both catalog buttons showed the same spinner, and grouped API keys required an awkward mode toggle before a group could be added.
- **Root cause:** Sync and repair shared one loading boolean; repair used a missing-values-only reconciliation mode; grouped credentials were modeled as mutually exclusive with the fallback key.
- **What we did:** Added a distinct Refresh Existing action state and provider-metadata refresh mode that updates fresh context/input capability values while preserving aliases, order, exclusions, and reasoning overrides. Kept the fallback API key visible and redesigned groups as optional model-specific overrides with a direct Add key group action; enabled groups participate in model discovery and protocol verification.
- **Evidence:** `pnpm typecheck` passed; focused catalog/readiness/form tests passed (40/40); locale key parity is 133/133 across en, zh, zh-TW, and ja; targeted Prettier check passed. Full `pnpm test:unit` was run and reported 14 failures in unrelated pre-existing dirty-tree wizard/settings/Sub-Agent tests, with 1,251 tests passing.
- **Don't again:** do not bind independent actions to one loading flag, do not overwrite user-owned catalog fields during provider metadata refresh, and do not make optional API-key groups hide or erase the fallback credential.

## 2026-08-27 - stream disconnected before completion audit

- **What happened:** User reported Codex failing with `stream disconnected before completion: Upstream Chat Completions stream ended before sending finish_reason` and asked whether recent fork commits or an upstream API change caused it.
- **Root cause:** The message is the intentional strict terminal guard, not a recent fork regression. Upstream commit `6940a4b2` ("fix(proxy): distinguish truncated chat streams from normal completion") added it in June 2026, and upstream main still emits it at `streaming_codex_chat.rs:900`. It fires when a Chat Completions upstream closes SSE without a `finish_reason` and without substantive output, producing `response.failed` with code `stream_truncated`. Upstream issue #3960 is the same message and was closed by the reporter after switching GLM base URL, not by a code fix.
- **What we did:** Located both emit sites in the fork (`streaming_codex_chat.rs:1187` and the hosted-loop path at `:1369`), verified the guard history, inspected tiered recovery, and audited upstream main and open PRs. Fork recovery can retry `response.failed` only in Aggressive mode for Codex Desktop; Safe mode and CLI/other clients are not covered.
- **Evidence:** `git log -S "ended before sending finish_reason"` points to `6940a4b2`; upstream HEAD `270a4ff3` still contains the string; #3960 has no linked fix commit and its comment says switching back to the V4 base URL fixed it. Open upstream candidates: #6092 (content parts/message snapshots), #6319 (reasoning leak), #6461 (tool index isolation), #6718 (commentary/final_answer phases). No merged upstream fix exists yet.
- **Don't again:** do not attribute this message to recent fork commits without checking the generator commit; do not claim upstream fixed #3960; do not recommend tiered recovery as a CLI solution.

## 2026-08-27 - Production executable and NSIS installer verification

- **What happened:** Rebuilt the production executable and the author-style NSIS installer after adding source-commit update detection and grouped Codex API-key UX. The default all-bundles Tauri build reached WiX and failed in `light.exe`; the NSIS-only build generated the installer, then returned nonzero because updater signing has a public key configured but `TAURI_SIGNING_PRIVATE_KEY` was unavailable.
- **Root cause:** A plain release Cargo build can embed Tauri's development URL without bundling the Vite frontend. Separately, `pnpm build` requests MSI as well as NSIS on this machine, and updater artifact signing cannot finish without the private key.
- **What we did:** Built the raw executable with `pnpm build:exe` (`tauri/custom-protocol` enabled), built the installer with the repository author's `pnpm tauri build --bundles nsis` method, and inspected the resulting executable bytes for the hashed Vite `index-` asset marker. Kept the configured `localhost:3000` string because it is normal compiled `devUrl` metadata and is not used when embedded assets are present.
- **Evidence:** `pnpm typecheck` passed; `pnpm test:unit` passed (155 files, 1,263 tests); `cargo test --manifest-path src-tauri/Cargo.toml` passed with exit code 0; `pnpm build:exe` passed; `cc-switch.exe` contains `index-` (`True`), SHA-256 `A4722875D7298BC190A3079D5A2A1C98EB3866E2F671E2186330AC2FA3C543E9`; NSIS installer generated at `src-tauri/target/release/bundle/nsis/CCSwitchMulti_3.19.2-18_x64-setup.exe`, SHA-256 `B9BC1FC55ED085F5AA726170341F594A7066265B97A3CBA509A24F50E69B13FD`. Both Windows Authenticode statuses are `NotSigned`; no updater signature was produced because the private key was unavailable.
- **Don't again:** never ship a production executable from plain `cargo build --release`; require `tauri/custom-protocol` and verify the embedded `index-` marker. Do not describe a signing-key failure as an installer-generation failure when the NSIS artifact was already produced, and do not publish it as an updater release without the private signing key.

## 2026-08-27 - Source commit update detection and grouped Codex key UX

- **What happened:** Added a GitHub compare check against `BigStrongSun/ccswitchmulti` in addition to release updates, reporting the exact number of commits behind and opening the compare page for cherry-picking. Codex provider forms now have an explicit single-key/grouped-key toggle; grouped model sync fetches each key and merges unique models.
- **Root cause:** Release metadata does not surface unreleased upstream commits, and the previous grouped-key panel left the single-key field visible, making credential mode ambiguous. Model sync only queried one credential even when groups were configured.
- **What we did:** Embedded the build commit via `build.rs`, added a proxy-aware `check_github_commits_behind` Tauri command and frontend update metadata, and hid the single key field while grouped mode is enabled. Grouped fetches run concurrently across all non-empty keys, deduplicate model IDs, and preserve existing catalog reconciliation.
- **Evidence:** `cargo check --manifest-path src-tauri/Cargo.toml` passed; `pnpm typecheck` passed; `pnpm vitest run src/lib/updater.test.ts` passed (3 tests). A nonexistent `CodexFormFields.test.tsx` path was attempted and correctly reported no test files; no full suite run yet.
- **Don't again:** do not treat a release-only check as proof the fork is current; always compare the embedded commit to the fork main branch and keep grouped credentials distinct when fetching model catalogs.

> Rules: see [README.md](./README.md). Corrections are new entries — never rewrite old ones.

## 2026-08-27 - Upstream and BigStrongSun cherry-pick audit

- **What happened:** Audited the latest `BigStrongSun/ccswitchmulti` and original `farion1231/cc-switch` commits for safe integration into this fork.
- **Root cause:** Both repositories diverge heavily from this fork's MultiRouter, OAuth, i18n, and traffic-policy architecture; whole-branch merges would overwrite fork-specific behavior.
- **What we did:** Kept BigStrongSun's recent probe/reasoning/profile fixes unchanged because equivalent fork adaptations already exist. Cherry-picked original upstream prompt preservation (`c911c7e3`), OpenCode Go usage (`270a4ff3`, local `a184dae7` plus reset-field compatibility fix `84595b4a`), and Otty terminal support (`bd15ea11`, local `022ccf54` plus locale labels `2295f3e2`). Applied Otty locale additions as isolated hunks to preserve unrelated uncommitted catalog edits. Rejected full OAuth/JWT commit `6243e20a` after clean-worktree simulation showed conflicts in all five fork-sensitive files; the security change requires a dedicated manual port. Pi deduplication was inapplicable because this fork has no `session_usage_pi.rs`.
- **Evidence:** `cargo check --manifest-path src-tauri/Cargo.toml` passed after OpenCode Go and Otty changes; `pnpm typecheck` passed; OpenCode Go focused tests passed (46 Rust tests, 10 Vitest tests); Otty terminal focused Rust tests passed (7/7). A component-path Vitest command for `TerminalSettings.tsx` failed with "No test files found" because no test file exists at that path; no Otty behavior was skipped because the Rust tests passed. Clean cherry-pick simulations applied `270a4ff3` and `bd15ea11` without conflicts.
- **Don't again:** never force the OAuth/JWT commit or merge either remote wholesale; inspect each conflicting hunk and preserve fork-specific contracts before porting security-sensitive changes.

## 2026-08-26 - Retry tiers separate safe replay from bounded Desktop continuation

- **What happened:** The main-page stream retry switch only controlled transparent pre-semantic proxy reconnects, so its broad label implied turn recovery that it did not provide. The requested behavior was to keep safe reconnects by default and optionally submit literal `continue` after a recoverable failed or truncated Codex Desktop turn, stopping after a user-selected bounded attempt count.
- **Root cause:** Replaying a stream after semantic output is unsafe, while starting another turn requires Desktop conversation authority. A single boolean conflated those different ownership and safety boundaries.
- **What we did:** Replaced the boolean UI with compact Off/Safe/Aggressive tiers; Safe and Aggressive allow five pre-semantic reconnects, while Aggressive observes the final client-facing Responses SSE stream and submits `continue` through CDP up to 1-3 times. Recovery is limited to verified local Desktop requests, excludes compaction and valid tool calls, suppresses permanent/cancelled failures, preserves state only for the exact injected continuation turn, and does not charge failed CDP submissions against the attempt budget. Legacy `enableStreamRetry` settings remain compatible.
- **Evidence:** `cargo check --manifest-path src-tauri/Cargo.toml` passed; `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` passed; focused Rust tests passed: recovery coordinator 8/8, streaming retry 38/38, handlers 94/94; `pnpm typecheck` passed; retry selector Vitest 2/2 passed; retry-related frontend files passed focused Prettier; full `cargo test --manifest-path src-tauri/Cargo.toml` passed (3,607 library tests plus every integration test binary); full `pnpm test:unit` passed (155 files, 1,262 tests). Repository-wide `pnpm format:check` still fails only on the unrelated pre-existing `src/components/universal/UniversalProviderFormModal.tsx`. No live failure was injected into the active Codex Desktop/proxy processes, so real CDP composer submission was not destructively exercised in-session.
- **Don't again:** do not call pre-semantic stream reconnect a complete turn retry, do not replay post-semantic output, and do not let generic Codex or external API traffic drive the Desktop composer.

## 2026-08-26 - Codex model picker labels remain source-owned routing preferences

- **What happened:** Long provider-prefixed picker labels made it difficult to identify the actual model. The router workspace now offers compact model labels, two provider-inclusive formats, and model/provider sorting without changing stable model IDs.
- **Root cause:** Persisting a label preference into `modelCatalog` would be incorrect because the catalog is a projection regenerated from schema-v2 routing configuration and is intentionally removed during router-save cleanup.
- **What we did:** Kept `modelDisplayStyle` in `codexRouting`, added it to the compiler fingerprint and projection, and applied it at the Desktop picker boundary. The UI restores custom drag ordering on a drag operation and shows the stable ID below a differing friendly label.
- **Evidence:** `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` passed; `cargo check --manifest-path src-tauri/Cargo.toml` passed before unrelated in-progress turn-recovery edits; focused Rust tests `catalog_projection_labels_and_groups_provider_models_without_changing_ids` and `schema_v2_router_save_removes_legacy_derived_catalog_storage` each passed; `pnpm typecheck` passed; `pnpm test:unit -- src/components/codex/CodexRouterWorkspacePage.test.ts --run` passed (77 tests); full frontend `pnpm test:unit` passed (155 files, 1262 tests).
- **Don't again:** do not persist a UI-only display option into the derived model catalog or restore automatic `[Provider]` prefixes by default; retain model identifiers independently of the rendered label.

## 2026-08-26 — Documentation overhaul: README, LICENSE, retry architecture doc, credits

- **What happened:** Cleaned up inherited cc-switch branding (sponsor section with 15+ ads, Trendshift/Star History badges pointing to farion1231), added proper fork attribution chain in LICENSE and README Acknowledgments, created `docs/architecture/retry-model.md` consolidating the two-layer resilience explanation.
- **Root cause:** README was inherited wholesale from cc-switch upstream without cleanup — sponsor content, badges, and issue links all pointed to the wrong repo. No consolidated retry architecture doc existed despite the two-layer model being a critical design boundary.
- **What we did:** (1) Replaced sponsor section with Acknowledgments table crediting farion1231 and BigStrongSun; (2) Updated badges to point to tngflx/ccswitchmultirouter; (3) Added fork-specific features list to header and Features section; (4) Added copyright chain to LICENSE; (5) Created retry-model.md documenting proxy reconnect vs Codex client retry layers; (6) Updated CONTRIBUTING.md and SUPPORT.md issue links; (7) Added retry-model reference to AGENTS.md.
- **Evidence:** README reduced from ~600 to ~453 lines (removed ~180 lines of inherited sponsor ads). All files verified by reading back content after edit.
- **Don'"'"'t again:** never inherit upstream README without auditing every badge, link, and sponsor block for correctness against the fork'"'"'s own repository.

## 2026-08-26 — Deep protocol probe adopted from upstream (1a3ccd1a) without losing fork features

- **What happened:** Upstream rewrote Codex protocol probing from a browser-side shallow check (one non-streaming request per protocol) to a backend-driven deep probe (baseline → SSE → reasoning → forced-tool → tool-continuation) with a live per-model progress dialog. Their `CodexFormFields.tsx` rewrite would have deleted our i18n, traffic policy, catalog sync, and API-key-group UI.
- **Root cause of the conflict:** upstream's file never contained our fork additions; adopting it wholesale = silent feature reversion.
- **What we did:** transplanted the feature, not the file — backend (`protocol_compatibility/*`, byte-identical to upstream) + new dialog (i18n-wrapped, logic-identical `failureLabel`/`reasoningLabel`) + rewired our form's `handleProtocolProbe` to the preflight command; deleted all old shallow-probe helpers (zero references remain).
- **Bonus bug found:** our `createCatalogRow` dropped the `enabled` flag (upstream carries it), so disabled catalog rows silently re-enabled on load. Fixed; covered by ported test "omits disabled catalog models from the deep-probe request".
- **Evidence:** `pnpm typecheck` ✅; targeted 36/36; full frontend 154 files / 1251 tests ✅; commit `47e18b20`.
- **Don't again:** never adopt an upstream UI file wholesale when the fork has diverged — diff their function-level core and transplant that instead.

## 2026-08-26 — Upstream v3.19.2-18 cherry-pick audit (7 code commits)

- **Verdicts:** `a7e87d1d` (route profile sync), `584a1834` (runtime inheritance), `1a3ccd1a` (deep probe), `23a3df8e` (reasoning semantics), `b5395d80` (521 outage classification), `128aaf4d` (version bump) all applied with core logic verified byte-identical or logic-identical; `8b38cba4` was intentionally superseded by upstream's own `23a3df8e` — final state matches upstream final (no `CodexReasoningClient` leftovers anywhere).
- **Evidence:** per-commit symbol checks + byte-identity diffs for `protocol_compatibility/*`, `dao/providers.rs`, `handler_context.rs`, `lib/api/protocol-compatibility.ts`; full Rust 3572/0, frontend 1251/1251.
- **Don't again:** intermediate self-reverting commit pairs (8b38cba4→23a3df8e) must be audited against upstream's _final_ state, not per-commit.

## 2026-08-26 — Commit history consolidation: 4 commits was too coarse

- **What happened:** squashing ~27 fork commits into 4 mega-commits hid feature boundaries and was rejected in review.
- **Fix:** rebuilt as 16 coherent feature commits (provider schema → proxy policy → usage → codex routing → infra sync → services → backend tests → UI layers → presets → i18n → CI → frontend tests), verifying `git diff old-tested..HEAD` was empty so all prior test results stayed valid.
- **Evidence:** pushed as `da53d93c` after byte-identity check; backup branch `codex/pre-consolidation-backup` retained.
- **Don't again:** force-pushing rewritten history without the tree-identity check invalidates every previously-run test result.

## 2026-08-26 — Adopted author-style AGENTS.md codebase guide; validation caught 3 real errors

- **What happened:** enriched AGENTS.md with an author-style "Architecture & Conventions" guide (upstream's AGENTS.md `4c2f6485` was never merged to their main; adapted it to our fork). Added `scripts/validate-agents-md.ps1` to verify every documented path/command/identifier.
- **Root cause of errors found:** wrote docs from memory instead of verifying — three claims were wrong: (1) `codex_traffic_policy.rs` is admission/rejection retry policy, NOT official-vs-third-party routing; (2) `official_first`/`third_party_first` is the Sub-Agent V2 selection policy (`types/codexSubagentV2.ts`, `forwarder.rs`); (3) `CodexApiKeyGroup` backend logic lives in `proxy/providers/codex.rs` (settings key `codexApiKeyGroups`), not `provider.rs`.
- **Evidence:** validator 44/44 PASS after fixes.
- **Don't again:** never document a module's purpose from its name alone — read the module header first; and always run the validator after editing AGENTS.md.

# 2026-08-28 - Responses-Lite compaction rejection and curated catalog refresh

- **What happened:** Codex compaction requests routed through Sublyx repeatedly failed with HTTP 400: `X-OpenAI-Internal-Codex-Responses-Lite requires reasoning.context to be all_turns`; model refresh also reintroduced remotely fetched models into manually curated provider catalogs.
- **Root cause:** the Lite request normalizer preserved the client `reasoning` object without enforcing the upstream-required `context`, while the catalog refresh path always appended fetched models when a catalog already existed.
- **What we did:** Lite normalization now preserves existing reasoning fields and forces `reasoning.context = "all_turns"`; refresh merges only metadata for existing catalog identities and appends fetched models only when the catalog is empty. Updated focused wizard tests to assert curated catalogs stay unchanged.
- **Evidence:** live `~/.cc-switch/logs/codex-router.log` showed repeated `codex_request_kind=compaction` 400s through `https://api.sublyx.org/v1/chat/completions`; targeted Rust test passed (`1 passed`), `cargo check` passed, focused Vitest passed (`3 files, 159 tests`). `pnpm typecheck` passed earlier in this turn.
- **What NOT to do again:** do not treat a complete `/models` response as permission to add models to an existing user-curated catalog; do not send Responses-Lite without the required `reasoning.context=all_turns` field.

# 2026-09-01 — Request-health first-turn gate and Codex executable resolution

What happened: oversized Codex requests were blocked immediately, so native compaction/continuation never got a chance; handoff diagnostics also described `codex app-server` as unavailable.

Root cause: request health had no per-session input-token history and treated every over-limit request as block-worthy. The coordinator launched the bare `codex` name, which is ambiguous on Windows even though the installed CLI exposes `app-server`.

What we did: allow the first oversized request through, then block only a subsequent same-session request that remains above the limit without at least a 20% input-token reduction. Resolve `codex.exe` explicitly from `CODEX_EXECUTABLE` or PATH. The coordinator remains a separate local stdio JSON-RPC process; it does not attach to the Desktop app-server process.

Evidence: `codex app-server --help` succeeds; process audit found the live Codex app-server as PID 45388. `pnpm typecheck` passed. Rust test execution reached the crate but was blocked by `src-tauri\\target\\debug\\cc-switch.exe` access denied because PID 22888 is running; no alternate target directory was used.

What NOT to do again: do not call the command “nonexistent” without checking the installed CLI and process table, and do not block the first oversized turn before observing whether subsequent input shrinks.

# 2026-09-09 - Image-aware request review and summary handoff completion boundary

- **What happened:** A live Windows Request Health approval selected “Summarize + new session” at 10:42:48 on September 9, 2026, but the handoff failed at 10:44:20 after waiting for a native compaction item that never appeared in `thread/read`. Large requests containing an inline image were also being measured as ordinary sustained history growth.
- **Root cause:** The renderer compatibility layer required `thread/read` to expose `contextCompaction`, although current Codex Desktop builds can complete compaction without projecting that item there; the rollout summary is the authoritative persisted boundary. Request Health had media accounting for diagnostics, but sustained-growth review did not exclude media-dominated payloads.
- **What we did:** Removed the renderer-side compaction-item wait, extended the Rust rollout-summary wait to 90 seconds, excluded media-dominated diagnostics from sustained-growth/preflight review, and added explicit transformed/wire byte accounting at the owning log boundaries. The existing Windows “Don't remind me (continue)” action persists `windows_notifications_enabled=false` and now remains part of the tested path.
- **Evidence:** `pnpm vitest run tests/lib/codexSummaryHandoff.test.ts` passed **7/7**; `cargo test --manifest-path src-tauri/Cargo.toml --lib proxy::request_health::tests -- --nocapture` passed **14/14**; the focused compaction detector test passed **1/1**; `pnpm typecheck` passed. Live evidence showed client bodies 603,739–777,976 bytes and transformed bodies 603,153–777,390 bytes, with no proxy inflation; the failed handoff was specifically `Timed out waiting for native Codex compaction`.
- **What NOT to do again:** Do not use a renderer projection as the sole completion signal for Desktop compaction, and do not classify a media-dominated first prompt as replay-history growth without separating client, transformed, retry, and wire sizes.

# 2026-09-09 - Manual coding-agent summaries are mandatory for passover

- **What happened:** The previous implementation interpreted “summarize + new session” as native Codex compaction, including `thread/compact/start` and a rollout-compaction completion check.
- **Root cause:** The handoff contract was wrong: compaction was treated as an acceptable summary producer even though the requested behavior is an ordinary coding-agent-written summary followed by a fresh-session passover.
- **What we did:** Replaced the compatibility-layer summarize step with an ordinary `turn/start` whose prompt forbids tools and file mutation and requires a plain-text handoff summary. The fresh root session is created only after that summary turn completes and returns non-empty text. Added an explicit permanent rule to `AGENTS.md` forbidding compaction for summarize/passover and tests asserting no compaction endpoint is called.
- **Evidence:** `pnpm vitest run tests/lib/codexSummaryHandoff.test.ts` passed **7/7** after the change. The Rust compatibility-script test was rerun after correcting its assertion to distinguish the summary turn from the later fresh-session handoff turn.
- **What NOT to do again:** Never implement “summarize” with native compaction, `/responses/compact`, or `thread/compact/start`; never create the fresh session before a successful manual coding-agent summary exists.
