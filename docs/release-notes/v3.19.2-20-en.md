## Highlights

- Made Codex model-catalog synchronization safer across provider forms, OAuth accounts, MultiRouter, and grouped API keys.
- Preserved user-curated catalogs during refreshes and prevented partial credential failures from deleting account-specific models.
- Added model search with bulk **Include Filtered** and **Exclude Filtered** actions.
- Cleaned authoritatively removed models from routes, custom ordering, Desktop projections, and spawn-agent candidates.
- Improved the four-step MultiRouter workflow with grouped-key fetching, clearer route previews, complete English UI text, and a responsive mobile layout.
- Added `[Provider] Model` labels to the Codex Desktop model picker while keeping stable model IDs for routing.
- Fixed Responses-Lite compaction requests by enforcing `reasoning.context = "all_turns"`.
- Pointed updater and product links at `tngflx/ccswitchmultirouter`, rotated the updater signing key, and added exact upstream-commit reporting.
- Standardized development and release builds on Node 22.23.2 and pnpm 11.24.0.
