# CCSwitchMulti v3.16.5-24

## 修复内容

- 修复 Codex GPT-Live 实时语音 `/v1/live` 路由：HTTP call-create 不再被 raw passthrough 拼成 `.../backend-api/codex/v1/live`，official backend 形态会转换成 `/backend-api/codex/realtime/calls` JSON。
- 新增 `/v1/live` WebSocket 中继：Codex Upgrade 会先完成上游 101 握手再返回，不再被普通 HTTP 转发吞掉。
- 修复未知模型错误归因：raw endpoint 报错固定显示 OpenAI Official，不再错误显示第二位的 DeepSeek/Qwen。
- 同步修正 3.16.5 系列遗留的 URL-only `input_file` 测试断言；该断言过期，不改变发布二进制行为。

## 验证

- 3.16.5-24 分支全量 Rust：`2116 passed / 0 failed / 2 ignored / 1 filtered`。
- 定向覆盖：空 body official 路由、multipart 解析、WebSocket 连接、HTTP call-create 不落到第三方文本路由、错误归因。
