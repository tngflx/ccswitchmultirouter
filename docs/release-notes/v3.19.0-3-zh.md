# CCSwitchMulti v3.19.0-3

## 修复内容

- 正式发布包含 Codex GPT-Live `/v1/live` 修复的 3.19.0 系列版本。
- 修复 HTTP call-create 被 raw passthrough 拼成错误 upstream：official backend 形态会转换成 `/backend-api/codex/realtime/calls?intent=quicksilver&architecture=avas` JSON。
- 新增 `/v1/live` WebSocket 中继：先完成上游 101 握手，再返回 Codex Upgrade。
- 修复未知模型错误归因：`/v1/live` 等 raw endpoint 报错固定显示 OpenAI Official，不再错误显示第二位的 DeepSeek/Qwen。
- 保留 3.19.0-1 的 OAuth 账号池、MultiRouter、模型目录同步、历史恢复与上游合并能力。

## 验证

- 全量 Rust：`2674 passed / 2 ignored / 1 filtered`。
- 定向覆盖：空 body official 路由、multipart 解析、WebSocket 连接、HTTP call-create 不落到第三方文本路由、错误归因。
